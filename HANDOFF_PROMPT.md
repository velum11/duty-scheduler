# 클로드 코드에게 그대로 붙여넣을 지시문

## 1) 재작업 지시 (지금 상태를 고칠 때)

```
DESIGN.md와 아차사고 관리.dc.html을 먼저 읽어라. DESIGN.md는 참고가 아니라 명세다.

현재 구현은 색만 바뀌고 구조가 그대로여서 반려한다. 다음을 구조부터 다시 만들어라:

1. 평가 관리 / 개선조치 관리: 좌우 분할과 체크박스 표를 삭제하고,
   DESIGN.md §1-A 골격(지표 스트립 → 큐 칩 스트립 → 전체폭 상세 → 하단 액션 바)으로 교체.
   "케이스를 선택하세요" 빈 우측 패널을 없애고, 진입 시 큐의 첫 건이 선택된 상태로 렌더.
2. 본문에 있는 빈 아이콘 툴바 띠를 삭제하고, 아이콘은 상단 52px 헤더 바 안으로만.
3. 지표(2건/0건/2건)를 화면 맨 아래에서 제목 바로 아래로 이동, 카드가 아니라 좌측 2px 보더 스타일로.
4. 모든 흰 카드(테두리+radius+그림자)를 제거하고 헤어라인+여백으로 구획.
5. 표 본문 14.5px 이상, 헤더 12.5px 이상으로 키우고 행 높이도 함께 키운다.
6. 사이드바 폴더 아이콘 제거, 모듈 클릭 시 하위 메뉴 접힘, 메뉴 앞 아이콘은 5px 점.

끝난 뒤 DESIGN.md §8 수용 기준 10개 항목을 하나씩 자평해서 결과를 알려줘.
임의 판단으로 명세와 다르게 만들지 말고, 애매하면 구현 전에 질문해라.
```

## 2) 새 화면 만들 때

```
DESIGN.md를 읽고, 만들 화면이 §1의 어느 유형(A 큐 처리형 / B 목록형 / C 스프레드시트 입력형 /
D 폼형 / E 표형 / F 분석형)인지 먼저 한 줄로 선언한 뒤 그 골격대로 구현해라.
색·글자 크기·간격은 §2~§4 값을 그대로 쓰고, 새 값은 만들지 마라.
레퍼런스 마크업이 필요하면 아차사고 관리.dc.html에서 같은 유형 화면을 찾아 구조를 베껴라.
```

## 3) 리뷰 요청할 때

```
방금 구현한 화면을 DESIGN.md §0(금지 8개)과 §8(수용 기준 10개)으로 자체 점검하고,
위반 항목은 바로 수정한 뒤 수정 내역만 요약해줘.
```
