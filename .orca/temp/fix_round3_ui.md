# 수정 라운드 3 — 화면 계층 (ui-feature, 코디네이터 발행)

사용자 실브라우저 피드백 2건 + Codex 평가 발굴 항목. 완료 시 커밋 해시들을 마지막 메시지로(push 금지). views/master_org.py는 절대 건드리지 말 것(별도 워커가 reconcile 작업 중 — writeScope 분리).

## U1. 사이드바 브랜드 상단 여백 (사용자: "교대 근무표가 너무 위에 붙었어")
modules/ui.py 사이드바 브랜드 블록(W 로고+교대 근무표)이 화면 최상단 y=0에 붙어 있음 — 상단 패딩(≈14~18px, dc.html 사이드바 브랜드 패딩 참조)을 줘 숨 쉴 공간 확보. 접힘 레일 상태도 동일 보정. 헤더(52px)와의 시각 정렬 확인.

## U2. 제안서 제출 버튼 → 체크리스트 아래 (사용자 지시)
near_miss_submit: 본문 중간(02 섹션과 03 사진 첨부 사이)의 [제안서 제출] 버튼+안내 문구를 우측 CHECKLIST 패널 하단으로 이동. 본문 순서는 01→02→03(사진)로 자연 종결. 제출 로직·검증·스테이징-후-첨부 계약 불변. 좁은 폭(체크리스트가 아래로 내려가는 반응형)에서 제출 버튼이 사진 섹션 뒤(최하단)에 오는지 확인.

## U3. 조회 전 빈 안내 패널 해소 (Codex M4 — 방침 확정)
- schedule_view(월간): 진입 시 세션 최초 1회 기본 조건(당월·접근 범위 전체)으로 bounded 자동 조회 seed — 매 rerun 자동조회 금지, [조회] 명시 갱신 유지.
- near_miss_view(조회): 최초 1회 최근 90일 기본 조회 seed(기간 셀렉트에 '최근 90일' 상태 반영).
- 계약 근거: 조회만으로 데이터 쓰기 금지 계약은 자동 '읽기' 조회를 금지하지 않음(Codex 확인). 빈 안내 패널은 제거.

## U4. 조건/필터 셀렉트 과폭 content-fit (Codex M3)
공용 condition_panel(views/common/erp/kit.py)에 content-fit 폭 옵션 도입 — 짧은 코드값 셀렉트(연도·월·상태·등급 등)는 값 길이 비례 폭, 검색 인풋만 신축. 소비 화면(편성표·월간·조회·기준정보·분석) 회귀 확인. §0.6 "컨트롤 폭 내용 맞춤" 근거.

## U5. 평가 상세 사진 뷰어 실구현 (Codex M5)
조회/내아차사고의 서명URL 썸네일 렌더를 공용 헬퍼로 추출(views 공용 위치 — modules 금지)하고 near_miss_evaluate 상세의 자리표시자를 교체. 읽기 전용.

## U6. 무음 폴백 3곳 표면화 (Codex M7 — 개별 정책)
- my_schedule.py:278 부근: 편성 스냅샷 조회 실패 시 현재 소속 폴백 사실을 캡션 경고로 표기(폴백 동작 유지).
- near_miss_view.py:270 부근: 사용자/부서 메타 조회 실패 시 '일부 표시 정보를 불러오지 못했습니다' 인라인 경고(코드값 폴백 유지).
- near_miss_improvement.py:604 부근: 사용자 목록 조회 실패 시 빈 옵션 대신 오류 배너 + 배정 필드 비활성(잠금).

## U7. 대시보드 KPI 제목 직후 이동 (Codex M8 축소판)
dashboard M/A 경로: 지표 스트립을 제목·설명 바로 아래로, 날짜 내비는 그 아래로 이동. USER 셸 통합은 이번 범위 밖.

## U8. 내 아차사고 '제출 취소' 비활성 버튼 제거 (Codex L9 — 제거가 정답)
near_miss_my.py:514 부근 자리표시자 버튼 삭제(주석으로 향후 계약 필요성만 남김).

## 검증
항목별 focused 테스트(+깨지는 계약 의도 보존 갱신) + compileall + diff --check. sample 새 포트 Playwright: U1 사이드바 여백·U2 제출 위치(1366+900)·U3 진입 즉시 데이터 로드·U4 셀렉트 폭·U7 KPI 위치 캡처 → .orca/artifacts/claude-design-adopt/round3/. 서버 종료. 커밋은 논리 단위 2~4건.
