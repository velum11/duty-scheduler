# CLAUDE.md

Claude Code가 이 저장소에서 작업할 때 반드시 따르는 프로젝트 가이드입니다.

## 1. 프로젝트 정체성

앱 이름은 항상 **생산 근무표 관리**로 표시합니다.

회사 생산 현장의 근무표를 등록·조회·수정·관리하는 사내 업무 시스템입니다.

- ERP·그룹웨어 수준의 안정적이고 일관된 화면을 지향합니다.
- 일반 웹사이트, Streamlit 데모, AI 샘플처럼 보이면 안 됩니다.
- SAP 로고·명칭·고유 아이콘·저작물은 복제하지 않습니다.
- 생산 현장 용어를 사용하고 병원·간호 용어를 사용하지 않습니다.
- `Phase`, `MVP`, `Demo`, `데모`, `준비 중`, `구현 예정`, `샘플 앱`, `테스트 화면`, `AI`를 사용자 화면에 노출하지 않습니다.

---

## 2. 지시와 문서 우선순위

작업 전 필요한 범위에서 다음을 확인합니다.

1. `CLAUDE.md`
2. `DESIGN.md`
3. `docs/requirements.md`
4. `docs/screens.md`
5. `docs/database.md`

충돌 시 다음 순서를 따릅니다.

```text
사용자의 가장 최근 지시
> CLAUDE.md
> DESIGN.md
> docs/*.md
> 기존 코드
```

오래된 문서를 그대로 구현하지 말고 현재 코드·DB 상태와 최근 지시를 우선 확인합니다.

---

## 3. 현재 핵심 상태

- 기준정보 CRUD, 근무표 등록/수정 1차, 전체 조회, 내 근무표 구현 완료
- Supabase migration·seed·CRUD 및 ADMIN/MANAGER/USER 화면 검증 완료
- 사용자 관리 AG Grid 편집기 안정화 완료
  - Excel 다중 행·열 붙여넣기
  - 마지막 빈 행 자동 확장
  - 조명 표시 ↔ `team_code`
  - 관리자/조장/조원 표시 ↔ 내부 권한값
  - 표와 저장 버튼 겹침 방지
- 사용자 관리의 실제 두벌식 IME 입력은 사용자 수동 확인 후 커밋 예정
  - 브라우저 자동화 검증 15건 중 14건 PASS (유일한 FAIL은 Playwright 타이밍 아티팩트로 판단, DB 저장값·새로고침 화면은 정상)
- 근무표 구조 개편 1단계 작성 완료
  - `schedule_assignments`
  - `shift_groups`
  - migration 002
  - Repository/DataFrame 계약 및 테스트
- migration 002는 아직 Supabase에 적용하지 않음

확인 필요·미완료 항목:

- 사용자 관리 변경(`views/master_users.py`, `views/workspace.py`)의 커밋 여부 — `git status`로 확인
- 저장 성공 메시지의 "수정 N건"이 실제 변경 건수가 아니라 화면 행 수 기준 (upsert는 diff 기반이라 데이터는 안전 — 별도 작은 작업으로 수정)
- 조 관리 화면에 사용자 관리 수정 전과 유사한 rerun 경합 패턴 잔존 (`mt_work=edited` 재전달 + save_bar) — 조 관리 작업 시 사용자 관리의 공용 콜백 패턴 참조

다음 단계 후보 순서 (한 번에 하나씩):

1. 조직 관리 디자인 대안·리뷰·사용자 승인
2. 승인된 조직 관리 디자인 구현
3. 근무형태 색상 선택기
4. 근무표 등록 화면 개편 (빈 행 월간 그리드, §9)

사용자 승인 없이 migration, 스키마 변경, 데이터 초기화를 실행하지 않습니다.

---

## 4. 기술 스택

| 구분 | 기술 |
|---|---|
| 프레임워크 | Streamlit |
| 언어 | Python 3.11+ |
| DB | Supabase PostgreSQL |
| DB 클라이언트 | supabase-py |
| 데이터 처리 | pandas |
| 표 편집 | streamlit-aggrid + 공용 그리드 헬퍼 |
| 로그인 유지 | 브라우저 쿠키 기반 세션 |
| 로컬 폴백 | `data/sample` CSV |

원칙:

- Supabase URL/KEY 하드코딩 금지
- `.streamlit/secrets.toml` 또는 `.env` 사용
- Supabase 미설정 시 로컬 샘플 폴백 유지
- MCP는 사용자가 요청한 경우만 검토
- 새 라이브러리는 기존 구현으로 해결할 수 없을 때만 추가
- Community/Enterprise 기능과 라이선스를 확인

---

## 5. 도메인과 데이터 규칙

### 사용자와 소속

사용자 기준정보에는 사번, 성명, 현재 부서, 현재 팀, 재직 여부, 권한을 저장합니다.

- 화면에는 부서명·조명/팀명을 표시합니다.
- 관계 연결에는 부서코드·팀코드를 사용합니다.
- 명칭을 관계키로 사용하지 않습니다.
- 부서명·팀명 변경으로 기존 연결이 끊어지면 안 됩니다.

### 월별 조 편성

- 사용자 기준정보의 팀은 현재 기본 소속입니다.
- `shift_groups`는 직원별 월 1개의 편성 조입니다.
- 근무표 등록 시 사용자 부서·팀을 기본값으로 사용합니다.
- 등록/수정 화면에서 해당 월의 부서·팀을 수정할 수 있습니다.
- 저장된 부서·팀은 편성 당시 스냅샷입니다.
- 사용자 기준정보 변경으로 과거 근무표를 자동 변경하지 않습니다.

### 일별 근무

- 한 직원은 하루 근무코드 1개만 가집니다.
- 직원 + 근무일자 중복을 허용하지 않습니다.
- 화면은 월간 가로형, DB는 일자별 세로형입니다.
- 빈 셀 때문에 기존 근무를 자동 삭제하지 않습니다.
- 삭제는 명시적 동작과 확인이 필요합니다.

### 근무형태

기본 범주는 `주간`, `야간`, `OFF`, `연차`, `경조사`, `훈련`입니다.

`주-6`, `6주`, `주2`, `야-4`, `특주` 등 세부 코드는 `work_types`에서 관리합니다.  
코드 의미를 시스템이 임의 해석하지 않고 기준정보의 코드·명칭·설명·시간·색상을 사용합니다.

---

## 6. 권한과 로그인

### 권한값

| 내부값 | 화면 표시 |
|---|---|
| `ADMIN` | 관리자 |
| `MANAGER` | 조장 |
| `USER` | 조원 |

- DB와 권한 분기에는 내부값을 유지합니다.
- 조회 시 한글 표시로 변환하고 저장 시 내부값으로 변환합니다.
- 한글 표시값을 DB 권한 컬럼에 직접 저장하지 않습니다.

### 권한별 기능

| 권한 | 기능 |
|---|---|
| ADMIN | 전체 기준정보와 근무표 관리 |
| MANAGER | 담당 부서 범위 조회·관리, 본인 근무표 |
| USER | 대시보드, 전체 근무표 조회·CSV, 본인 근무표 |

USER는 `dashboard`, `schedule_view`, `my_schedule`만 허용하며 편집·저장은 금지합니다.

### 로그인 요구사항

- 현재는 사번 단독 로그인입니다.
- 사번은 앞뒤 공백을 제거하고 대소문자를 구분하지 않습니다. (영문 사번 `ADMIN`·`admin`·`Admin` 은 동일 사용자)
- 정규화(trim + `casefold`)는 조회 비교에만 적용하고, DB 의 원래 `emp_no` 값은 변경하지 않습니다.
- 세션·토큰에는 입력값이 아니라 DB 의 정규 `emp_no` 를 저장합니다.
- Enter와 로그인 버튼은 동일한 로그인 경로(단일 폼 제출)를 사용합니다.
- 로그인 중 중복 제출·중복 조회가 발생하지 않게 합니다.
- 쿠키 기반 로그인 유지를 보존합니다.
- 운영 전환 시 PIN·비밀번호 등 인증을 재검토합니다.

---

## 7. 전체 UI 원칙

### App Shell

ADMIN/MANAGER:

```text
단일 다크 사이드바(브랜드 헤더 + 그룹 메뉴 + 하단 사용자 카드) + 본문 브레드크럼 + 메인 콘텐츠
```

- Streamlit 기본 라디오 메뉴 사용 금지
- 메뉴는 데이터 기반 (`modules/nav.py` MENU_GROUPS)
- 하위 1개 그룹은 단독 항목, 여러 개면 그룹 라벨 + 도트 하위 항목
- 메뉴 텍스트는 좌측 정렬, 높이·여백·글자·활성 상태 통일
- 활성 메뉴는 배경(`#2B2B2E`)·골드 도트·흰 글자로 구분
- 사이드바 숨김/열기는 ▤ 버튼과 `st.session_state.sb_hidden` 으로 제어
- 사용자명·사번·로그아웃은 사이드바 하단 사용자 카드에 표시 (본문 상단에는 브레드크럼만)
- 디자인 토큰·수치는 DESIGN.md §3 기준
- 관련 없는 전체 App Shell 재설계 금지

USER:

- 전용 상단 헤더 사용
- 사이드바 없음
- 앱명, 사용자명, 부서, 팀/조, 로그아웃 표시
- 모바일 가로 스크롤 금지

### 관리자 업무 화면

기본 순서:

```text
화면 제목 → 설명 → 조회/입력 조건 → 표/그리드 → 하단 작업 버튼
```

- [조회]는 조회 조건 우측
- 보조 버튼은 저장 버튼 왼쪽
- [저장]은 우측 끝 Primary
- 같은 의미의 버튼은 크기·높이·문구·스타일 통일
- 표와 버튼이 겹치거나 가려지면 안 됩니다.

### 디자인 동결 (Design Freeze)

여러 도구(Claude·Codex)가 병행 작업하면서 화면 틀이 반복적으로 바뀌는 것을 금지합니다.

- 공용 CSS·디자인 토큰·App Shell(`modules/ui.py`)과 공용 그리드 헬퍼(`views/workspace.py`)의 스타일은 **사용자가 "디자인 변경"을 명시한 작업에서만** 수정합니다.
- 기능 작업 중 색·여백·폰트·버튼 스타일·레이아웃 변경 금지. 기능 수정 후 화면 틀이 이전과 달라 보이면 **그 자체가 결함**입니다.
- 화면을 재생성하지 말고 기존 코드를 최소 diff로 수정합니다.
- 새 화면·새 요소는 기존 공용 컴포넌트와 스타일을 재사용하고, 화면별 새 스타일을 만들지 않습니다.
- 디자인 변경 작업일 때도 변경 범위를 사전에 명시하고, 그 외 화면의 틀은 유지합니다.

---

## 8. 기준정보 화면 공통 표준

기준정보 화면은 하나의 업무 모듈처럼 동일한 형태를 유지합니다.

현재 참조 구현:

```text
views/master_users.py
views/workspace.py
```

사용자 관리에서 검증된 제품 문법을 조직 관리와 근무형태 관리에 재사용합니다. 조직 관리의 구체적인 화면 표현은 디자인 대안·리뷰·사용자 승인으로 확정합니다.

### 공통 구성

```text
화면 제목 → 간단한 설명 → 편집 표 → 하단 작업 영역 → 저장 → 결과 메시지
```

다음 요소를 모든 기준정보 화면에서 통일합니다.

- 제목과 설명의 위치·크기
- 표 폭·높이·여백
- 헤더·행 높이와 셀 정렬
- 필수값 표시
- 버튼 크기·높이·간격·순서
- 저장 버튼과 메시지 위치
- 신규 입력 방식

화면별로 서로 다른 카드, 입력 폼, 버튼, 표 스타일을 임의로 만들지 않습니다.

### 공통 편집 동작

- 목록에서 기존 셀 직접 수정
- 마지막 빈 행에서 신규 입력
- 마지막 행 입력 시 빈 행 자동 확장
- Excel 여러 행·열 복사/붙여넣기
- 탭은 열, 줄바꿈은 행으로 분배
- 한글 IME 조합 보존
- 저장 전 편집 상태 유지
- [저장] 버튼으로 일괄 반영
- 저장 후 Repository 재조회
- 자동 저장 금지

### AG Grid 공통 규칙

- `views/workspace.py`의 공용 paste handler와 DataFrame 정규화 로직을 우선 재사용합니다.
- Enterprise 전용 clipboard 기능에 의존하지 않습니다.
- 페이지별 붙여넣기 코드 복제를 금지합니다.
- 행 수를 고려한 제한 높이를 사용합니다.
- 표가 저장 버튼 영역을 침범하면 안 됩니다.
- `z-index`로 겹침을 덮어 해결하지 않습니다.
- 조회 원본을 rerun마다 편집 결과로 다시 전달해 경합을 만들지 않습니다.
- 저장 버튼은 편집기 blur 이후 최신 데이터가 반영되는 공용 콜백 패턴을 사용합니다.

### 코드와 표시명

```text
team_code → 조명/팀명
ADMIN → 관리자
MANAGER → 조장
USER → 조원
```

- 화면은 사람이 읽는 명칭을 사용합니다.
- DB 관계와 권한은 내부 코드를 유지합니다.
- 부서 + 조명/팀명으로 내부 코드를 정확히 매핑합니다.
- 중복 명칭으로 식별할 수 없으면 저장을 차단합니다.
- 존재하지 않는 부서·조/팀은 저장하지 않습니다.

### 화면별 규칙

**사용자 관리**

- 현재 기준 화면
- 목록 직접 편집, Excel 붙여넣기, 마지막 빈 행 자동 확장
- 조코드 대신 조명 표시
- 관리자·조장·조원 표시
- 저장 시 내부 코드로 변환
- 표 높이는 하단 버튼을 침범하지 않게 제한

**조직 관리 (통합 화면 — 디자인 승인 필요)**

- 사이드바에는 **조직 관리** 메뉴 하나만 표시하고 `views/master_org.py`를 직접 엽니다. 기존 부서·조 route의 호환 위임은 유지할 수 있지만 별도 메뉴로 노출하지 않습니다.
- 데이터 관계는 **그룹 → 부서 → 운영단위** 계층을 유지합니다. 상위 목록의 선택에 따라 바로 아래 목록의 데이터가 필터링됩니다.
- 화면 배치와 계층 표현 방식은 현재 화면 분석, 디자인 대안, 별도 리뷰와 사용자 승인을 거쳐 확정합니다. 데이터 관계만으로 특정 UI 형태를 미리 고정하지 않습니다.
- 검색창은 두지 않습니다.
- 세 목록의 공통 컬럼은 `선택 / 코드 / 이름 / 표시순서 / 사용여부`입니다. 필요한 내부 ID/FK는 저장 계약으로 유지하되 사용자용 공통 컬럼을 흐리지 않습니다.
- 세 섹션의 추가·삭제·저장 버튼 형식, 표 헤더, 행 높이, 선택·활성 상태를 통일합니다.
- 기존 사용자 관리·근무형태 관리와 같은 제품 안의 화면으로 보이도록 색상, 타이포그래피, 여백과 공용 컴포넌트를 재사용합니다.
- 그룹은 별도 테이블이 아니라 `departments`의 `department_group`/`group_sort_order`로 유지하며, 신규 부서는 선택 그룹, 신규 운영단위는 선택 부서에 FK로 귀속합니다. 이 데이터 계약은 화면을 트리로 표현해야 한다는 뜻이 아닙니다.
- 디자인 변경은 코드보다 먼저 현재 화면 분석 → A/B 대안 → 별도 디자인 리뷰 → 사용자 승인 순서로 진행합니다. 승인 전에는 `views/master_org.py`를 수정하지 않습니다.

**근무형태 관리**

- HEX 코드 직접 입력 대신 색상 선택기 또는 색상표 사용
- HEX는 필요할 때 보조 정보로 표시
- 기존 DB 색상 저장 형식 유지
- 다른 기준정보와 동일한 레이아웃 유지

---

## 9. 주요 화면 원칙

### 근무표 등록/수정

#### 입력 화면의 기본 형태

근무표 등록 화면은 **전체 사용자를 한 행씩 미리 표시하는 방식으로 만들지 않습니다.**

현재처럼 모든 사용자가 자동으로 나열되어 사용자가 원본 Excel과 행을 일일이 맞추게 하면 안 됩니다.  
선택한 월의 날짜 열만 생성하고, 사용자가 필요한 직원 범위를 붙여넣을 수 있는 **빈 월간 입력 행**을 제공합니다.

```text
사번 | 성명 | 부서 | 조/팀 | 1일 | 2일 | 3일 | ... | 말일
빈 행
빈 행
빈 행
...
```

- 선택한 월의 1일부터 말일까지 날짜 열을 생성합니다.
- 최초에는 일정 수의 빈 행을 제공합니다.
- 붙여넣는 데이터가 현재 행 수를 초과하면 빈 행을 자동 확장합니다.
- 사용자는 직원 전체가 아니라 부서·조·기간 등 필요한 범위만 선택하여 붙여넣을 수 있습니다.
- 기존 전체 사용자 자동 나열 방식으로 되돌리지 않습니다.

#### 사번 입력과 기본 소속

근무표 입력의 기준키는 사번입니다.

- 사번을 직접 입력하거나 Excel에서 붙여넣으면 사용자 기준정보를 정확히 조회합니다.
- 존재하는 사번이면 성명을 자동 표시합니다.
- 사용자 기준정보의 현재 부서와 현재 조/팀을 해당 행의 기본값으로 자동 채웁니다.
- 부서와 조/팀은 **근무표 등록·수정 화면에서만 편성값으로 수정할 수 있습니다.**
- 등록 화면에서 부서·조/팀을 수정해도 사용자 기준정보의 현재 소속은 변경하지 않습니다.
- 저장되는 부서·조/팀은 해당 월 편성 당시의 스냅샷입니다.
- 과거 근무표는 이후 사용자 기준정보 변경으로 자동 변경되지 않습니다.
- 존재하지 않는 사번은 저장을 차단하고 해당 행을 명확히 표시합니다.
- 같은 월에 동일 사번이 여러 행으로 중복되면 저장을 차단합니다.

화면에는 사람이 읽는 부서명과 조명/팀명을 표시하고, 저장에는 내부 부서코드와 `team_code`를 사용합니다.

#### 범위 붙여넣기

사용자가 Excel에서 선택한 범위를 그대로 붙여넣을 수 있어야 합니다.

- 사번 열만 여러 행 붙여넣기
- 사번·부서·조/팀을 포함한 여러 열 붙여넣기
- 특정 날짜 구간의 근무코드만 붙여넣기
- 여러 직원 × 여러 날짜의 직사각형 범위 붙여넣기
- 기존 행의 일부 날짜 범위만 수정하여 붙여넣기

탭은 열, 줄바꿈은 행으로 분배합니다.  
붙여넣은 사번별로 성명과 기본 부서·조/팀을 조회해 채웁니다.

빈 셀은 기존 근무를 자동 삭제한다는 뜻으로 해석하지 않습니다.  
삭제는 별도의 명시적 동작과 확인을 거쳐야 합니다.

#### 연·월 선택

근무표 등록·수정의 대상 월 선택은 가능한 한 `내 근무표`에서 사용하는 **아이폰식 연·월 wheel picker**를 재사용합니다.

- 연도와 월을 2열 wheel 형태로 선택
- 스크롤 스냅 동작 유지
- 기본값은 `today` 기준 현재 연·월
- 기존 좌우 월 이동 기능이 있으면 같은 동작과 디자인 사용
- 별도의 서로 다른 연도·월 selectbox UI를 새로 만들지 않음
- 동일 기능을 페이지마다 복제하지 말고 기존 picker 또는 공용 컴포넌트를 재사용

근무표는 월 단위 편성이므로 별도의 일자 선택기를 새로 추가하지 않습니다.  
선택한 연·월에 따라 날짜 열과 말일을 계산합니다.

#### 저장

- 기준정보에 등록된 근무코드만 허용합니다.
- 한 직원은 하루 근무코드 1개만 저장합니다.
- [저장] 버튼을 눌렀을 때만 일괄 반영합니다.
- 신규 행과 변경 행만 저장합니다.
- 저장 후 Repository에서 다시 조회하여 실제 반영 결과를 확인합니다.

### 전체 근무표 조회

- 연도·월·부서·팀/조·사번/성명 조회
- 사번·성명·부서·팀/조·1일부터 말일까지 표시
- CSV 다운로드
- USER는 조회·다운로드만 허용

### 내 근무표

- 로그인 사용자 본인만 조회
- 직원 선택 없음
- 연·월 wheel picker와 좌우 이동
- 주간·야간·OFF·휴가 요약
- 모바일 7열 달력
- 기본 연월은 `today` 기준

---

## 10. 저장과 검증

저장 전 확인:

- 사번·부서·팀/조·근무코드 존재
- 필수값과 자연키 중복
- 직원 + 근무일자 중복
- 화면 표시값을 내부 코드로 변환 가능

저장 원칙:

- 신규 행과 변경 행 구분
- 가능한 경우 변경 행만 upsert
- 표시명을 코드 컬럼에 저장하지 않음
- 일부 실패를 전체 성공으로 표시하지 않음
- 저장 후 fresh client 또는 Repository로 재조회
- 테스트 데이터는 명확한 접두어 사용 후 정리
- 실사용 데이터 임의 삭제·비활성화 금지

---

## 11. 작업 방식

큰 작업을 한 번에 진행하지 않습니다.

**프롬프트 하나에는 사용자가 화면에서 확인할 수 있는 결과 하나만 포함합니다.**

좋은 작업 단위:

```text
표와 저장 버튼 겹침 해결
조코드 대신 조명 표시
권한 표시명 변경
로그인 Enter 제출
사이드바 좌측 정렬
조 관리 편집기 통일
근무형태 색상 선택기
```

나쁜 작업 단위:

```text
기준정보 전체 재설계
로그인·사이드바·기준정보·근무표 동시 수정
UI 수정과 migration 동시 실행
```

기본 절차:

```text
1. git status 확인
2. 수정 대상과 영향 범위 확인
3. 최소 파일만 수정
4. 관련 테스트만 실행
5. 결과 보고
6. 사용자 확인 후 다음 단계
```

- 관련 없는 문제는 수정하지 않고 보고합니다.
- 불명확하거나 부적합한 요구사항은 구현 전에 질문합니다.
- 관련 없는 리팩터링과 UI 재설계를 하지 않습니다.
- 다음 단계를 선행 구현하지 않습니다.
- 전체 브라우저 검증과 전체 테스트를 매번 반복하지 않습니다.
- 에이전트 팀·서브에이전트는 사용자가 요청한 경우만 사용합니다.
- 병렬 작업 시 같은 파일을 동시에 수정하지 않습니다.

### 도구 역할 분담

Claude(터미널 Claude Code·데스크탑 Cowork)와 Codex가 병행 작업합니다. Codex용 규칙은 `AGENTS.md`에 있습니다.

- **Claude: UI 전담** — 화면 레이아웃, CSS, App Shell, AG Grid/편집기 UX, DESIGN.md 관련 작업
- **Codex: 로직·데이터 전담** — 비즈니스 로직, Repository/db 계층, validators, 테스트, 스크립트, migration 초안

`CLAUDE.md`의 규칙이 바뀌면 `AGENTS.md`의 요약도 함께 갱신해 두 문서가 어긋나지 않게 합니다.

---

## 12. 중단·재개, 테스트, Git

재개 전 확인:

```text
현재 브랜치
git status --short --branch
staged / unstaged 파일
최근 커밋
앱 실행 가능 여부
완료·미완료 항목
migration·DB 적용 여부
```

사용자 지시 없이 `git reset`, `git restore`, `git clean`을 실행하지 않습니다.  
다른 정상 변경이 섞여 있으면 파일 전체를 원복하지 말고 변경 단위로 분리합니다.

기본 검사:

```powershell
python -m compileall .
python -m streamlit run app.py
```

테스트 순서:

1. 수정 범위의 단위·계약 테스트
2. 관련 Repository·화면 회귀 테스트
3. 필요한 경우 Streamlit AppTest
4. IME·붙여넣기·레이아웃은 실제 브라우저
5. 안정화 시 전체 회귀 테스트

사용자 요청 없이 commit, push, migration 적용, Supabase 초기화, 원격 병합을 하지 않습니다.

### 작업 로그(WORKLOG)

터미널 Claude Code와 데스크탑 Cowork가 번갈아 작업하므로 맥락 공유용 로그를 유지합니다.

- 대상 파일: `docs/WORKLOG.md`
- 재개 시: 위 재개 전 확인 항목과 함께 `docs/WORKLOG.md` 최신 항목을 읽어 직전 작업 맥락을 파악합니다.
- 작업(사용자가 화면에서 확인 가능한 결과 1개)을 마칠 때마다 `docs/WORKLOG.md`의 **로그** 섹션 **맨 위**에 한 항목을 추가합니다. 최신 항목이 맨 위(역순)입니다.
- 항목 형식(간결하게):

```text
## YYYY-MM-DD HH:MM · [터미널|데스크탑] · 한 줄 제목
- 요청: 사용자 요청 요약
- 변경: 무엇을 어떻게 바꿨는지
- 파일: 수정/생성한 파일 목록
- 비고: 미완료·후속·주의사항 (없으면 생략)
```

- 시각은 Asia/Seoul(KST) 기준, 도구는 `[터미널]` 또는 `[데스크탑]`으로 표기합니다.
- 로그 기록 자체는 커밋 대상이 아니며, 커밋은 사용자 요청이 있을 때만 합니다.

---

## 13. 결과 보고

기본 형식:

```text
1. 무엇을 변경했는지
2. 핵심 설계 결정
3. 테스트 통과 여부
4. 남은 위험 또는 결정사항
5. git status
```

필요할 때만 브라우저 검증, migration·Supabase 변경, 커밋·push 결과를 추가합니다.  
상세 diff, 전체 명령 로그, 전체 테스트 목록은 요청받지 않으면 출력하지 않습니다.

---

## 14. 금지 사항 요약

- 승인 없는 migration·Supabase 스키마 변경
- 실사용 데이터 임의 삭제
- 관련 없는 대규모 UI 재설계
- 화면마다 제각각인 기준정보 편집기
- 내부 코드와 화면 표시명 혼용
- 빈 셀로 기존 근무 자동 삭제
- 자동 저장
- Enterprise 기능을 Community 기능처럼 사용
- 여러 독립 작업을 한 프롬프트에 결합
- 테스트·브라우저 검증의 불필요한 반복
- 승인 없는 commit·push
- AI 데모처럼 보이는 화면과 문구
