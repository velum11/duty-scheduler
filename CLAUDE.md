# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 반드시 참고해야 하는 프로젝트 가이드입니다.

---

## 1. 프로젝트 정체성

이 프로젝트는 회사 생산 현장 근무표를 관리하기 위한 **생산 근무표 관리** 웹 시스템입니다.

사용자에게 보이는 앱 이름은 항상 다음으로 통일합니다.

```text
생산 근무표 관리
```

이 시스템은 일반 웹사이트, Streamlit 데모 앱, AI 생성 샘플 화면처럼 보이면 안 됩니다.  
SAP/ERP/그룹웨어 계열의 사내 업무 시스템처럼 보여야 합니다.

단, SAP 로고, SAP 명칭, SAP 고유 아이콘, SAP 저작물을 복제하지 않습니다.  
목표는 SAP Business ByDesign과 유사한 **업무 시스템 구조와 사용감**을 구현하는 것입니다.

---

## 2. 문서 우선순위

작업 전 필요한 범위에서 아래 문서를 확인합니다.

1. `CLAUDE.md`
2. `DESIGN.md`
3. `docs/requirements.md`
4. `docs/screens.md`
5. `docs/database.md`

문서 간 충돌이 있을 경우 우선순위는 다음과 같습니다.

```text
사용자 최근 지시 > CLAUDE.md > DESIGN.md > docs/*.md > 기존 코드
```

UI 구현과 화면 품질은 반드시 `DESIGN.md`를 따릅니다.

---

## 3. UI 구현 강제 원칙

- 화면 UI는 반드시 `DESIGN.md`를 따릅니다.
- Streamlit 기본 사이드바 라디오 메뉴를 그대로 사용하지 않습니다.
- 좌측 1차 아이콘 메뉴 바 + 좌측 2차 업무 메뉴 패널 + 우측 메인 콘텐츠 영역 구조를 구현합니다.
- 메뉴는 하드코딩하지 않고 향후 업무 모듈을 추가할 수 있는 데이터 기반 구조로 만듭니다.
- 사용자 화면에는 개발 단계가 아니라 완성된 업무 화면만 보여야 합니다.
- 화면이 AI 생성 데모처럼 보이면 실패로 간주합니다.

사용자 화면에 아래 표현을 노출하지 않습니다.

```text
Phase
MVP
Demo
데모
준비 중
구현 예정
오픈 후 이용 가능
샘플 앱
테스트 화면
AI
```

아래 용어도 사용하지 않습니다.

```text
병원
간호
간호팀
nursing
hospital
```

생산 현장 기준 용어를 사용합니다.

예:

```text
PET생산부(본동)
PET생산부(원료실)
생산관리팀
A조
B조
C조
A팀
B팀
조장
사원
파트장
```

---

## 4. 기술 스택

| 구분 | 기술 |
|---|---|
| 프레임워크 | Streamlit |
| 언어 | Python 3.11+ |
| DB | Supabase PostgreSQL |
| DB 클라이언트 | supabase-py |
| 데이터 처리 | pandas |
| 로그인 유지 | 브라우저 쿠키 기반 세션 토큰 |
| 로컬 폴백 | data/sample CSV |

원칙:

- Supabase 설정이 없어도 로컬 샘플 데이터로 실행 가능해야 합니다.
- Supabase 접속 정보는 `.streamlit/secrets.toml` 또는 `.env`에서 읽습니다.
- Supabase URL/KEY를 코드에 하드코딩하지 않습니다.
- MCP는 사용하지 않습니다. 사용자가 명시적으로 요청한 경우에만 검토합니다.

---

## 5. 핵심 기능 원칙

이 앱은 자동 근무표 생성기가 아닙니다.  
근무는 사람이 직접 입력하고, 시스템은 등록/조회/수정/정합성 관리를 담당합니다.

### 5.1 사용자 권한

| 권한 | 설명 |
|---|---|
| USER | 본인 근무표만 조회 |
| MANAGER | 소속 부서/조 근무표 조회 및 수정 |
| ADMIN | 전체 기준정보 및 근무표 관리 |

### 5.2 로그인

1차 구현은 사번만 입력하는 간단 로그인입니다.

```text
사번 입력
→ users에서 사용자 확인
→ 세션 생성
→ 쿠키로 로그인 유지
```

사번 단독 로그인은 보안 한계가 있습니다.  
향후 사번+이름 또는 PIN 방식으로 보완할 수 있음을 문서와 리뷰에서 언급합니다.

### 5.3 등록 화면

모든 등록/수정 화면은 스프레드시트 형태를 기본으로 합니다.

- 수기 입력
- 엑셀 복사/붙여넣기
- 행 추가
- 행 삭제
- 조회
- 저장
- 저장 전 검증

저장은 자동 저장하지 않고, 사용자가 [저장] 버튼을 눌렀을 때만 반영합니다.

### 5.4 근무표 저장 구조

근무표는 실제 엑셀처럼 가로형으로 입력받습니다.

```text
사번 | 성명 | 부서 | 조/팀 | 1(화) | 2(수) | 3(목) | ...
```

DB에는 세로형으로 저장합니다.

```text
사번 | 근무일자 | 근무형태
```

### 5.5 저장 전 검증

근무표 저장 전 다음을 검증합니다.

- 사번이 사용자 정보에 존재하는지
- 사번이 없으면 성명으로 매칭 가능한지
- 부서가 departments에 존재하는지
- 조/팀이 teams에 존재하는지
- 근무코드가 work_types에 존재하는지
- 같은 사번 + 같은 근무일자가 중복되지 않는지

붙여넣기 데이터의 빈칸 때문에 기존 근무를 자동 삭제하지 않습니다.  
삭제는 반드시 명시적 확인 후 처리합니다.


### 5.6 근무형태 기준

기본 근무형태는 다음 정도로 단순하게 관리합니다.

```text
주간
야간
OFF
연차
경조사
훈련
```

실제 근무 편성 화면에서는 기본적으로 `주`, `야`, `OFF`, `연차`처럼 단순한 코드를 우선 표시합니다.

다만 현장 상황이나 특근수당 계산을 위해 세부 코드가 필요할 수 있습니다.

예:

```text
주-6
6주
주2
야-4
야-6
특주
특야
자녀결혼
출산휴가
```

이런 세부 코드는 기본 설계에 하드코딩하지 않고, `work_types` 기준정보에서 추가하거나 수정할 수 있게 합니다.

숫자가 포함된 근무코드는 기준 근무시간에서 앞/뒤 어느 시간에 덜 했는지 또는 더 했는지를 구분하기 위한 현장 운영 코드입니다.  
따라서 시스템은 코드를 임의 해석하지 말고, 기준정보에 등록된 코드와 설명을 기준으로 표시·검증합니다.

---

## 6. 주요 화면

초기 화면 구조는 다음과 같습니다.

```text
홈
- 대시보드

근무표 관리
- 근무표 등록/수정
- 전체 근무표 조회

기준정보
- 사용자 관리
- 부서 관리
- 조 관리
- 근무형태 관리

내 정보
- 내 근무표
```

향후 다른 업무 프로그램을 추가할 수 있도록 메뉴는 데이터 기반 구조로 설계합니다.

---

## 7. 예정 디렉터리 구조

```text
duty-scheduler/
├── app.py
├── views/
│   ├── login.py
│   ├── dashboard.py
│   ├── master_users.py
│   ├── master_departments.py
│   ├── master_teams.py
│   ├── master_work_types.py
│   ├── schedule_edit.py
│   ├── schedule_view.py
│   └── my_schedule.py
├── modules/
│   ├── auth.py
│   ├── db.py
│   ├── nav.py
│   ├── ui.py
│   ├── schedule.py
│   ├── validators.py
│   └── config.py
├── data/
│   └── sample/
├── docs/
├── .claude/
├── .streamlit/
├── DESIGN.md
├── CLAUDE.md
├── README.md
└── requirements.txt
```

---

## 8. 작업 방식

큰 작업을 한 번에 진행하지 않습니다.

권장 진행 방식:

```text
1. 현재 상태 점검
2. 수정 대상 파일과 계획 제시
3. 한두 파일만 수정
4. 실행/문법 검사
5. 결과 보고
```

사용량 한도, 오류, 터미널 중단 등으로 작업이 멈췄을 경우 바로 이어서 수정하지 않습니다.

먼저 다음을 확인합니다.

```text
1. git status 확인
2. 변경된 파일 목록 확인
3. 현재 앱 실행 가능 여부 확인
4. 이전 작업이 어디까지 반영되었는지 확인
5. 이어서 할 최소 작업 계획 제시
```

---

## 9. Git 체크포인트

안정적인 단계가 끝나면 커밋을 권장합니다.

예:

```powershell
git add .
git commit -m "Checkpoint before ERP app shell redesign"
```

중요 시점:

- 로그인/샘플 데이터 폴백 완료
- DESIGN.md/CLAUDE.md 기준 확정
- App Shell 재구성 전
- App Shell 재구성 후
- 기준정보 등록 화면 구현 후
- Supabase 연동 전후

---

## 10. 테스트 원칙

수정 후 가능한 경우 Python 문법 검사를 수행합니다.

```powershell
python -m compileall .
```

앱 실행:

```powershell
python -m streamlit run app.py
```

확인할 것:

- 로그인 가능 여부
- 로그아웃 가능 여부
- 권한별 메뉴 표시
- USER 화면과 ADMIN/MANAGER 화면 분기
- App Shell이 DESIGN.md 기준을 따르는지
- 병원/간호 용어가 없는지
- 사용자 화면에 개발자용 문구가 없는지