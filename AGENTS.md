# AGENTS.md

Codex 및 범용 코딩 에이전트가 이 저장소에서 작업할 때 사용하는 최소 지침입니다. 제품 요구사항이나 특정 화면 배치를 이 파일에서 정의하지 않습니다.

## 시작 순서

1. 사용자의 최신 지시를 읽습니다.
2. 작업 영역에 맞는 기준 문서를 읽습니다.
   - 기능·권한: `docs/requirements.md`
   - 디자인: `DESIGN.md`
   - DB·migration: `docs/database.md`
   - 실행·Supabase: `docs/supabase-setup.md`
3. 실제 호출 경로와 관련 테스트를 확인합니다.
4. 현재 `git status`와 미커밋 변경을 보존합니다.

과거 `docs/WORKLOG.md` 내용은 인계 참고자료일 뿐 현재 요구사항이 아닙니다. 문서와 코드가 다르면 추정으로 한쪽을 덮지 말고, 사용자 지시와 테스트를 근거로 범위 안에서 함께 정합화합니다.

## 작업 원칙

- 요청된 결과에 필요한 최소 범위만 수정합니다.
- 파일명만 보고 판단하지 않고 import, route, 호출부를 추적합니다.
- 기존 미커밋 변경과 관련 없는 코드를 되돌리지 않습니다.
- 화면 표현은 데이터 계층만으로 결정하지 않습니다. 명시된 디자인 요구가 없으면 현재 UI를 보존합니다.
- 디자인 변경 요청에서는 `DESIGN.md`를 기준선으로 사용하되, 사용자의 최신 요구가 우선합니다.
- 테스트를 통과시키기 위해 기능 계약을 약화하거나 assertion을 임의로 바꾸지 않습니다.
- 에러를 sample fallback이나 광범위한 예외 처리로 숨기지 않습니다.

## 안전 경계

이 절은 프로젝트 안전경계(승인 게이트·금지명령)의 **상위 단일 정본(SoT)**입니다. migration 적용의 세부 안전 절차(적용 순서·guarded DDL·부분적용 복구 등 실행 메커니즘)는 `docs/database.md` §5가 소유하되, 그 §5도 "승인 없이 실행 금지" 같은 **승인 게이트 자체는 이 절을 참조**합니다. `CLAUDE.md`, `.orca/PLAYBOOK.md`, `docs/database.md` 등 다른 문서는 이 절의 금지 규칙을 **참조만 하며**, 요약·재정의·복제하지 않습니다. 다른 문서에는 "왜 적용되는지"의 프로젝트 맥락 설명만 둘 수 있습니다.

**로컬 산출물 정책(2026-07-29 개정 — 종전 DLP 생성 금지 해제)**: 로컬 문서·이미지·스크린샷·HTML 등 작업 산출물 생성을 **허용**합니다. 규율:

- 일회성·작업 산출물은 `.orca/artifacts/<작업명>/` 또는 OS 임시 디렉터리에 두고, 소스 트리와 리포 루트를 오염시키지 않습니다. 작업 종료 시 불필요 산출물은 정리합니다.
- 산출물의 commit·push는 아래 승인 게이트를 그대로 따릅니다(산출물 생성 자체는 승인 불요).
- **service role key·개인정보·세션 파일·실데이터 덤프는 어떤 산출물에도 포함 금지** — 이 부분은 종전과 동일하게 불변입니다(아래 금지 목록 참조).
- 산출물은 참고 자료일 뿐이며 규범 권위는 정본 문서(`requirements.md`·`DESIGN.md`·`database.md`·이 문서)에만 있습니다.

사용자의 명시적 승인 없이 다음을 하지 않습니다.

- migration 실행 또는 운영·실DB 쓰기
- seed, 대량 수정, 삭제, 백필
- service role key, 개인정보, 세션 파일(`.local_sessions.json` 포함), 로컬 DB 백업의 출력·커밋
- commit, push, reset, restore, clean, revert — **commit이 생성되는 `git merge`(merge commit 포함)도 여기에 포함됩니다**
- 새 worktree 생성 — 실제 checkout 충돌, 독립 통합 계획, 데이터 격리 필요성은 **생성의 필요성 판단 근거일 뿐 사용자 승인을 대체하지 않습니다**

화면 목업·QA 작업 중 실제 사용자 입력 데이터와 원격(Supabase) 데이터를 변경하지 않습니다. Supabase 원격 쓰기 테스트는 전용 테스트 프로젝트와 `DUTY_SUPABASE_TEST_PROJECT=true`, 명령의 `--confirm-test-project`가 모두 있어야 합니다.

## 검증과 보고

- 변경 범위에 맞는 focused test를 먼저 실행합니다.
- Python 변경은 관련 테스트, `compileall`, `git diff --check`를 기본으로 합니다.
- UI 변경은 가능한 경우 대상 viewport에서 실제 화면을 한 번 확인합니다.
- DB 문서는 live schema를 확인하지 않았다면 적용 여부를 단정하지 않습니다.
- 완료 보고에는 공통 핵심 5개 항목 — 변경 결과, 수정 파일, 실행한 검증, 남은 위험, `git status` — 를 포함합니다. 이 공통 핵심은 `CLAUDE.md`의 완료 보고와 정합됩니다.

오래 걸리거나 가설이 반복해서 틀리는 문제는 같은 시도를 계속하지 말고 현재 근거와 실패 지점을 정리해 독립 진단으로 전환합니다. ORCA 역할과 worker 사용 규칙은 `C:\Users\velum\.orca\lee-mode\PLAYBOOK.md` 및 프로젝트 `.orca/PLAYBOOK.md`를 따릅니다.
