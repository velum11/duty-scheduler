# AGENTS.md — Codex 작업 가이드

이 저장소에서 Codex(및 CLAUDE.md를 읽지 않는 다른 코딩 에이전트)가 반드시 따르는 규칙입니다.

## 0. 규칙 원본

- 이 프로젝트의 **규칙 원본은 `CLAUDE.md`** 입니다. 작업 전 `CLAUDE.md` 전체를 읽고 그대로 따릅니다.
- 이 파일(AGENTS.md)은 요약과 Codex 전용 규칙만 담습니다. 충돌 시 우선순위:

```text
사용자의 가장 최근 지시 > CLAUDE.md > AGENTS.md > DESIGN.md > docs/*.md > 기존 코드
```

- 작업 시작 전 `docs/WORKLOG.md`의 최신 항목을 읽어 직전 작업 맥락을 파악합니다.

## 1. 도구 역할 분담

이 프로젝트는 Claude(터미널 Claude Code·데스크탑 Cowork)와 Codex가 병행 작업합니다.
화면 틀이 도구마다 바뀌는 문제를 막기 위해 역할을 고정합니다.

- **Claude: UI 전담** — 화면 레이아웃, CSS, App Shell, AG Grid/편집기 UX, DESIGN.md 관련 작업
- **Codex: 로직·데이터 전담** — 비즈니스 로직, Repository/db 계층, validators, 테스트, 스크립트, migration 초안

Codex는 사용자가 **명시적으로 요청하지 않는 한** 다음을 수정하지 않습니다.

```text
modules/ui.py                    (App Shell·공용 CSS·디자인 토큰)
views/workspace.py               (공용 AG Grid 헬퍼·paste handler)
views/*.py 의 레이아웃/스타일     (로직 수정 시에도 화면 구조·스타일 코드는 보존)
DESIGN.md
```

views 파일의 로직(검증 호출, 데이터 변환 등)을 고쳐야 할 때는 해당 부분만 최소 수정하고,
레이아웃·컬럼 구성·스타일·버튼 배치는 그대로 둡니다.

## 2. 디자인 동결 (Design Freeze)

- 화면의 색·여백·폰트·버튼 스타일·레이아웃은 **사용자가 "디자인 변경"을 명시한 작업에서만** 수정합니다.
- 기능 작업 후 화면 틀이 이전과 달라 보이면 **그 자체가 결함**입니다.
- 기존 화면을 재생성하지 말고 최소 diff로 수정합니다.
- 기준 화면: 기준정보는 `views/master_users.py`, 모바일은 내 근무표. 새 요소는 기존 공용 컴포넌트를 재사용하고 새 스타일을 만들지 않습니다.

조직 관리의 승인 기준은 `CLAUDE.md`의 **조직 관리 (통합 화면 — 디자인 승인 필요)** 절입니다. 데이터 관계와 화면 표현을 분리해 검토하며, 특정 UI 형태는 디자인 대안·별도 리뷰·사용자 승인으로 확정합니다. 승인 전 `views/master_org.py`를 수정하지 않습니다.

## 3. 작업 단위

- 프롬프트 하나 = 사용자가 화면/결과로 확인 가능한 결과 하나 (10~15분 수준)
- 여러 독립 작업을 한 번에 묶지 않습니다.
- 관련 없는 문제는 수정하지 않고 보고만 합니다.
- 관련 없는 리팩터링·UI 재설계·선행 구현 금지.

## 4. 작업 로그 (WORKLOG)

작업을 마칠 때마다 `docs/WORKLOG.md`의 **로그** 섹션 **맨 위**에 한 항목을 추가합니다.

```text
## YYYY-MM-DD HH:MM · [코덱스] · 한 줄 제목
- 요청: 사용자 요청 요약
- 변경: 무엇을 어떻게 바꿨는지
- 파일: 수정/생성한 파일 목록
- 비고: 미완료·후속·주의사항 (없으면 생략)
```

시각은 Asia/Seoul(KST) 기준. Codex 작업은 `[코덱스]`로 표기합니다.

## 5. 안전 원칙 (사용자 지시 없이 금지)

```text
git reset / restore / clean
commit / push / 원격 병합
migration 실행 (특히 migration 002 — 아직 Supabase 미적용, 승인 전 실행 금지)
Supabase 스키마 변경·초기화
실사용 데이터 삭제·비활성화
```

작업 시작 전 `git status --short --branch`와 최근 커밋을 확인하고, 다른 변경이 섞여 있으면
파일 전체를 원복하지 말고 변경 단위로 분리합니다.

## 6. 핵심 도메인 규칙 요약 (상세는 CLAUDE.md)

- 앱명은 항상 **생산 근무표 관리**. 병원·간호 용어, `Phase/MVP/데모/AI` 등 개발 문구를 화면에 노출 금지.
- 권한 내부값 `ADMIN/MANAGER/USER` 유지, 화면 표시만 관리자/조장/조원. 표시값을 DB에 저장 금지.
- USER는 dashboard·schedule_view·my_schedule 조회만 (전체 근무표 조회는 의도된 허용, 편집·저장 절대 금지).
- 사번 조회는 trim + 대소문자 무시(`casefold` 비교). 정규화는 비교에만, DB emp_no 원본·세션은 정규 emp_no 유지. 저장값에 정규화 미적용.
- 한 직원은 하루 근무코드 1개. 화면은 가로형, DB는 세로형. 빈 셀로 기존 근무 자동 삭제 금지. 자동 저장 금지.
- 관계키는 코드(dept_code/team_code), 화면은 명칭. 근무표의 부서·팀은 편성 당시 스냅샷.
- 저장 전 검증은 `modules/validators.py` 재사용.

## 7. 결과 보고 형식

```text
1. 무엇을 변경했는지
2. 핵심 설계 결정
3. 테스트 통과 여부
4. 남은 위험 또는 결정사항
5. git status
```

상세 diff·전체 로그·전체 테스트 목록은 요청 없으면 출력하지 않습니다.
