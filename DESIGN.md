# DESIGN.md

현재 애플리케이션에서 재사용되는 디자인 시스템과 공용 UI 기준입니다. 특정 업무 화면의 레이아웃을 미리 확정하거나 모든 UI 작업에 별도 설계·승인 절차를 강제하지 않습니다.

사용자의 최신 디자인 요구가 이 문서보다 우선합니다. 작업 중 승인된 새 패턴이 생기면 공용화가 필요한 범위만 이 문서와 구현에 반영합니다.

## 1. 현재 화면 구조

### ADMIN / MANAGER

- 단일 다크 사이드바와 크림색 본문
- 사이드바 상단: 로고, `교대 근무표`, 접기 버튼
- 사이드바 본문: 권한별 메뉴와 현재 활성 상태
- 사이드바 하단: 사용자 정보와 로그아웃 아이콘
- 본문 상단: 사이드바 열기 버튼과 브레드크럼

### USER

- 상단 사용자 정보와 로그아웃
- 반응형 3개 메뉴: 대시보드, 월간 근무표, 내 근무표
- 개인 근무표는 월 이동·연월 선택과 7열 달력 사용

화면별 메뉴 내용은 `modules/nav.py`와 실제 render 경로를 기준으로 하며 이 문서에 중복 정의하지 않습니다.

## 2. 공용 토큰

ADMIN/MANAGER App Shell의 실제 토큰은 `modules/ui.py::_SHELL_CSS`가 구현 기준입니다.

| 용도 | 값 |
|---|---|
| 사이드바 폭 | `232px` |
| 사이드바 배경 | `#1B1B1D` |
| 사이드바 기본 글자 | `#C9C7C0` |
| 사이드바 보조 글자 | `#8A8880` |
| hover | `#242427` |
| active | `#2B2B2E` |
| 골드 액센트 | `#C9A26B` |
| 본문 배경 | `#F1EEE9` |
| 카드 | `#FFFFFF` |
| 경계선 | `#E7E3DB` |
| 기본 네이비 | `#1E3A6E` |
| 기본 본문 글자 | `#26282B` |

글꼴은 Streamlit 테마의 sans-serif와 운영체제 한글 시스템 글꼴을 사용합니다. 장식보다 정보 가독성, 상태 구분, 충분한 대비를 우선합니다.

## 3. 공용 컴포넌트

### App Shell

- `modules/ui.py::app_shell`: ADMIN/MANAGER
- `modules/ui.py::user_app_shell`: USER
- 접기·펼치기, 사용자 카드, 로그아웃의 동작과 접근성 라벨을 유지합니다.
- App Shell 변경은 해당 shell을 쓰는 모든 권한과 1366×768 화면을 함께 확인합니다.

### 기준정보 화면

- 헤더: `views/workspace.py::master_screen_head`
- 그리드: `selectable_master_grid`
- 작업 버튼: `master_action_bar`
- 행 상태: `_row_id`, `_row_state`, `_sel`, `_removed`
- 추가, 삭제, 저장, 새로고침은 공용 동작을 재사용합니다.
- 신규 행은 버튼 또는 붙여넣기로 만듭니다. 자동 빈 행을 추가하지 않습니다.
- 저장 실패 시 편집 내용을 유지하고 원인을 명확히 표시합니다.

### 근무표

- 날짜와 근무코드가 핵심 정보입니다.
- 근무형태 색상과 약칭은 기준정보를 사용합니다.
- 월 이동, 조회 조건, 범례는 동일 화면 안에서 일관된 형태를 유지합니다.
- 빈 월은 오류로 위장하지 않고 정상 empty state로 표시합니다.

## 4. 상태와 상호작용

- active, hover, focus, disabled 상태가 구분되어야 합니다.
- 색상만으로 선택이나 오류를 표현하지 않습니다.
- 아이콘 전용 버튼에는 tooltip 또는 접근성 라벨을 둡니다.
- 저장·삭제 결과는 성공과 실패를 구분하며 실패를 조용히 무시하지 않습니다.
- 위험한 삭제는 참조 관계와 실제 삭제 대상을 먼저 확인합니다.
- 데이터가 계층형이라는 사실은 트리, 열, 행, 패널 등 특정 시각 표현을 자동으로 뜻하지 않습니다.

## 5. 반응형 기준

- ADMIN/MANAGER 업무 화면의 기본 확인 viewport는 1366×768입니다.
- USER 화면은 좁은 모바일 폭에서도 메뉴, 달력, 로그아웃이 겹치지 않아야 합니다.
- 가로 스크롤이 필요한 데이터 그리드는 본문 전체가 아니라 그리드 영역 안에서 처리합니다.
- 고정 높이는 하단 버튼이나 사용자 카드가 viewport 밖으로 밀려나지 않는지 확인합니다.

## 6. 변경 원칙

- 단순 메뉴명·route·문구 변경은 기존 구조와 스타일을 유지합니다.
- 화면 구조나 시각 위계를 바꾸는 요청은 사용자가 제공한 요구와 레퍼런스를 먼저 따릅니다.
- 사용자가 이미 구체적인 디자인을 지정했다면 불필요한 대안을 새로 만들지 않습니다.
- 사용자가 디자인 탐색을 요청한 경우에만 대안, 목업, 별도 디자인 검토를 작업 범위에 포함합니다.
- 공용 CSS 수정은 영향을 받는 화면과 권한을 명시하고 회귀 확인합니다.

## 7. 완료 기준

디자인 관련 변경은 요청 범위에 따라 다음 중 필요한 항목만 확인합니다.

- 지정한 viewport에서 실제 렌더링
- 주요 메뉴·버튼·입력의 상태와 동작
- 텍스트 잘림, 겹침, 불필요한 전체 페이지 가로 스크롤
- 인접 화면과의 일관성
- 키보드 focus와 아이콘 버튼 라벨
- 관련 UI 계약 테스트

디자인 검증은 기능 검증을 대체하지 않으며, 기능 테스트도 실제 시각 결과를 대신하지 않습니다.
