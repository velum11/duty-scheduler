# 현장 운영관리 — 화면 설계 기준

이 문서는 **무엇을 강제하고 무엇을 판단에 맡기는지**를 구분해 적는다.

- **§1 토큰**은 전역이며 예외가 없다. 기계로 검증한다.
- **§2 화면 유형**의 골격은 유형마다 다르다. 한 유형의 규칙을 다른 유형에 적용하지 않는다.
- **§4 금지 조항**은 반드시 이유와 함께 적는다. 이유를 쓸 수 없는 규칙은 규칙이 아니다.

화면별 픽셀값은 규정하지 않는다. 값이 필요하면 §1 토큰에서 고른다.

---

## 0. 화면을 만들기 전에 쓰는 한 문장

새 화면·구조 변경 화면은 코드 상단에 **이 화면이 내리는 결정**을 한 문장으로 적는다.

```python
SCREEN_ARCHETYPE = "WORKLIST"
SCREEN_DECISION = "이 건의 발생 빈도와 피해 강도가 얼마인가"
SCREEN_EVIDENCE = ("사고 내용", "첨부 사진", "빈도·강도 기준표", "유사 작업 발생 이력")
```

레이아웃은 이 문장에서 나온다. 결정에 필요한 근거가 결정 컨트롤보다 **앞에** 오고, 근거가 아닌 것은 뒤로 간다.

문장을 정확히 쓰는 것이 중요하다. 위 예시에서 평가자가 정하는 것은 **등급이 아니라 두 축**이고 등급은 산출값이다(§7.2-1). 결정 문장을 "어떤 등급을 줄 것인가"로 쓰면 화면이 등급 버튼 5개로 잘못 설계된다.

> **왜** — 종전 명세는 블록 순서와 픽셀값만 규정하고 화면의 목적을 적지 않았다. 그래서 구현은 명세를 지켰는데도 "왜 이렇게 생겼는지" 설명할 수 없는 화면이 나왔다. 목적이 적혀 있지 않으면 결과물에도 목적이 드러나지 않는다.

---

## 1. 토큰 — 전역, 예외 없음

### 1.1 간격

`4 · 8 · 12 · 16 · 24 · 32 · 40` (px). 2·4·8의 배수만 쓴다.

컴포넌트 내부 간격과 컴포넌트 사이 간격에 같은 스케일을 쓴다. 이 목록에 없는 값은 쓰지 않는다.

> **왜** — 종전에는 `14.5 / 22 / 26 / 420 / 260`처럼 서로 배수 관계가 없는 값이 파일마다 흩어져 있었다. 스케일이 없으면 매번 눈대중이 되고, 화면끼리 리듬이 어긋난다.

**예외** — 데이터 밀도가 지배하는 곳(매트릭스 셀 폭·높이, sticky 신원 열 폭)은 스케일 밖 값을 쓸 수 있다. 31일을 한 화면에 넣는 것이 스케일보다 우선한다. 예외를 쓰면 그 이유를 코드 주석에 남긴다.

### 1.2 타이포그래피

| 역할 | 크기 / 굵기 | 용례 |
|---|---|---|
| page | 28 / 600 | 화면 제목 |
| section | 20 / 600 | 상세 대상 제목, 섹션 |
| object | 16 / 600 | 식별자(보고번호 등) |
| body | 14 / 400 | 본문, 표 값, 입력 |
| label | 12 / 400 | 라벨, 캡션, 메타 |

본문 **IBM Plex Sans KR**, 숫자·ID·코드·날짜 **IBM Plex Mono**.
세로로 정렬되는 숫자에는 `font-variant-numeric: tabular-nums`를 건다.

다섯 단계 밖의 크기를 쓰지 않는다. 긴 문장에 `text-wrap: pretty`.

### 1.3 색

sign-off된 팔레트를 유지한다. 새 색을 만들지 않는다.

| 역할 | 값 |
|---|---|
| 캔버스 | `#f4f2ee` |
| 표면 / 입력 | `#fbfaf8` / `#ffffff` |
| 텍스트 | `#1c1a17` · 보조 `#4a453d` · 약함 `#6b665d` |
| 비활성 · 장식 | `#8b857c` (비텍스트 전용) |
| 헤어라인 | 행 `#e6e2da` · 표 헤더 `#cfc8bd` · 섹션 `#e0dbd2` |
| 액센트 | `#c2410c` (hover `#a3350a`, 텍스트 `#b4451a`, 틴트 `#fdf3ec`) |
| 사이드바 | 배경 `#1a1917` · 구분선 `#2b2925` · 선택 `#2f2c26` · 텍스트 `#a49d92` / 선택 `#fdf3ec` |

**읽는 텍스트의 최저 대비는 `#6b665d`**(캔버스 위 5.1:1)다. `#8b857c`(3.27:1)·`#a09a90`(2.5:1)은 읽는 텍스트에 쓰지 않는다.

**의미 색** — 배경 / 글자 / 테두리

| 의미 | 값 |
|---|---|
| 대기 · 주의 | `#fdf3e3` / `#8a6212` / `#f0dfbe` |
| 진행 · 정보 | `#eef2fb` / `#2f4d99` / `#dbe3f4` |
| 완료 · 정상 | `#eef5f0` / `#2f6b45` / `#d8e6dd` |
| 부정 · 반려 | `#fbeeee` / `#9c3232` / `#f0d9d9` |
| 종료 · 중립 | `#f2f0ec` / `#5c564d` / `#e4e0d8` |

의미 색은 상태 배지, 셀 채움, 부정적 액션 버튼에 **같은 값**으로 쓴다. 상태마다 색을 따로 만들지 않는다.

**도메인 색은 이 팔레트의 예외다.** 사용자가 기준정보에서 등록하는 색(근무형태 `색상` 등)은 **UI 토큰이 아니라 데이터**다. 디자이너가 정하는 값이 아니라 현장이 정하는 값이므로 팔레트로 제한하지 않는다.

> **왜** — 근무형태는 종류가 계속 늘어난다(§7.2-3). 유한한 팔레트로는 감당할 수 없고, 어느 색을 쓸지는 현장 관행이 정한다. 팔레트 규칙은 화면 크롬(배경·헤어라인·상태·액션)에 적용되고, 데이터가 실어나르는 색은 별개 축이다.

도메인 색에는 두 가지가 따라붙는다.

- **그 위의 텍스트는 대비를 계산해 보장한다.** 임의 색이 오므로 고정 글자색을 쓸 수 없다. (`master_work_types.py`의 `_textOn` 구현이 이 계약이다)
- **색은 보조 수단이다. 구분은 약칭이 담당한다.** 종류가 늘면 색만으로는 구분이 불가능해지고, 색각 이상 사용자에게도 색은 단독 신호가 될 수 없다.

### 1.4 밀도

| 밀도 | 대상 | 히트 영역 |
|---|---|---|
| **cozy** | 터치 조작. §3.4의 모바일 우선 화면 | 44px 이상 |
| **compact** | 기본. 마우스 조작 데스크톱 | 34~38px |
| **condensed** | 그리드 표 전용. compact 위에 덧붙여 적용 | 24~28px |

- condensed는 **그리드 표에만** 쓰고 폼·상세·버튼에는 쓰지 않는다.
- **compact·condensed는 터치로 조작할 수 없다.** 손가락으로 34px 이하를 정확히 누를 수 없다. 모바일 우선 화면은 cozy를 쓰고, condensed 화면은 §3.4에서 모바일 미지원으로 둔다.
- cozy와 condensed를 한 화면에 섞지 않는다.

> **왜** — 근무표 편성은 31일 × 12인을 한 화면에 넣어야 하고, 아차사고 등록 폼은 그럴 이유가 없다. 두 화면에 같은 행 높이를 강제하면 한쪽은 반드시 망가진다. 종전 명세가 "편성표 셀도 14.5px"로 못박은 것이 이 실패였다.

---

## 2. 화면 유형

유형은 `SCREEN_ARCHETYPE` 상수로 선언하고 `scripts/test_screen_scaffold.py`가 검증한다.
**골격은 유형마다 다르다.** 한 유형에서 만든 레이아웃 규칙을 다른 유형에 옮기지 않는다.

### WORKLIST — 처리할 건을 골라 결정한다

- **결정** — 이 건을 어떻게 처리할 것인가 (등급 확정 · 승인 · 반려)
- **골격** — 제목 → 상태 탭 → 2열(목록 33% / 상세 67%)
- **밀도** — compact
- **해당** — 평가 관리, 개선조치 관리, 업무요청 처리\*, 숙소 예약 승인 관리\*

```
제목 / 설명
[상태 탭]  대기 5 │ 진행중 1 │ 전체 6      ← 탭은 목록을 실제로 바꾼다
──────────────────────────────────────
[목록 33%]              │ [상세 67%]
 제목            경과일  │  식별자 + 상태 배지 + 대상 제목 + 메타
 번호 소속        분류   │  ── 주요 내용 표
                        │  ── 첨부
                        │  ── 결정(근거 → 컨트롤 → 실행)
```

- **목록 행은 2줄 고정**이다. 상태에 따라 줄 수가 달라지면 안 된다 — 선점·잠금 같은 상태는 좌측 2px 보더 색으로 표현한다.
- 목록 행에는 **무엇을 먼저 처리할지 고를 근거**를 담는다(경과일·소속·분류). 식별자만 있는 행은 정렬 순서 외에 아무 판단도 돕지 못한다.
- 상세는 **근거 → 결정 컨트롤 → 실행 버튼** 순서다. 판정에 쓰는 자료가 판정 컨트롤보다 뒤에 오면 안 된다.
- 지표는 탭 안의 건수로 표현한다. **읽기만 하는 지표 타일을 따로 두지 않는다** — 합계 타일은 앞의 두 타일을 더한 값이라 새 정보가 없다.

레퍼런스: SAP Fiori Worklist floorplan. 승인 큐는 마스터-디테일 레이아웃을 쓴다.

### READ_VIEW — 현재 상태를 확인한다

- **결정** — 다음에 무엇을 할 것인가 (또는 결정 없음)
- **골격** — 제목 → 조회 조건 → 표
- **밀도** — compact, 행이 많으면 condensed
- **해당** — 아차사고 조회, 월간 근무표, 내 근무표, 내 아차사고, 숙소 예약 캘린더\*, 내 숙소 예약\*

  `내 업무요청`\*은 종결 확인·보완 요청 2지 판정이 있어 WORKLIST 여지가 있다 — §7.2-9 미결.

### FORM_ENTRY — 새 사실을 기록한다

- **결정** — 제출할 준비가 되었는가
- **골격** — 제목 → 신원 줄 → 섹션별 필드(라벨 열 + 값 열) → 하단 고정 제출 바
- **밀도** — compact
- **해당** — 아차사고 등록, 업무요청 제출\*, 숙소 예약 신청\*

```
[신원]  신고자 · 사번 · 소속          ← 세션에서 확정, 편집 불가
01 · 기본 정보
  작업명 *        [                    ]   ← 라벨 열 130px 고정
  발생일 *        [160px]                  ← 컨트롤은 제 폭만
02 · 상황 기술
  사고내용 *      [                    ]
  …
─────────────────────────────────────
필수 7/8 ▓▓▓▓░  예방대책이 남았습니다   [제출]   ← 하단 고정
```

- **필드는 §3.1 라벨 열 + 값 열 구조를 쓴다.** 읽기 화면(WORKLIST 상세·READ_VIEW)과 같은 라벨 열 폭·같은 필드 순서를 쓴다.

  > **왜** — 같은 레코드를 등록에서 쓰고 평가에서 읽는다. 두 화면의 배치와 순서가 다르면 작성자는 자기 글이 어떻게 보일지 예측할 수 없다.

- **필수 항목 충족 상태는 하단 고정 바에 개수 + 남은 항목 이름으로 표시한다.** 별도 체크리스트 패널을 두지 않는다.

  > **왜** — 라벨 열 구조에서는 폼 자체가 세로 목록이라 빈 값이 이미 보인다. 항목을 패널에 다시 나열하면 §4-2(진입점 중복)에 걸리고, 필드가 늘 때 두 곳을 고쳐야 한다.

- 제출 버튼을 눌러야 뭐가 빠졌는지 알게 하지 않는다.

### EDIT_GRID — 여러 건을 한 번에 고친다

- **결정** — 무엇을 저장할 것인가
- **골격** — 제목 → 필터 → 건수 + 편집 상태 칩 → 그리드
- **밀도** — condensed
- **해당** — 사용자 관리, 조직 관리, 근무형태 관리, 부서 관리, 조 관리

저장·삭제·추가·새로고침의 실행은 상단 헤더 아이콘이 담당하고 본문에 같은 버튼을 두지 않는다.

> **왜** — 같은 기능의 진입점이 두 곳이면 그 자체가 불일치다. (2026-07-31 사용자 결정)

### MATRIX_EDIT — 격자를 직접 채운다

- **결정** — 무엇을 저장할 것인가
- **골격** — 제목 → 필터 + 액션 → 범위·집계 스트립 → 입력 단축키 → 격자 → 범례
- **밀도** — condensed 필수
- **해당** — 근무표 편성

- 신원 열은 `sticky left`, 헤더는 `sticky top`. 격자는 자체 스크롤 컨테이너를 갖는다.
- 셀 입력은 클릭 순환 + 숫자키. **붙여넣기 입력창을 두지 않는다**(Excel `Ctrl+V` capability는 유지).
- 셀 폭·행 높이는 §1.1 스케일 예외다.

### DASHBOARD — 어디에 개입할지 정한다

- **결정** — 무엇부터 손댈 것인가
- **골격** — 제목 → 지표 스트립 → 분포·추이 블록
- **밀도** — compact
- **해당** — 대시보드, 아차사고 분석

지표는 제목 바로 아래 첫 블록에 둔다.

**역할에 따라 다른 화면을 만들지 않는다.** 같은 화면 id는 누가 보든 같은 골격·같은 블록을 쓰고, **권한은 데이터 범위로만 갈린다**(전사 / 자기 부서). 개인 맥락이 필요하면 목록 안에서 본인 항목을 표시하는 정도로 그친다.

> **왜** — 대시보드는 `dashboard.py:132`에서 USER를 별도 함수로 분기시켜 카드 5장짜리 다른 화면을 보여줬다. 이름만 같고 내용이 달라 유지보수가 두 배가 되고, 그 카드들("오늘 내 근무" 등)은 내 근무표와 중복이라 §4-2에도 걸렸다. (2026-08-18 사용자 결정 — USER 분기 폐기)

> **\* 표시 화면은 `app.py`에 라우팅되지 않는 프로토타입이다.** `scripts/preview_new_screens.py`에서만 열린다.
> 선언된 22개 화면 중 실제 서비스되는 것은 15개다. 프로토타입은 이 기준을 따르되 검증 대상에서 제외한다.

---

## 3. 공통 패턴

### 3.1 가변 길이 텍스트

**값 열은 하나만 둔다.** 라벨을 좌측 열로 전치하고 값을 한 열에 모은다.

> **왜** — 값 열이 둘 이상이면 폭을 `flex basis`로 맞춰야 하는데, 내용 길이가 다르면 반드시 어긋난다. 같은 결함이 이 프로젝트에서 세 번 재발했다. 열을 하나로 줄이면 맞출 대상이 사라진다.

산문(30자 이상)과 짧은 분류값을 같은 구조에 넣지 않는다. 산문은 세로 블록, 짧은 값은 속성 목록으로 나눈다.

**입력 화면에도 같은 구조를 쓴다.** 라벨은 좌측 열, 컨트롤은 값 열에 두고 각자 필요한 폭만 쓴다(날짜 160px, 코드 180px, 서술 전폭). 같은 레코드를 다루는 읽기 화면과 **라벨 열 폭·필드 순서를 일치**시킨다.

### 3.2 액션

- 현재 상태에서 **불가능한 전이는 그리지 않는다.** 조건만 안 갖춰진 것(입력 필요 등)은 비활성으로 두고 **사유를 화면에 쓴다.**
- 비활성 사유를 툴팁에만 두지 않는다. 마우스를 올려야 알 수 있으면 모르는 것과 같다.
- 주 액션은 우측 끝 액센트 채움, 부정적 액션(반려·삭제)은 부정 의미 색, 나머지는 기본 버튼.
- 같은 크기·같은 색 버튼을 4개 이상 나열하지 않는다.

### 3.3 상태 표현

상태는 배지로, 그 상태의 **결과**는 별도 표기로 적는다. 예: `검토중` 배지 옆에 `보고자 수정 잠김`.

> **왜** — 상태 이름만으로는 그 상태가 무엇을 막고 무엇을 여는지 알 수 없다. 사용자가 버튼을 왜 눌러야 하는지 모르는 화면이 실제로 있었다.

### 3.4 반응형

**모바일 지원은 유형이 아니라 화면별로 정한다.** 전 화면을 모바일에 맞추지 않는다.

| 등급 | 뜻 | 해당 화면 |
|---|---|---|
| **모바일 우선** | 좁은 폭을 먼저 설계하고 데스크톱으로 넓힌다. 밀도 cozy | 대시보드, 내 근무표, 아차사고 등록 |
| **1열 폴백** | 데스크톱 설계를 좁은 폭에서 무너지지 않게만 한다 | WORKLIST 전부, 그 외 READ_VIEW |
| **미지원** | 좁은 폭에서 안내만 띄운다 | MATRIX_EDIT, EDIT_GRID |

> **왜** — 근무표 편성은 31일 × 12인을 condensed로 채우는 화면이라 터치로 조작할 수 없다(§1.4). 기준정보 편집도 마찬가지다. 이 둘을 억지로 모바일에 맞추면 데스크톱 밀도를 잃는다. 대신 좁은 폭에서 "가로로 돌리거나 PC에서 여세요"를 명확히 안내한다.

**브레이크포인트** — `<599` / `599–1023` / `>1023`. FCL 기준과 같은 값을 쓴다.

**폭에 따라 바뀌는 것**

- **라벨 열 + 값 열은 `<599px`에서 세로로 접는다** — 라벨 위, 값 아래. 값 열이 하나라는 §3.1 성질은 그대로 유지된다(오히려 강해진다).
- **지표 스트립** — 6열 → `599–1023`에서 3열 → `<599`에서 2열. 좌측 보더는 유지한다.
- **2열 분할(WORKLIST)** — `<599`에서 1열. 목록을 보여주고 선택하면 상세로 이동한다.
- **표** — `overflow-x: auto` + `min-width`. 열을 줄이지 않는다(숨기면 어느 열이 사라졌는지 알 수 없다).

**공통** — `flex: N 1 <basis>` + `min-width: 0`. 목록 320 / 상세 460 / 폼 480.
셸 폭, MATRIX sticky 열, 일자 열 폭은 고정폭 예외다.

### 3.5 카피

담백한 실무체. 감탄사·이모지 없음. 숫자에 단위(건·일·%). 빈 상태 문구로 화면을 채우지 말고 애초에 빈 영역이 생기지 않게 배치한다.

### 3.6 어휘 — 한 도메인에 한 낱말

한 업무 흐름에서 **같은 개념에 두 낱말을 쓰지 않는다.** 화면명·역할·상태·액션·필드가 같은 어근을 공유해야 한다.

| 도메인 | 정본 | 상태 | 액션 | 등급 |
|---|---|---|---|---|
| 아차사고 평가 | **평가** | 평가 대기 → **평가중** → 평가완료 | **평가착수** → 평가확정 | **평가 등급** |

> **왜** — 종전에는 한 화면에 세 낱말이 섞였다. 화면명은 "평가 관리", 역할은 "평가자", 완료 상태는 "평가완료"인데 진행 상태만 "검토중"이고 시작 액션만 "검토착수"였다. 등급은 "확정 등급"이라는 세 번째 낱말을 썼는데, 이는 제안등급과 대비하기 위한 수식어였고 제안등급 폐기(§7.2-1b)로 대비 대상이 사라졌다.

**영문 코드값은 바꾸지 않는다.** `IN_REVIEW`·`confirmed_grade`는 그대로 두고 한글 라벨만 통일한다. 코드값이 한글 표기를 규정하지 않는다.

**표시 폭이 좁은 곳에서는 줄일 수 있다** — 표 헤더처럼 열이 좁으면 `평가 등급` → `등급`. 줄임은 허용하되 다른 낱말로 바꾸지 않는다.

**앱 이름에 범위를 담지 않는다.** 사이드바·탭 제목은 `WorkOps` 단독이며 한글 부제를 두지 않는다.

> **왜** — 종전 부제 `현장운영`은 근무표·아차사고·숙소예약·업무요청서를 포괄하려고 골랐는데, 시험성적서·계측기 주기관리가 들어오자 범위를 좁게 규정하는 이름이 됐다. 모듈이 계속 늘어나는 한 어떤 범위 서술어도 같은 일을 겪는다. (2026-08-18 사용자 결정)

**라벨은 단일 출처를 갖는다.** 화면마다 상태·등급 라벨 사전을 복제하지 않는다.

> **왜** — `_STATUS_LABEL`이 최소 3개 화면(`near_miss_evaluate.py:55`·`near_miss_my.py:53`·`near_miss_view.py:47`)에 복제돼 있다. 한 낱말을 바꾸려면 전부 찾아 고쳐야 하고, 하나를 빠뜨리면 화면마다 다른 말이 나온다. §1.3 팔레트가 파일마다 복제된 것과 같은 실패다.

---

## 4. 금지 — 이유와 함께

1. **읽기 전용 지표 타일을 화면 상단에 나열하지 않는다.** 지표가 목록을 바꾸지 않으면 자리값을 못 한다. 상태별 건수는 탭으로 만든다.
2. **한 화면에 같은 기능의 진입점을 두 곳 두지 않는다.** 어느 쪽이 정본인지 사용자가 알 수 없다.
3. **비활성 컨트롤의 사유를 숨기지 않는다.** 사유 없는 회색 버튼은 고장과 구분되지 않는다.
4. **가변 길이 텍스트를 여러 값 열에 나눠 담지 않는다.** (§3.1)
5. **행 높이가 상태에 따라 달라지는 목록을 만들지 않는다.** 리듬이 깨지면 스캔이 불가능해진다.
6. **읽는 텍스트에 `#8b857c` 이하 대비를 쓰지 않는다.** 3.27:1은 본문 기준 미달이다.
7. **§1 토큰 밖의 색·폰트 크기를 만들지 않는다.** 예외는 §1.1의 밀도 예외뿐이며 주석으로 사유를 남긴다.

**이 목록에 없는 것은 금지가 아니다.** 종전 명세의 "좌우 분할 마스터-디테일 금지"는 이유 없이 적혀 있었고, 그 결과 승인 큐라는 표준 패턴을 막아 목록에 우선순위 정보를 담을 자리를 없앴다. 규칙을 추가할 때는 이유를 함께 적고, 이유가 사라지면 규칙도 폐기한다.

---

## 5. 검증

**기계 검증** — `scripts/test_screen_scaffold.py`

현재 검사하는 것:

- `SCREEN_ARCHETYPE`이 허용 유형인가
- 표준 헤더 크롬을 실제로 호출하는가

**아직 검사하지 않는 것** — `SCREEN_DECISION` 문장 존재 여부. §0이 요구하지만 검사가 없고, 22개 화면 전부 미선언 상태다. 검사를 추가하기 전까지 §0은 규범이지 집행 사항이 아니다.

**측정** — visual-qa (렌더 DOM 기하)

- 토큰 밖 색·폰트 크기 사용 여부
- 대비율 4.5:1 이상 (읽는 텍스트)
- 1366×768에서 겹침·잘림·가로 스크롤 없음
- 목록 행 높이 편차 0

**사람** — 사용자 실브라우저 sign-off. 최종 게이트다.

기계가 판정할 수 없는 것(정보 우선순위, 근거 배치의 적절성)을 기계 검증 항목으로 만들지 않는다.

---

## 6. 참조 구현

| 유형 | 화면 | 상태 |
|---|---|---|
| WORKLIST | 평가 관리 | 목업 합의 완료 |
| MATRIX_EDIT | 근무표 편성 | 목업 검증 완료 |
| EDIT_GRID | 사용자 관리 | 기존 구현 — 표준 원본 |

새 화면은 같은 유형의 참조 구현을 먼저 읽는다.

---

## 7. 미해결

이 문서를 확정하기 전에 답이 필요한 것들이다. **채워지기 전까지 해당 화면은 완성으로 보지 않는다.**

### 7.1 확정된 이관 — 코드를 문서에 맞춘다 (2026-08-18 사용자 결정)

§2 WORKLIST 골격(상태 탭 + 2열 33/67)은 **현재 구현체가 0개**다. `erp.master_detail_frame`은 정의만 있고 미호출, `st.tabs`는 전 화면 0회다. 그리고 `scripts/test_near_miss_ui_gates.py:168`이 **좌우 분할 제거를 PASS 조건으로 강제**한다.

사용자 결정에 따라 **문서를 유지하고 코드를 이관한다.** 최소 12곳:

| 대상 | 내용 |
|---|---|
| **`views/common/scaffold.py:46`** ← **먼저** | `ARCHETYPES`에 `WORKLIST` 추가, `MASTER_DETAIL` 제거. `:3` docstring "5유형" → 실제 개수.<br>**이 항목이 첫 순서다** — `screen-design` 스킬이 값의 출처로 이 튜플을 읽으므로(`SKILL.md:16`), 여기가 바뀌기 전에는 스킬을 켜도 `WORKLIST`를 모른다 |
| `SCREEN_ARCHETYPE` 7개 | 판정 4개(`near_miss_evaluate` `near_miss_improvement` `work_request_handle` `lodging_manage`) → `WORKLIST` / 조회 3개(`near_miss_my` `lodging_my` `work_request_my`) → `READ_VIEW` |
| `scripts/test_screen_scaffold.py:492` | 6튜플 하드 assert 갱신 |
| `scripts/test_near_miss_ui_gates.py:161,168` | `MASTER_DETAIL` assert 및 **좌우분할 금지 PASS 조건 폐기** |
| 승인 큐 4화면 | 칩 스트립 + 전체폭 상세 → 상태 탭 + 2열. `erp.master_detail_frame` 실사용 시작 |
| **`modules/db.py:1743`** `NEAR_MISS_TRANSITIONS` | `SUBMITTED`의 허용 전이에서 `EVALUATED`·`REJECTED`를 빼고 `IN_REVIEW`만 남긴다(§7.2-6 확정 반영). **화면 골격만 옮기면 코드는 여전히 두 전이를 허용한다** |
| `docs/database.md:183` 전이표 | 같은 내용으로 문서 갱신. `docs/database.md:91`의 "보고자가 등급을 제안하고 평가자가 확정하는 2단계 모델" 서술도 §7.2-1b(제안등급 폐기)·§7.2-1(빈도×강도 산출)에 맞춰 정정 |

**담당 분리** — 화면 골격 이관은 `ui-feature`, **상태 전이 상수·DB 문서·migration은 `data-contract`** 소관이다(§7.3-4). 한 Owner가 다 하지 않는다.

**회귀 기준선 (2026-08-18 측정)** — `scripts/test_*.py` **35개 전부 통과, 실패 0**(원격 쓰기하는 `test_supabase_crud.py` 제외). 이관 중 이 숫자가 떨어지면 원인을 먼저 찾는다. 이관으로 **의도적으로 깨지는 것은 아래 3개뿐**이며, 그 외가 깨지면 이관 범위를 벗어난 것이다.

| 깨질 assert | 조치 |
|---|---|
| `test_screen_scaffold.py:492` | 튜플의 `"MASTER_DETAIL"` → `"WORKLIST"` 문자열 교체 |
| `test_near_miss_ui_gates.py:161` | 비교 문자열 교체 |
| `test_near_miss_ui_gates.py:168` | **조건 폐기 후 재작성** — 좌우분할 금지 → `master_detail_frame` 사용 + 비율(33/67) 검사 |

**구현 수단 (조사 완료, Streamlit 1.59.1 기준)**

| 요소 | 수단 | 상태 |
|---|---|---|
| 33/67 분할 | `erp.master_detail_frame(list_ratio=1, detail_ratio=2)` | 기존 자산. 기본값이 1.5/1.0(≈60/40)이라 **인자를 반드시 넘겨야 한다** |
| 상태 탭 | `st.segmented_control` | `my_schedule.py:408`에 실사용 선례. 클릭=rerun이 기본이라 필터링이 자동 성립 |
| 목록 독립 스크롤 | `st.container(height=520)` | 표준 API. 저장소 최초 사용 |
| 목록 행 2줄 + 클릭 선택 | **`erp.select_list` 신규 작성 (확정)** | `st.button`은 label이 inline 요소만 허용해 2줄 불가. `select_grid`는 `_SELECT_COL_CONFIG_ALLOWED`(`kit.py:653`)가 `cellRenderer`를 구조적으로 차단. `st.components.v2.component`(`my_schedule.py:49` 패턴)로 만든다 — WORKLIST 4화면이 공유한다 |
| 평가 등급 입력 | **빈도 × 강도 매트릭스 직접 선택 (확정)** | 칸 하나 = (빈도, 강도) = 등급. 클릭 1회로 셋이 정해지고 기준표가 곧 입력 UI가 된다. 등급 버튼 S~D 5개는 폐기 |

`st.tabs`는 기본값이 모든 탭을 미리 렌더해 필터링이 성립하지 않는다. `on_change="rerun"`을 주면 되지만 저장소에 선례가 없어 `segmented_control`을 우선한다.

### 7.2 미해결

| # | 항목 | 상태 |
|---|---|---|
| 1 | 등급 산출 방식 | **확정(2026-08-18 사용자 결정) — 가능성 × 중대성 5×4 격자, 조합은 행렬법.**<br>평가자는 등급을 직접 고르지 않고 **칸 하나**를 누른다. 칸 = (가능성, 중대성) = 등급.<br>**척도는 KOSHA 위험성평가지원시스템(KRAS)의 5×4를 따른다** — 가능성 5단계 × 중대성 4단계.<br>**조합은 곱셈법이 아니라 행렬법이다**(KOSHA 지침이 제시하는 행렬법·곱셈법·덧셈법·분기법 중 하나). 칸마다 등급을 직접 지정한다.<br><br>**왜 곱셈법을 쓰지 않는가** — 곱셈은 중대성을 빈도로 나눈다. `중대성 4(사망·영구장애) × 가능성 2(드묾) = 8점`이 "허용 가능·계획적 개선" 구간에 떨어진다. **아차사고 관리에서 가장 놓치면 안 되는 유형이 가장 낮게 나온다.** 20칸 분포도 `D 10 / A 1`로 치우친다.<br>덧셈법은 반대로 빈도를 과대평가한다(`중대성 1 × 가능성 5 = 6 → B`).<br><br>**격자를 지배하는 두 규칙**<br>① **중대성 4는 최소 A** — 사망 가능은 드물어도 중대하다<br>② **중대성 1은 최대 C** — 안 다치는 일은 아무리 잦아도 상한이 있다 |
| 1-c | 격자 값 | **구조 확정 · 값은 사내 조정 대상.**<br>확정된 것 — 5×4 격자, 행렬법, §7.2-1의 두 규칙.<br>**아직 임시값인 것** — ① 가능성 5단계·중대성 4단계의 **라벨 문구**(KOSHA 공식 문구는 PDF 추출이 막혀 못 구했다. 현재는 `거의 없음/드묾/가끔/자주/상시` × `무휴업/단기 휴업/장기 휴업/사망·영구장애`로 구성) ② **20칸 각각의 등급**(두 규칙을 지키며 제가 배치).<br>사내 안전 기준·위험성평가 기준표가 있으면 **라벨과 칸 값만 갈아끼운다.** 격자 구조와 화면은 그대로다 |
| 1-d | 오버라이드 | **확정(2026-08-18 사용자 결정) — 허용하되 변경 사유를 필수로 받는다.**<br>산출 등급을 덮어쓸 수 있으나 **사유 없이는 평가확정이 활성화되지 않는다**(§3.2 — 비활성 사유를 화면에 쓴다).<br>화면은 `산출 B → 확정 A` 형태로 **바꿨다는 사실 자체를 남긴다.** 저장은 `frequency`·`severity`·`confirmed_grade`·`변경 사유` 넷이며, **산출 등급은 컬럼을 두지 않고 두 축에서 파생**한다(격자가 바뀌면 과거 건의 산출값도 소급해 바뀌는 것을 감수).<br>**왜 사유가 필수인가** — 사유 없이 덮어쓸 수 있으면 격자가 장식이 된다. 그리고 "격자를 안 따른 비율"을 볼 수 있어야 한다 — 그 비율이 높으면 개별 판정이 아니라 **격자 자체를 고쳐야 한다는 신호**다 |
| 1-e | 빈도·강도 저장 | **미해결.** `frequency`·`severity` 컬럼 추가 migration 필요. 현재 `confirmed_grade`만 있어 **등급의 근거가 저장되지 않는다** — 공정성이 목적인데 근거를 안 남기면 반쪽이다.<br>**백필은 설계하지 않는다**(§7.2-1f — 기존 데이터가 전부 샘플). 새 컬럼은 nullable로 추가하고 기존 행은 비운 채 둔다.<br>게이트: `AGENTS.md` 승인 + lee-mode Codex 2단(§7.3-1) |
| 1-f | 기존 평가 데이터 | **해소(2026-08-18 사용자 확인) — 현재 아차사고 데이터는 전부 샘플이며 폐기 예정이다.** 소급 입력·백필을 설계하지 않는다. 매트릭스는 빈 상태로 두고, 빈도·강도가 없는 레코드는 과도기 표시만 처리한다 |
| 1-b | 제안등급 폐기 | **확정(2026-08-18 사용자 결정) — 등급은 확정등급만 쓴다.** `proposed_grade`는 등록 폼에 입력 경로가 없어 신규 건은 항상 비는데도 4곳에서 표시된다: `near_miss_evaluate.py:331`, `near_miss_my.py:287,430`, `near_miss_view.py:350,619`, `near_miss_pdf.py:106,207`. 표시를 전부 제거하고, `near_miss_my`의 자기 정정 경로(`:665`)에서도 입력을 뺀다. DB 컬럼·제약(`006_near_miss.sql:55,86`)은 **미사용으로 남긴다**.<br>단 §7.2-1f로 데이터 보존 제약이 사라졌으므로 **컬럼 제거도 선택지**다 — 다만 되돌리기 비싼 스키마 변경이라 `frequency`/`severity` 추가 migration과 **한 번에 묶어 판단**한다 |
| 2 | 근무형태 색 | **해소(2026-08-18) — §1.3 "도메인 색" 예외로 정리.**<br>기준정보 `근무형태 관리`가 이미 색상 피커(`master_work_types.py:126`)·약칭 중복 검증(`:162`)·선택 색 위 텍스트 대비 자동 계산(`:194` `_textOn`)을 갖추고 있고, `work_types.color`가 근무표 렌더의 단일 기준이다(`:10`).<br>**팔레트로 제한하지 않는다** — 종류가 계속 느는 도메인 데이터라 유한 팔레트로 감당할 수 없고, 색은 현장이 정한다. 대신 **대비 보장**과 **약칭이 주 구분 수단**이라는 두 계약이 붙는다 |
| 3 | 근무형태 종류 | **확정(2026-08-18 사용자 확인) — 종류 수를 고정하지 않는다. 늘어난다.**<br>따라서 **약어(`short_label`)를 설계 기준으로 삼는다.** 실측 최대 3자(`야-4`·`OFF`·`특주`). 달력 셀·범례·매트릭스는 약어 폭에 맞춰 설계하면 종류가 늘어도 안 깨진다.<br>**약어 최대 3자를 기준정보 입력 제약으로 건다** — 이 제약이 없으면 화면 설계가 데이터에 인질로 잡힌다.<br>`seed_default_work_types.py`(6종)와 `data/sample/work_types.csv`(10종)가 다른 것은 종류를 고정하지 않기로 한 이상 정본 다툼이 아니다. 다만 **seed 스크립트가 샘플과 다른 세트를 심는 것**은 별개 정리 대상이다 |
| 4 | 경과일 기준 | **확정(2026-08-18 사용자 결정) — 접수일 기준.** `created_at`(`006_near_miss.sql:79`)에서 파생한다. `incident_date`가 아니다 — 평가자의 책임 구간은 접수 이후다.<br>**주의**: `006_near_miss.sql:69` 주석의 "기간 통계는 `incident_date` 기준, `created_at` 아님"은 **집계 축**에 대한 규칙이고, 경과일은 처리 지연 측정이라 별개다. 나중에 누가 "일관성"을 이유로 `incident_date`로 바꾸지 않도록 이 구분을 남긴다.<br>**미해결 2건** — ① 계산 코드가 아직 없다(신규 작성, `timestamptz` → 한국 날짜 경계 처리 필요) ② 보완요청으로 반송·재제출된 건의 경과일을 최초 접수부터 누적할지, 재제출 시점부터 다시 셀지 |
| 4-b | 경과일 강조 임계 | **확정 — 강조하지 않는다.** SLA가 없으면 색을 칠하지 않는다. 목록이 경과일 내림차순이라 순서가 이미 우선순위를 말하고, 색은 같은 말을 두 번 한다. SLA가 정해지면 그때 넣는다 |
| 4-c | 반송 건의 경과일 | **확정 — 최초 접수부터 누적한다.** 보완요청 반송은 처리 과정의 일부지 새 접수가 아니다.<br>**왜 리셋하지 않는가** — 리셋하면 반송을 반복해 경과일 지표를 0으로 되돌릴 수 있다. 지연을 숨기는 경로를 만들지 않는다 |
| 5 | 헤더 아이콘 | **확정 — 화면별로 실제 쓰는 것만 노출한다.** 8종 상시 노출 + 음영을 폐기한다.<br>**왜** — 라벨 없는 아이콘이라 비활성 사유가 툴팁뿐이고(§4-3 위반), 미매핑 화면은 8개 중 7개가 영구 회색이다. 2026-07-31 "전 화면 통일" 결정의 목적은 일관성이었는데, 영구 비활성 7개는 일관성이 아니라 소음이다. 같은 아이콘이 같은 자리에 있는 것은 유지하되 **쓰지 않는 것은 그리지 않는다**(§3.2) |
| 6 | 착수 없이 반려 | **확정 — 차단한다.** `SUBMITTED → REJECTED` 직행을 막고 평가착수를 거치게 한다.<br>**왜** — 즉시 확정을 막은 것과 같은 이유다. 평가자가 여러 명이면 반려도 담당자가 기록돼야 하고, 반려는 종결 분기라 되돌리기가 비싸다. 전이가 `평가착수 → 평가중 → 반려` 하나로 통일된다 |
| 7 | USER 조회 범위 | **확정 — MANAGER와 같은 자기 부서.** 대시보드 역할 통합(§2 DASHBOARD)에 따라 USER에게도 범위가 필요해졌다.<br>**왜** — 같은 부서의 오늘 근무는 현장에서 이미 공유되는 정보고, `_scope_for`의 기존 fail-closed 패턴을 그대로 재사용하면 새 권한 축이 생기지 않는다. 부서 미확정이면 MANAGER와 동일하게 전체 차단 |
| 5 | 헤더 아이콘 | **미해결.** `modules/ui.py:1216` 8종 상시 노출, 미매핑 화면은 7개가 영구 음영. 라벨 없는 아이콘이라 사유가 툴팁뿐이라 §4-3 위반 상태 |
| 6 | 필터 축 라벨 | **미해결.** 근무표 편성의 "부서" 필터가 대분류를 고른다(`schedule_edit.py:300`). 라벨과 값이 다르다 |
| 7 | 담당자 선점 | **미해결.** `review_started_by` / `review_started_at` migration 미작성. `supabase/migrations/`는 001~011(005 결번). `AGENTS.md` 승인 게이트 대상 |
| 8 | §4-1 vs 지표 스트립 9화면 | **미해결.** `erp.metric_strip`은 클릭·필터링이 없는 순수 HTML인데 9화면이 제목 직후에 깐다. 특히 `near_miss_evaluate.py:209`의 "대기 합계"는 §2가 이름 붙여 금지한 타일 그 자체다. 9화면 재설계인지 규칙 완화인지 결정 필요 |
| 9 | `내 업무요청` 유형 | **미해결.** `work_request_my.py:182` 종결 확인 / 보완 요청 2지 판정 + 의견 필수. READ_VIEW로 둘지 WORKLIST로 올릴지 |
| 10 | 타이포 마이그레이션 | **부분 해소.** 제목·설명은 끝났다 — `views/master/style.py`의 `.ms-title`/`.ms-desc`를 **28px/600 · 12px**로 바꿔 22화면 제목이 §1.2를 따른다(실렌더 4화면 × 3폭 계측 확인, 2026-08-18). 같은 작업에서 `views/workspace.py`의 **죽은 사본**(`.ms-title{1.18rem/700}`)을 제거하고 재유입 검사를 걸었다.<br>**남은 것** — 제목 밖 본문·라벨·표는 여전히 §1.2 밖 단계가 돌아간다. 다섯 단계를 **전부** 지키는 화면은 아직 0개다 |
| 11 | 구판 § 번호 dangling | **미해결 — 범위가 docstring만이 아니다.**<br>① 22화면 docstring이 `§0-1`·`§1-A`·`부속서 A-5` 등을 인용<br>② **프로젝트 스킬** — `.claude/skills/screen-design/SKILL.md`가 값의 출처로 `§0/§0.6`(`:26`), `§0.3`(`:33`), `§0.6`(`:34`), `§0.6·§4`(`:45`), `§0.6`(`:73`) 5곳을 인용. **스킬이 존재하지 않는 절을 정본으로 가리킨다**<br>③ **`docs/BACKLOG.md`** — `:53`(§0 매니페스트·MASTER_DETAIL), `:40`(§0.1·§0.2), `:45`(§0.1 매니페스트) 3건. 게다가 이 세 항목은 오늘 결정으로 §7.1에 흡수·대체됐으므로 제거 대상이다 |
| 11-b | 스킬 자산 결손 | **미해결.** `screen-design/references/`가 비어 있다 — 부록이 Streamlit 위젯 매핑표의 소유권을 이 스킬에 위임했는데 실물이 없다. `duty-app`은 `SKILL.md` 없는 빈 디렉터리다 |
| 12 | `SCREEN_DECISION` 미선언 | **미해결.** 22화면 전부 미선언. §5 기계 검증도 미구현 |
| 13 | 006/007 적용 상태 | **부분 해소.** 문서 규율상 `docs/database.md`(2026-08-07 live 확인, 날짜 기록)와 `WORKLOG.md:284`(적용 정황 구체적)가 우선이고, `BACKLOG.md:42`는 **날짜 없이 "미적용"인 stale 항목**이다 — BACKLOG 자체 규칙("해결되면 제거하고 WORKLOG에 기록")도 어겼다. → `BACKLOG.md:42` 제거 대상.<br>**단 이는 테스트 프로젝트 상태다.** 운영 프로젝트 적용 여부는 여전히 어느 문서도 말하지 않는다. §7.4의 006-only 배포 결함이 성립할 여지가 남아 있다 |
| 14 | `requirements.md`에 아차사고 계약 부재 | **미해결 — 근본 결손.** 등급 산출·상태 전이·담당자 선점·경과일 중 **어느 것도 `docs/requirements.md`에 없다**(전수 확인). 오늘 "확정"이라 부른 항목들이 기능 요구사항 수준에서는 한 줄도 없는 상태다. DESIGN.md는 시각 기준이지 기능 계약의 정본이 아니므로, 아차사고 절 신설이 필요하다 |
| 15 | `requirements.md:27` 개인 대시보드 | **미해결.** USER 권한에만 "개인 대시보드"가 별도 항목으로 적혀 있어 §2 DASHBOARD 역할 통합과 어휘가 어긋난다. ADMIN/MANAGER 목록에는 대시보드 항목 자체가 없다. 세 역할 공통 항목으로 재서술 필요 |
| 16 | `supabase-setup.md` stale | **해소(2026-08-18).** 006~011을 목록에 추가하고 **005가 결번**임을 명시했다(누락으로 오해되지 않게). 006·007의 런타임 probe 판정과 §7.4 결함도 주의사항에 연결했다 |
| 17 | `HANDOFF_PROMPT.md` | **해소(2026-08-18).** 종전 판은 폐기된 규칙을 재지시했다 — 좌우분할 금지(§2와 정반대), 상단 지표 타일(§4-1 위반), `14.5px`(부록 폐기), 무효 절 인용(`§1-A`·`§8`). 전면 재작성해 **값을 복제하지 않고 절 번호만 가리키도록** 바꿨다. 종전 실패 원인을 문서 서두와 §5에 명시해 재발을 막았다 |
| 18 | `§0` 오인용 | **해소(2026-08-18).** `CLAUDE.md:64`를 "§0(결정 문장 선언)과 §2(화면 유형 골격)"로, `README.md`를 §2로 정정 |
| 19 | `README.md` 인벤토리 결손 | **해소(2026-08-18).** 개요·현재 기능·권한별 화면표·주요 구조에 아차사고 7파일을 넣고, 업무요청·숙소를 **프로토타입(미라우팅)**으로 표시했다. 테스트 목록에 `test_screen_scaffold.py`·`test_near_miss_ui_gates.py`를 추가하고 "고정 목록이 아니라 대표 예시"임을 명시해 `CLAUDE.md`의 복제 금지 원칙과 맞췄다 |
| 20 | 서체 고지 | **해소(2026-08-18).** `THIRD_PARTY_NOTICES.md`에 IBM Plex 항목을 추가했다. 조사 결과 **동봉이 아니라 CDN 로드**라 OFL 재배포 고지 요건은 발생하지 않는다 — 사용 사실 기록으로 정리했다 |
| 23 | **서체가 CDN 의존** | **미해결 — §1.2의 실효성 문제.** `modules/ui.py:37`이 IBM Plex를 Google Fonts CDN(`fonts.googleapis.com`)에서 `@import`한다. **외부망이 차단된 공장 내부망에서는 폰트를 못 받고 맑은고딕 등으로 폴백**한다.<br>§1.2가 서체를 정본으로 정했지만 **환경에 따라 지켜지지 않는다.** 타이포 스케일(28·20·16·14·12)은 폰트 metrics에 의존하므로 폴백 시 줄바꿈·폭이 달라진다. 사내 배포 환경의 외부망 정책 확인이 먼저이고, 차단이면 `fonts/`에 서브셋 TTF를 동봉하고 `@font-face`로 바꿔야 한다 |
| 21 | 신규 모듈 4종 | **미해결 — 곧 들어온다.** 시험성적서 요청 · 시험·계측기 주기관리 · 숙소 예약 · 업무요청서.<br>**§2가 3개는 그대로 담는다** — 시험성적서·업무요청·숙소는 전부 `FORM_ENTRY`(신청) + `WORKLIST`(처리) 쌍이고, 뒤 둘은 이미 프로토타입이 그 구조다.<br>**계측기 주기관리만 다르다** — "주기 도래 임박"이 핵심이라 요청·처리 흐름이 아니다. `WORKLIST` 변형인지 새 유형인지 판정이 필요하다. §2가 새 모듈에서 검증되는 첫 사례다 |
| 22 | 사이드바 그룹 8개 | **미해결.** 현재 `홈·근무표·아차사고 관리·기준정보` 4개에 신규 4개가 붙는다. 평평하게 8개를 둘지 업무 영역(안전·품질·총무)으로 묶을지 결정이 필요하다. `modules/nav.py`가 데이터 기반이라 구조 변경 자체는 쉽다 |

### 7.3 lee-mode 정합

`/lee-mode`로 이 문서를 구현할 때 글로벌 `~/.orca/lee-mode/PLAYBOOK.md`·프로젝트 `.orca/PLAYBOOK.md`와 부딪히는 지점이다. 스킬 rename은 두 문서가 이름을 직접 쓰지 않고 "프로젝트 ERP UI 스킬"로 지칭해 **깨지지 않았다**.

| # | 지점 | 정합 |
|---|---|---|
| 1 | **migration은 Codex 교차검증 2단** | 글로벌 §5가 `schema/migration`·`인증·권한 의미 변경`을 Codex 필수 구간으로 두고 `DESIGN_DECISION`+`FINAL_INTEGRATED` 2단을 요구한다. §7.2의 `frequency`/`severity`·`review_started_by/at` 두 migration과 §7.2-7 USER 범위 변경이 **전부 이 구간**이다. `AGENTS.md` 승인 게이트 **위에** 이 2단이 더 붙는다 |
| 2 | **UI_CENTRIC은 시안 3개가 기본** | 글로벌 §7이 `현재 실화면 확인 → 시안 3개 → 사용자 선택 → 같은 Owner 구현`을 UI_CENTRIC 기본 흐름으로 둔다. 단 "사용자가 정확한 결과를 이미 지정한 경우"와 소규모 2안은 허용한다. **이 문서에 이미 확정된 골격(§2)은 그 예외에 해당**하므로 이관 시 시안을 다시 만들지 않는다 |
| 3 | **목업 기본형이 다르다** | 프로젝트 §목업이 "실제 Streamlit sample/static 프로토타입을 **기본**으로 하되 이미지·정적 목업도 허용"이라고 정한다. 지금까지의 설계 목업은 전부 정적 HTML이라 **기본형을 따르지 않았다.** 구현 단계에서는 실렌더로 전환하고, §5 검증(대비율·1366×768 겹침·행 높이 편차)을 실행한다 — **아직 한 번도 측정하지 않았다** |
| 4 | 담당 경계 | 글로벌 §2가 `ui-feature`에 **데이터 계약 변경 금지**를 건다. §7.2의 migration 2건·제안등급 폐기·USER 범위 변경은 `data-contract` Owner 소관이고, 화면 골격 이관만 `ui-feature` 소관이다. **한 Owner가 다 하지 않는다** |
| 5 | 어휘 | 두 PLAYBOOK이 스킬을 "프로젝트 ERP UI 스킬"로 지칭한다. 이름이 `screen-design`이 됐으므로 지칭도 맞추는 것이 좋다(깨지지는 않는다) |

### 7.4 발견된 결함 (P1)

**평가 관리 보완요청 버튼이 활성인데 클릭하면 실패한다.**
`views/near_miss_evaluate.py:171`의 활성 게이트는 **006 프로브**(`near_miss_schema_probe`)를 쓰는데, 실행부 `modules/db.py:2566` `request_near_miss_revision`은 **007 프로브**로 fail-closed 차단한다. 006만 적용된 배포에서 버튼이 눌리고 실패한다. §3.2("불가능한 전이는 그리지 않는다") 위반이자 실사용 버그다.

---

## 부록 — 이전 명세에서 폐기한 것

| 폐기 | 이유 |
|---|---|
| 화면별 픽셀값(`flex:2 1 420px` 등) | 값이 코드에 복제되며 드리프트했고, 정렬 실패의 직접 원인이었다 |
| "좌우 분할 마스터-디테일 금지" | 이유 없는 규칙. 승인 큐의 업계 표준 구조를 막았다 |
| "본문 ≥14.5px" 일괄 적용 | 매트릭스와 폼에 같은 밀도를 강제해 한쪽이 망가졌다. §1.4 밀도로 대체 |
| 부속서 정정 방식 | 본문과 충돌하는 정정이 8개까지 쌓였다. 정정은 본문을 고쳐서 반영한다 |
| Streamlit 위젯 매핑 표 | "이럴 땐 이 위젯"의 일반 대응표는 도구 사용법이지 설계 기준이 아니다. 프로젝트 스킬(`screen-design`)이 소유한다.<br>**단 §7.1의 "구현 수단"은 폐기 대상이 아니다** — 일반 대응표가 아니라 이번 이관에 한정된 **결정 기록**(어떤 수단을 왜 골랐는지)이며 이관이 끝나면 §7.1과 함께 사라진다 |
