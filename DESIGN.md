# DESIGN.md — SAP ByD 스타일 사내 업무 시스템 디자인 가이드

이 문서는 `생산 근무표 관리` 시스템의 화면 디자인 기준을 정의한다.

이 프로그램은 일반 웹사이트, Streamlit 데모 앱, AI 자동 생성 화면처럼 보이면 안 된다.  
회사의 내부 업무 시스템, ERP, 그룹웨어, SAP Business ByDesign 계열 화면처럼 보여야 한다.

단, SAP 로고, SAP 명칭, SAP 고유 아이콘, SAP 저작물을 복제하지 않는다.  
목표는 SAP ByD와 유사한 **업무 시스템 구조와 사용감**을 구현하는 것이다.

---

## 1. 디자인 핵심 원칙

### 1.1 목표

사용자는 이 프로그램을 보고 다음과 같이 느껴야 한다.

- 전문 개발자가 만든 사내 업무 시스템 같다.
- 회사에서 실제 운영해도 될 것 같다.
- 화면 구조가 ERP/그룹웨어/SAP ByD 계열과 유사하다.
- AI가 급하게 만든 데모 화면처럼 보이지 않는다.

### 1.2 절대 금지

다음 방식은 사용하지 않는다.

- Streamlit 기본 사이드바 메뉴를 그대로 사용하는 것
- 단순 라디오 버튼 메뉴
- 버튼과 표만 나열한 테스트 화면
- 빈 화면에 안내 문구만 크게 보여주는 화면
- 과한 이모지, 장식 문구, 그라데이션, 큰 배너
- `Phase`, `MVP`, `Demo`, `데모`, `준비 중`, `구현 예정`, `오픈 후 이용 가능`, `샘플 앱`, `테스트 화면`, `AI` 같은 개발자용 표현
- 병원, 간호, 간호팀 등 회사 업무와 맞지 않는 용어

사용자 화면에는 개발 진행 상태를 노출하지 않는다.  
개발 단계 정보는 `README.md`, `docs/development-plan.md`에만 기록한다.

---

## 2. App Shell 핵심 구조

관리자/매니저용 PC 화면은 아래 2단 구조를 따른다.

1. 좌측 단일 다크 사이드바 (브랜드 헤더 + 그룹 메뉴 + 하단 사용자 카드)
2. 우측 메인 콘텐츠 영역 (브레드크럼 → 페이지 제목 → 조회 조건 → 요약 카드 → 그리드)

Streamlit의 기본 `st.sidebar` 라디오 메뉴에 의존하지 않는다.  
좌측 메뉴는 커스텀 App Shell(`modules/ui.py` `app_shell`)로 구현한다.

```text
┌─────────────────────┬─────────────────────────────────────────────┐
│ [W] 생산 근무표    ▤ │ 브레드크럼(그룹명)                             │
│     WORKFORCE       │ 페이지 제목                                   │
│                     │ 짧은 업무 설명                                 │
│ ⌂ 대시보드           │ ┌─ 조회 조건 영역 ────────────────────────┐   │
│                     │ │ 필터 조건                        [조회] │   │
│ 근무표               │ └─────────────────────────────────────────┘  │
│  · 근무표 편성       │ ┌─ 요약 카드 영역 ────────────────────────┐   │
│  · 월간 근무표       │ │ 카드1     카드2     카드3     카드4     │   │
│  · 내 근무표         │ └─────────────────────────────────────────┘  │
│ 기준정보             │ ┌─ 데이터 그리드 영역 ────────────────────┐   │
│  · 사용자 관리       │ │                                         │   │
│  · 부서/조/근무형태   │ └─────────────────────────────────────────┘  │
│                     │                       [행 추가] [삭제] [저장] │
│ (관) 관리자      ⎋  │                                              │
│      ADMIN          │                                              │
└─────────────────────┴─────────────────────────────────────────────┘
```

- 사이드바 숨김: 사이드바 헤더 우측 ▤ 버튼. 숨김 상태에서는 본문 좌상단(브레드크럼 왼쪽)에
  같은 ▤ 버튼이 나타나 다시 연다. 상태는 `st.session_state.sb_hidden` 으로 유지한다.
- 기존 Streamlit «/» 접기 토글은 사용하지 않는다 (CSS 로 숨김).

---

## 3. 좌측 사이드바 (단일 다크)

`modules/ui.py`의 `--sb-*` CSS 디자인 토큰과 동일하게 유지한다.  
"진한 채움"이 아니라 "다크 차콜 + 골드 액센트 + 일정한 리듬"으로 표현한다.

### 3.1 디자인 토큰

| 토큰 | 값 | 용도 |
|---|---|---|
| `--sb-w` | `232px` | 사이드바 폭 |
| `--sb-bg` | `#1B1B1D` | 사이드바 배경 (다크 차콜) |
| `--sb-item` | `#C9C7C0` | 메뉴 기본 글자 |
| `--sb-item-dim` | `#8A8880` | 그룹 라벨·보조 글자 |
| `--sb-active-bg` | `#2B2B2E` | 활성 항목 배경 |
| `--sb-hover-bg` | `#242427` | hover 배경 |
| `--gold` | `#C9A26B` | 액센트 (로고·활성 도트·사번 표기) |
| `--content-bg` | `#F1EEE9` | 본문 배경 (크림, ADMIN/MANAGER 전용) |
| `--card` | `#FFFFFF` | 카드 배경 |
| `--line` | `#E7E3DB` | 경계선 |

### 3.2 사이드바 구성 (위→아래)

1. 브랜드 헤더 (패딩 16px)
   - 로고 마크: 34px 라운드 9px, 골드 그라데이션(`#D5B27C`→`#B8905A`), 글자 "W" 15px/800 다크
   - 앱명 2줄: "생산 근무표" 14px/700 `#F0EEE9` + "WORKFORCE" 9.5px letter-spacing .18em `#8A8880`
   - 우측: 사이드바 숨김 ▤ 버튼 (고스트, 아이콘 17px)
2. 단독 메뉴 항목 (하위 1개 그룹, 예: 홈→대시보드)
   - 높이 38px, 라운드 8px, 아이콘 16px + 글자 13.5px
3. 그룹 라벨 + 하위 항목 (근무표, 기준정보)
   - 그룹 라벨: 정적 텍스트(클릭 대상 아님), 11px/600 `--sb-item-dim`, letter-spacing .05em, 위 16px 아래 6px
   - 하위 항목: 높이 36px, 라운드 8px, 좌측 4px 도트 불릿(`#5E5C55`, 활성 시 `--gold`), 글자 13.5px
4. 하단 사용자 카드 (사이드바 맨 아래 고정)
   - 배경 `#242427`, 라운드 10px, 마진 10~12px, 패딩 9px 10px
   - 28px 골드 원형 아바타(이름 첫 글자) + 사용자명 12.5px/600 흰색
     + 사번 10px `--gold` letter-spacing .08em + 우측 로그아웃 아이콘 버튼

### 3.3 상태 표현

- 기본: 글자 `--sb-item`, 배경 없음
- hover: 배경 `--sb-hover-bg`, 글자 흰색
- 활성: 배경 `--sb-active-bg` + 흰 글자/600 (+ 하위 항목은 골드 도트)
- 전환 효과: background-color 130ms ease 만, 그림자·애니메이션 금지

---

## 4. 사이드바 메뉴 구성 규칙

메뉴는 `modules/nav.py`의 `MENU_GROUPS` 데이터로만 정의한다.

- 하위 메뉴가 1개인 그룹(홈)은 단독 항목으로 표시한다.
- 하위 메뉴가 여러 개인 그룹(근무표, 기준정보)은 그룹 라벨 + 하위 항목으로 표시한다.
- 모든 메뉴 텍스트는 좌측 정렬. 중앙 정렬 금지.
- 권한별 표시는 §6.2 를 따른다 (MANAGER 는 기준정보 미노출).
- 향후 업무 모듈은 MENU_GROUPS 에 그룹만 추가하면 App Shell 수정 없이 노출된다.

### 4.1 초기 메뉴 구조

```text
홈
- 대시보드            (단독 항목으로 표시)

근무표
- 근무표 편성
- 월간 근무표
- 내 근무표

기준정보
- 사용자 관리
- 부서 관리
- 조 관리
- 근무형태 관리
```

---

## 5. 우측 메인 콘텐츠 영역

메인 콘텐츠는 업무 화면답게 구성한다.

### 5.1 기본 기준

- 배경: `#F7F8FA`
- 콘텐츠 카드 배경: `#FFFFFF`
- 본문 좌우 여백: 24~32px
- 카드 내부 여백: 16~20px
- 카드 간 간격: 16~24px
- 데이터 그리드는 화면 폭을 최대한 사용
- 빈 화면처럼 보이지 않게 구성한다

### 5.2 메인 콘텐츠 기본 순서

모든 관리자/매니저 화면은 아래 순서를 따른다.

```text
페이지 제목
짧은 업무 설명
조회 조건 카드
요약 카드 영역
데이터 그리드 영역
하단 액션 버튼 영역
```

---

## 6. 메뉴 확장 설계 원칙

이 시스템은 현재 `생산 근무표 관리` 중심이지만, 향후 다른 업무 프로그램을 추가할 수 있어야 한다.

### 6.1 메뉴는 하드코딩하지 않는다

메뉴는 가능한 한 설정 데이터 또는 공통 구조로 관리한다.

권장 메뉴 데이터 구조 예시:

```python
MENU_GROUPS = [
    {
        "id": "home",
        "label": "홈",
        "icon": "home",
        "roles": ["USER", "MANAGER", "ADMIN"],
        "children": [
            {"id": "dashboard", "label": "대시보드", "page": "dashboard"}
        ],
    },
    {
        "id": "schedule",
        "label": "근무표 관리",
        "icon": "calendar",
        "roles": ["MANAGER", "ADMIN"],
        "children": [
            {"id": "schedule_edit", "label": "근무표 등록/수정", "page": "schedule_edit"},
            {"id": "schedule_view", "label": "전체 근무표 조회", "page": "schedule_view"},
        ],
    },
    {
        "id": "master",
        "label": "기준정보",
        "icon": "settings",
        "roles": ["ADMIN"],
        "children": [
            {"id": "users", "label": "사용자 관리", "page": "users"},
            {"id": "departments", "label": "부서 관리", "page": "departments"},
            {"id": "teams", "label": "조 관리", "page": "teams"},
            {"id": "work_types", "label": "근무형태 관리", "page": "work_types"},
        ],
    },
    {
        "id": "my",
        "label": "내 정보",
        "icon": "user",
        "roles": ["USER", "MANAGER", "ADMIN"],
        "children": [
            {"id": "my_schedule", "label": "내 근무표", "page": "my_schedule"}
        ],
    },
]
```

향후 새 업무를 추가할 때는 기존 App Shell을 바꾸지 않고 메뉴 설정만 추가한다.

예:

```python
{
    "id": "raw_material",
    "label": "원료 관리",
    "icon": "package",
    "roles": ["MANAGER", "ADMIN"],
    "children": [
        {"id": "raw_inout", "label": "원료 수불 관리", "page": "raw_inout"},
        {"id": "raw_stock", "label": "원료 재고 조회", "page": "raw_stock"},
    ],
}
```

### 6.2 권한별 메뉴 표시

권한에 따라 메뉴 표시를 제한한다.

| 권한 | 표시 메뉴 |
|---|---|
| USER | 내 정보, 내 근무표 |
| MANAGER | 근무표 관리, 내 정보 |
| ADMIN | 전체 메뉴 |

권한이 없는 메뉴는 숨긴다.  
비활성화된 메뉴를 회색으로 보여주기보다 아예 보이지 않게 한다.

---

## 7. 색상 기준

### 7.1 기본 팔레트

| 용도 | 색상 |
|---|---|
| 사이드바 배경 | `#1B1B1D` |
| 사이드바 활성 배경 | `#2B2B2E` |
| 골드 액센트 | `#C9A26B` |
| 메인 배경 | `#F1EEE9` (ADMIN/MANAGER) · `#F7F8FA` (USER·로그인) |
| 카드 배경 | `#FFFFFF` |
| Primary 네이비 | `#1E3A6E` |
| 포인트 블루 | `#1E88E5` |
| 본문 텍스트 | `#26282B` |
| 보조 텍스트 | `#6B7280` |
| 구분선 | `#DDE3EA` |
| 성공 | `#2E9E5B` |
| 오류 | `#D93025` |
| 경고 | `#E8862E` |

### 7.2 색상 사용 원칙

- 네이비와 그레이를 중심으로 한다.
- 파란색은 선택 상태, 조회 버튼, 링크 등에 제한적으로 사용한다.
- 빨간색은 오류와 삭제 확인에만 사용한다.
- 화면마다 새로운 색상을 임의로 추가하지 않는다.
- 버튼, 메뉴, 근무코드 색상은 전체 화면에서 일관되게 사용한다.

---

## 8. 타이포그래피

별도 웹폰트를 강제로 사용하지 않는다.  
시스템 기본 산세리프를 사용한다.

권장 폰트 스택:

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Malgun Gothic", "Apple SD Gothic Neo", Arial, sans-serif;
```

### 8.1 텍스트 크기

| 요소 | 크기 | 굵기 |
|---|---:|---:|
| 앱명 | 18~20px | 700 |
| 페이지 제목 | 24~28px | 700 |
| 섹션 제목 | 16~18px | 600 |
| 본문 | 14px | 400 |
| 보조 설명 | 12~13px | 400 |
| 메뉴 텍스트 | 13~14px | 500 |
| 버튼 텍스트 | 13~14px | 600 |

타이포그래피는 크기와 굵기로 위계를 만든다.  
색상만으로 위계를 만들지 않는다.

---

## 9. 본문 상단 (브레드크럼)

관리자/매니저 화면 상단에는 사용자 정보 헤더 대신 브레드크럼만 표시한다.

구성:

```text
브레드크럼(현재 그룹명)
```

기준:

- 높이: 52px
- 브레드크럼: 현재 그룹명, 11px/600, 색 `#9A968C`, letter-spacing .06em
- 사용자명·사번·로그아웃은 사이드바 하단 사용자 카드에 표시한다 (§3.2)
- 사이드바 숨김 상태에서는 브레드크럼 왼쪽에 ▤ 열기 버튼을 표시한다
- 개발 상태 문구는 표시하지 않는다

---

## 10. 업무 화면 공통 구조

모든 관리자/매니저 업무 화면은 아래 구조를 따른다.

```text
페이지 제목
한 줄 설명

조회 조건 카드
요약 카드 영역
데이터 그리드 영역
하단 액션 버튼 영역
```

### 10.1 페이지 제목 영역

- 페이지 제목은 명확한 업무명 사용
- 예: `전체 근무표 조회`, `근무표 등록/수정`, `사용자 관리`
- 설명은 한 줄만 사용
- 설명은 업무 중심 문구로 작성

좋은 예:

```text
부서와 조를 선택하여 월별 근무표를 조회합니다.
```

나쁜 예:

```text
이 화면은 Phase 3에서 구현 예정입니다.
```

---

## 11. 조회 조건 카드

조회 조건 카드 기준:

- 배경: `#FFFFFF`
- 테두리: 1px solid `#DDE3EA`
- 내부 여백: 16~20px
- 라운드: 4~8px
- 필터는 가로 배치
- 필터가 많으면 2줄까지 허용
- 조회 버튼은 우측 끝
- 필터 변경만으로 자동 조회하지 않는다
- 사용자가 [조회]를 눌렀을 때만 조회한다

예시:

```text
┌ 조회 조건 ───────────────────────────────────────────────┐
│ 기준월 [2026-07]   부서 [PET생산부]   조 [A조]   [조회] │
└─────────────────────────────────────────────────────────┘
```

---

## 12. 요약 카드 영역

업무 화면이 비어 보이지 않도록 주요 화면에는 요약 카드를 배치한다.

예시:

```text
등록 인원 18명
근무 데이터 558건
부서 3개
근무형태 10개
```

요약 카드 기준:

- 3~4개 카드 사용
- 카드 배경: `#FFFFFF`
- 테두리: 1px solid `#DDE3EA`
- 숫자는 크고 굵게
- 라벨은 작고 회색
- 과한 아이콘 사용 금지

---

## 13. 데이터 그리드 영역

업무 시스템의 핵심은 데이터 그리드다.

### 13.1 등록 화면

등록 화면은 `st.data_editor` 기반 스프레드 입력을 사용한다.

필수 기능:

- 직접 키인
- 엑셀 복사/붙여넣기
- 행 추가
- 행 삭제
- 저장
- 저장 전 검증

### 13.2 조회 화면

조회 화면은 편집 가능해 보이면 안 된다.

- 조회 전에는 표 영역을 비워두지 않는다.
- `조회 조건을 선택한 후 조회하세요.` 수준의 업무 문구를 표시한다.
- 조회 후에는 가로형 근무표 또는 일반 표를 표시한다.
- 읽기 전용 표는 `data_editor`처럼 보이지 않게 한다.

---

## 14. 버튼 기준

| 버튼 | 위치 | 스타일 |
|---|---|---|
| 조회 | 조회 조건 카드 우측 | 기본/보조 |
| 행 추가 | 그리드 하단 우측 | 보조 |
| 행 삭제 | 그리드 하단 우측 | 보조 또는 위험 |
| 저장 | 그리드 하단 우측 끝 | Primary |
| 로그아웃 | 상단 헤더 우측 | 보조 |

버튼 기준:

- 높이: 36~40px
- Primary 버튼은 화면당 1개만 사용
- 버튼 위치는 화면마다 바꾸지 않는다
- 삭제/초기화는 확인 절차를 둔다

---

## 15. 근무코드 색상

근무코드는 화면 어디서든 같은 색상 체계를 사용한다.

| 코드 | 명칭 | 색상 |
|---|---|---|
| 주 | 주간 | `#1E6FD9` |
| 야 | 야간 | `#7B4FD8` |
| 야-4 | 야간 4시간 | `#9A7BE3` |
| 야-6 | 야간 6시간 | `#5A3DB8` |
| 특주 | 특근(주간) | `#E8862E` |
| 특야 | 특근(야간) | `#B4541B` |
| OFF | 휴무 | `#9AA0A6` |
| 연차 | 연차휴가 | `#2E9E5B` |
| 출산휴가 | 출산휴가 | `#D64596` |
| 자녀결혼 | 경조(자녀결혼) | `#12A5A5` |

조회 화면:

- 셀 배경은 코드 색상의 연한 톤
- 글자는 진한 본문색
- 범례는 표 하단에 표시

모바일 화면:

- 코드 색상 뱃지 사용
- 텍스트는 코드 그대로 표시

---

## 16. 근무표 그리드 디자인

근무표는 실제 엑셀과 유사한 가로형 구조를 사용한다.

```text
사번 | 성명 | 부서 | 조/팀 | 1(화) | 2(수) | 3(목) | ...
```

기준:

- 직원 정보 컬럼은 왼쪽에 고정
- 날짜 컬럼은 오른쪽으로 이어짐
- 날짜 헤더에는 요일 병기
- 토요일은 파란 계열, 일요일은 빨간 계열로 헤더 구분
- 셀 텍스트는 `주`, `야`, `OFF`처럼 코드 그대로 표시
- 아이콘으로 근무코드를 대체하지 않는다
- 표 하단에 근무코드 범례 표시

---

## 17. 모바일 개인 조회 화면

직원용 화면은 별도 앱이 아니라 모바일 웹이다.

모바일 화면은 SAP/ERP 관리자 화면과 다르게 단순해야 한다.

구성:

```text
김OO님의 근무표
[◀] 2026.07 [▶]

오늘 근무
7/9(목)  [야] 야간

이번 달 근무
7/1(화)  [주] 주간
7/2(수)  [주] 주간
7/3(목)  [OFF] 휴무
...

이달 집계
주 10 · 야 6 · OFF 7 · 연차 1
```

기준:

- 모바일에서는 좌측 App Shell을 사용하지 않는다.
- 가로 스크롤 금지
- 오늘 근무 카드가 첫 화면에 보여야 한다.
- 날짜별 리스트는 한 행이 하루를 의미한다.
- 월 이동 버튼은 터치하기 쉽게 만든다.
- 차트보다 텍스트 집계를 우선한다.

---

## 18. Streamlit 구현 주의사항

Streamlit으로 구현하되, Streamlit 기본 화면처럼 보이면 안 된다.

필수 원칙:

- 기본 `st.sidebar` 라디오 메뉴에 의존하지 않는다.
- 좌측 1차 아이콘 바와 2차 메뉴 패널은 커스텀 App Shell로 구현한다.
- `modules/ui.py`, `modules/nav.py` 등 공통 모듈에서 App Shell을 재사용한다.
- Streamlit 기본 Deploy 버튼, 기본 메뉴, 푸터는 사용자 화면에서 보이지 않게 처리한다.
- 과도한 JavaScript 사용은 피한다.
- CSS는 공통 모듈에서 관리한다.
- 화면별로 CSS를 흩뿌리지 않는다.

---

## 19. 금지 문구

사용자 화면에는 아래 표현을 표시하지 않는다.

```text
Phase
MVP
Demo
데모
준비 중
구현 예정
오픈 후 이용 가능
샘플 앱
테스트 화면
AI
```

대신 업무 문구를 사용한다.

좋은 예:

```text
조회 조건을 선택한 후 조회하세요.
등록된 근무표가 없습니다.
저장할 데이터가 없습니다.
권한이 없어 접근할 수 없습니다.
입력 내용을 확인하세요.
```

---

## 20. 완료 기준

디자인 적용이 완료되었다고 판단하려면 다음 조건을 만족해야 한다.

- 좌측 단일 다크 사이드바(브랜드 헤더 + 그룹 메뉴 + 하단 사용자 카드)가 있다.
- Streamlit 기본 사이드바 라디오 메뉴가 보이지 않는다.
- 본문이 빈 화면처럼 보이지 않는다.
- 조회 조건 카드, 요약 카드, 데이터 영역이 배치되어 있다.
- 앱 이름은 `생산 근무표 관리`로 통일되어 있다.
- 병원/간호 관련 용어가 없다.
- 사용자 화면에 개발 단계 문구가 없다.
- 전체 화면이 SAP/ERP 계열 사내 업무 시스템처럼 보인다.