# 현장 운영관리 — 구현 명세 (Claude Code 필독)

이 문서는 "참고"가 아니라 **명세**다. 색만 바꾸고 기존 구조를 유지하는 것은 실패다.
레퍼런스 구현: `아차사고 관리.dc.html` — 각 화면의 정답 골격이 그대로 들어 있으니 **먼저 읽고 구조를 베껴라.**

---

## 0. 절대 금지 (이걸 하면 반려)

1. **행 선택용 체크박스 열** — 목록에서 체크박스로 상세를 여는 방식 금지. 행(또는 칩) 클릭이 곧 선택이다.
2. **빈 우측 패널** ("케이스를 선택하세요 / 왼쪽 큐에서 행을 클릭하면…") — 좌우 분할 마스터-디테일 자체를 쓰지 않는다.
3. **빈 툴바 띠** — 아이콘만 오른쪽에 떠 있는 회색/흰색 가로 띠를 페이지 본문에 넣지 않는다. 아이콘 툴바는 **최상단 헤더 바(52px) 안**에만 존재한다.
4. **KPI를 화면 맨 아래에** 두지 않는다. 지표는 항상 제목 바로 아래 첫 블록.
5. **카드**(흰 배경 + 테두리 + radius + 그림자)로 섹션을 감싸지 않는다. 구획은 헤어라인과 여백으로만.
6. 표 본문 13px 이하, 라벨 11px 이하 금지.
7. 사이드바 메뉴에 폴더 아이콘(📁 모양) 금지. 모듈은 텍스트 + 6px 사각 마크 + 캐럿.
8. 새 색을 만들지 않는다. §2 팔레트 밖의 색 사용 금지.

---

## 1. 화면 골격 (그대로 구현)

### A. 큐 처리형 — 평가 관리, 개선조치 관리
```
제목 / 설명
[지표 스트립]        ← 좌측 2px 보더 + 26px 모노 숫자, 3~6개 가로 배치
──────────────── 헤어라인
[큐 칩 스트립]  "평가 대기 큐 ③"  (0003 제목 ●) (0002 제목 ●) …   [1/3] [‹][›]
                ↑ 선택 칩 = 오렌지 배경, 나머지 = 흰 반투명 + 테두리
──────────────── 헤어라인
[선택된 건 전체폭 상세]
   보고번호(18px 모노) + 상태 배지
   제목 17px/600
   우측: 발생일 / 신고자 / 부서 / 등급  (라벨 10.5px 모노 대문자 + 값 13.5px)
──────────────── 헤어라인
[본문 블록들]  WHAT / TASK / SITE / CAUSE / ACTION
   각 블록 = 좌측 2px 보더 + 모노 오버라인 + 라벨 + 본문 14px/1.7
   긴 항목 flex:2 1 420px, 짧은 항목 flex:1 1 260px  (2열로 자연스럽게 채워짐)
──────────────── 헤어라인
[액션 바]  등급 세그먼트(S A B C D) | 의견 입력(flex:1) | 검토착수 보완요청 반려 [평가 확정]
```
- 우측 빈 공간이 생기면 **디테일을 전체폭으로 늘려** 채운다. 빈 패널을 두지 않는다.
- 개선조치 관리는 액션 바가 `담당자 select | 기한 date | 확인 의견 | 조치 등록 · 확인 완료 · [보고서 종결]`.

### B. 목록형 (건수 적음) — 내 아차사고
행 = 한 줄 요약(보고번호 모노 · 제목 14.5/600 · 날짜 · 원인 · 상태 배지 · 캐럿), **클릭하면 아래로 펼침**.
펼친 영역: 진행 단계 pill(제출→검토중→평가완료→종결) + 본문 블록 + 수정/취소 버튼. 카드 아님, 행 구분은 헤어라인.

### C. 스프레드시트 입력형 — 근무표 편성
```
연도 [2026] 월 [7월] 부서 [PET생산부] 조 [A조]        패턴 채우기  되돌리기  선택 비우기  [저장]
──────────────── 헤어라인
입력 단축키 [1 주][2 야][3 OFF][4 연차]
2026-07 · PET생산부 · A조   인원 12 · 입력 372 · 미입력 0     셀 클릭 = 근무 순환 · 방향키 이동
┌ 흰 배경 + 1px #cfc8bd + radius 8, max-height 62vh, 내부 스크롤 ─────────┐
│ 사번(96, sticky left 0) 성명(82, 96) 부서(104, 178) 조(52, 282) │ 1수 2목 3금 … 31 │
│ 2008082501  황봉준  PET생산부  A조  │ 주 주 OFF OFF 야 …            │
└──────────────────────────────────────────────────────────┘
범례 [주][야][OFF][연차]                                   미저장 변경 0건
```
- 헤더 `sticky top`, 신원 4열 `sticky left`, 셀 높이 30px·min-width 34px.
- 붙여넣기 입력창은 **넣지 않는다**(셀 직접 입력이 요구사항).

### D. 폼형 — 아차사고 등록
`01 · 기본 정보` / `02 · 상황 기술` / `03 · 사진 첨부` 3섹션(모노 오버라인 + 헤어라인),
필드는 flex-wrap(작업명 2 1 240 / 발생일 1 1 150 등), 입력 14.5px,
우측에 필수 8항목 체크리스트 + 진행 바(좁아지면 아래로 내려감), 하단에 임시 저장 / [제안서 제출].

### E. 표형 — 사용자·조직·근무형태 관리
필터 줄(라벨 12.5 + 입력 14.5) → 헤어라인 → 제목 + 건수 pill + 우측 `행 추가` `[저장]` → 표.
표: 헤더 배경 없음·하단 1px `#cfc8bd`, 행 구분 1px `#e6e2da`, 첫/마지막 열 좌우 패딩 0,
본문 14.5~15px, 권한·재직·사용 여부는 pill(12.5/600, padding 3×11).
조직 관리는 3단 드릴을 카드 3개가 아니라 **세로 헤어라인으로 나눈 3열**로.

### F. 분석형 — 아차사고 분석
지표 6개(좌측 보더 스타일) → 분포 4블록. 각 블록 = 제목 + `76px 라벨 / 8px 바 / 52px 수치` 그리드.
색: 등급 오렌지 `#c2410c`, 상태 초록 `#2f6b45`, 원인 황토 `#8a6212`, 추이 파랑 `#2f4d99`.

---

## 2. 색

| 역할 | 값 |
| --- | --- |
| 캔버스 | `#f4f2ee` |
| 헤더 바 / 입력 | `#fbfaf8` / `#ffffff` |
| 텍스트 | `#1c1a17` · 보조 `#4a453d` · 약함 `#8b857c` · 아주 약함 `#a09a90` |
| 헤어라인 | 행 `#e6e2da` · 표 헤더 `#cfc8bd` · 섹션 `#e0dbd2` |
| 액센트 | `#c2410c` (hover `#a3350a`, 텍스트 `#b4451a`, 틴트 `#fdf3ec`) |
| 사이드바 | 배경 `#1a1917` · 구분선 `#2b2925` · 선택 `#2f2c26` · 텍스트 `#a49d92` / 선택 `#fdf3ec` |

상태 배지(배경/글자/테두리): 제출됨 `#fdf3e3`/`#8a6212`/`#f0dfbe` · 검토중 `#eef2fb`/`#2f4d99`/`#dbe3f4` ·
평가완료 `#eef5f0`/`#2f6b45`/`#d8e6dd` · 반려 `#fbeeee`/`#9c3232`/`#f0d9d9` · 종결 `#f2f0ec`/`#5c564d`/`#e4e0d8`

근무형태 셀: 주 `#eef2fb`/`#2f4d99` · 야 `#fbeeee`/`#9c3232` · OFF `#f4f2ee`/`#8b857c` · 연차 `#eef5f0`/`#2f6b45`

## 3. 타이포그래피

- 본문 **IBM Plex Sans KR**, 숫자·ID·코드·날짜·시간 **IBM Plex Mono**.
- 페이지 제목 25/600(-0.025em) · 설명 13.5 · 섹션 14/600 · 표 본문 14.5~15 · 표 헤더 12.5/600 ·
  라벨 13/500 · 입력 14.5 · 캡션 12.5 · 모노 오버라인 10~11 + letter-spacing .12~.14em 대문자.
- 긴 문장 `text-wrap: pretty`.

## 4. 셸(모든 화면 공통)

- **사이드바** 234px / 접힘 66px. 모듈(홈·근무표·아차사고 관리·기준정보)은 **클릭 시 하위 메뉴 접힘**,
  모듈 행에 6px 사각 마크(활성 오렌지) + 캐럿 회전. 메뉴 앞 아이콘은 5px 점만(접힌 레일에서만 2글자 글리프,
  배지는 글리프 우상단 점). 접힘/로그아웃은 16px stroke 아이콘. 접힌 상태에서 브랜드·유저바 세로 스택.
- **헤더 바** 52px: 좌측 `MODULE / SCREEN` 모노 브레드크럼, 우측 연결 pill + 30px 아이콘 버튼 8개
  (정보·언어·추가·새로고침·삭제·인쇄·저장·즐겨찾기), hover `#f1eee8`.
- 본문 패딩 24px, 화면 제목 블록 → 지표 → 큐/필터 → 상세/표 순서.

## 5. 반응형

`flex: N 1 <basis>` + `min-width: 0`만 사용(고정폭 그리드 금지). 목록 320 / 상세 460 / 폼 480 / 사이드 패널 230~300.
900~1000px에서 사이드 패널이 본문 아래로 wrap 되는지 반드시 확인. 표는 `overflow-x:auto` + `min-width`.

## 6. 카피

담백한 실무체, 감탄사·이모지 없음. 숫자에 단위(건·일·%). 상태는 한국어 + 필요 시 영문 병기(제출됨(SUBMITTED)).
빈 상태 문구로 화면을 채우지 말고, 애초에 빈 영역이 생기지 않게 배치한다.

---

## 7. Streamlit 구현 매핑

| 하려는 것 | 쓰지 말 것 | 쓸 것 |
| --- | --- | --- |
| 큐에서 건 선택 | `st.dataframe` 체크박스 / `st.data_editor` 선택열 | 칩 버튼(`st.button` 반복 + `st.session_state.sel`) 또는 HTML 칩 스트립 |
| 상세 표시 | 좌우 `st.columns([1,1])` 분할 | 선택 후 **전체폭** 블록 렌더 |
| 본문 블록 | `st.metric`, `st.info`, `st.expander` 남용 | `st.markdown`(unsafe_allow_html) 로 오버라인+보더 블록 |
| 지표 | 하단 `st.columns` 카드 | 상단 좌측 보더 스트립(HTML) |
| 편성표 | `st.data_editor` 기본 스타일 | 커스텀 HTML 테이블 + sticky, 또는 data_editor에 CSS 주입 |
| 표 | 기본 `st.dataframe` 격자 | 헤어라인 스타일 주입한 HTML 테이블 |

Streamlit 기본 크롬 제거: 상단 여백/툴바/푸터 숨기고 `.block-container { padding: 24px }`,
`div[data-testid="stVerticalBlock"]` 안의 흰 카드 배경 제거.

## 8. 수용 기준 (구현 후 스스로 체크)

- [ ] 체크박스 열이 하나도 없다.
- [ ] "선택하세요" 같은 빈 안내 패널이 없다.
- [ ] 본문에 빈 아이콘 띠가 없다(아이콘은 상단 헤더에만).
- [ ] 지표가 제목 바로 아래에 있다.
- [ ] 흰 카드로 감싼 섹션이 없다(테두리+그림자 0).
- [ ] 표 본문 ≥14.5px, 헤더 ≥12.5px.
- [ ] 선택된 항목은 오렌지 칩/오렌지 좌측 보더로 구분된다.
- [ ] 1200px에서 화면 우측에 큰 빈 영역이 없다. 900px에서 겹침·잘림이 없다.
- [ ] 근무표 편성에 사번·성명·부서·조 4열이 sticky로 고정되고, 붙여넣기 입력창이 없다.
- [ ] 팔레트 외 색, 새 폰트가 없다.

---

## 부속서 A — 구현 정합 정정 (2026-07-30, Codex 검수 GO-WITH-CHANGES 반영)

명세 내부 충돌·기능 계약 충돌을 구현 과정에서 다음과 같이 정정한다. 본문과 이 부속서가 다르면 부속서가 우선한다.

1. **체크박스(§0-1·§8-1 범위 축소)**: 금지 대상은 "목록/큐에서 **상세를 여는** 행 선택 체크박스"다. EDIT/MATRIX 편집 그리드의 **일괄 삭제 대상 선택** 체크박스는 허용한다(requirements §6 "삭제는 선택된 행이 있을 때만" 계약 유지).
2. **접근 가능한 약한 텍스트(§2 추가)**: 작은 의미 텍스트(라벨·오버라인·메타·캡션)는 `#6b665d`(캔버스 위 5.1:1) 이상을 쓴다. `#8b857c`는 비활성 컨트롤·3:1 이상 비텍스트 아이콘 전용, `#a09a90`은 순수 장식 전용으로 제한한다.
3. **표 글자 우선순위(§1-C vs §8-6)**: 수용 기준 §8-6(본문 ≥14.5px)이 목업 픽셀 값보다 우선한다. 편성표 셀·신원 열도 14.5px(행 30px·sticky 유지).
4. **네이티브 위젯 동등 대응(§7 추가)**: `st.button` 행에서 혼합 서식(제목만 600, 인라인 pill)이 불가한 경우, 상태 텍스트+좌측 상태 색바를 동등 대응으로 허용한다. HTML 투명 오버레이는 권고하지 않는다.
5. **헤더 아이콘(§4 확정 — 2026-07-31 사용자 결정으로 재정정)**: 표준 아이콘 8종(정보·언어·추가·새로고침·삭제·인쇄·저장·즐겨찾기)을 **전 화면 상시 노출**하고, 해당 화면에서 사용할 수 없는 아이콘은 **음영(disabled) 처리**한다(전 화면 아이콘 세트 통일 — 2026-07-27 결정 계승). 단 저장·삭제의 실제 실행은 범위 소유 섹션의 버튼이 담당하며, 헤더의 저장·삭제 아이콘은 단일 범위 화면에서만 활성화할 수 있고 다범위 화면(조직 등)에서는 음영으로 둔다.
6. **고정폭 예외(§5 정정)**: "고정폭 그리드 금지"는 일반 콘텐츠 분할에 한정한다. 셸 폭(234/66px)·MATRIX sticky 신원 열·34px 일자 열·차트 76/8/52 그리드는 예외다.
7. **붙여넣기(§1-C 보충)**: 제거 대상은 붙여넣기 **입력창(UI)**이며, Excel Ctrl+V 붙여넣기 capability는 유지한다(2026-07-30 사용자 결정).
8. **계약 필수 상태 안내(§8-2 예외)**: 조직 관리 하위 시트의 "상위를 먼저 선택하세요" 잠금 안내는 드릴다운 계층 계약(requirements §6)의 필수 상태 표시로, 금지 대상인 "빈 우측 패널"이 아니다. 편집 그리드의 frame(1px 외곽+radius)도 §1-C 스프레드시트 컨테이너와 같은 허용 예외다.
