# WorkOps 화면 설계 정본 — 목업 실측 기록

**정본 순서. ① 목업 `docs/mockup/WorkOps 최종 목업.dc.html` ② 이 문서 ③ 코드. 어긋나면 위가 이긴다.**

이 문서의 px·색은 전부 그 목업에서 뽑은 값이다(2026-08-21 실측 · 1,445행 · 템플릿 12–908행 ·
상태 909–1443행). 목업이 말하지 않는 값은 지어내지 않고 **§12 미측정**에 적는다.
목업과 **의도적으로** 다르게 정한 곳은 **§11**에 전부 모아 뒀다 — 그 목록에 없는 어긋남은 버그다.

값 표기 규칙: 표의 값이 현재 기준이고, **괄호 안이 목업 원본값**이다. 괄호가 없으면 목업 그대로다.

> **기능 계약은 이 문서가 소유하지 않는다.** 등급 산출·상태 전이·경과일 규칙 같은 것은
> 시각 기준이 아니다. 구판 §7.2에 쌓여 있던 그 결정들은 **§10**에 이관 대기 상태로 보존했고,
> 정본은 `docs/requirements.md`다. 이 문서는 **보이는 것**만 규정한다.

---

## 0. 화면을 만들기 전에 쓰는 한 문장

새 화면·구조 변경 화면은 코드 상단에 **이 화면이 내리는 결정**을 한 문장으로 적는다.

```python
SCREEN_ARCHETYPE = "EDIT_GRID"
SCREEN_DECISION = "이 사람의 소속·권한·재직 상태를 어떻게 고칠 것인가"
SCREEN_EVIDENCE = ("현재 값", "부서 트리", "권한 그룹", "최종 비밀번호 변경일")
```

레이아웃은 이 문장에서 나온다. 결정에 필요한 근거가 결정 컨트롤보다 **앞에** 오고,
근거가 아닌 것은 뒤로 간다.

> **왜** — 종전 명세는 블록 순서와 픽셀값만 규정하고 화면의 목적을 적지 않았다. 그래서 구현은
> 명세를 지켰는데도 "왜 이렇게 생겼는지" 설명할 수 없는 화면이 나왔다.

§8의 기계 검증이 아직 이 문장을 세지 않는다. 검사가 붙기 전까지 §0은 규범이지 집행 사항이 아니다(§13).

---

## 1. 왜 이 문법인가

국내 제조 현장의 MES·ERP 문법 그대로다. 상단 GNB, 주황 리본, 좌측 트리 메뉴, 조회조건 줄,
장식 없는 고밀도 표. 사용자가 이미 매일 보는 화면이라 배울 것이 없다.

세 가지가 이 문법의 뼈대다.

1. **페이지는 스크롤하지 않는다.** 셸(GNB·리본·사이드바)은 고정이고 본문만 스크롤한다.
2. **표가 주인공이다.** 카드·그림자·큰 라운딩이 없다. 구획은 `■ 제목`과 여백만으로 나눈다.
3. **한 화면에 결정 하나.** 그 결정에 쓰는 버튼은 구획 제목 줄 오른쪽에 모인다(§4.9).

---

## 2. 토큰 — 전역, 예외 없음

### 2.1 색

액센트 하나(`#b8460d` 계열)와 따뜻한 무채색 계단, 그리고 의미색 소수다. **새 색을 만들지 않는다.**

#### 액센트

| 토큰 | 값 | 쓰임 (실측) |
|---|---|---|
| `accent` | `#b8460d` | 리본 배경 · 주버튼 · 로고 뱃지 · 선택 표시(`inset 3px 0 0`) · 필수 `*` · 링크 · 오늘 테두리 |
| `accent-hover` | `#963a0a` | 주버튼 hover · 리본 좌측 구분선 · 링크 hover |
| `accent-tint` | `#fdf0e8` | 선택된 메뉴·선택된 행 배경 · 보조 액센트 버튼 배경 · 활성 칩 |
| `accent-tint-2` | `#fdf6f1` | **필수 입력 칸 배경**(국내 ERP 살구색 관례) · 행 hover · 표 합계 행 |
| `accent-line` | `#e6a684` | 리본 검색 박스 테두리 |
| `accent-weak` | `#d18a5e` · `#c07a51` | GNB 탭의 `⧉` · `✕` 글리프 |
| `accent-hot` | `#f6dcca` / `#8f350a` | GNB 탭 `✕` hover 배경 / 글자 |
| `resize-hot` | `#d9702a` | 사이드바 폭 조절 핸들 hover |

#### 잉크 (읽는 텍스트)

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `ink-strong` | `#1c1a17` | 로고 워드마크 · 기간 이동기 값 |
| `ink` | `#222` | 본문 기본 · 표 헤더 글자 · 표 셀 값 |
| `ink-name` | `#2e2a26` | 사람 이름(GNB · 사이드바 하단) |
| `ink-label` | `#333` | **라벨 · 구획 제목 · 보조 버튼 글자** |
| `ink-2` | `#444` | 칩 글자 · 범례 글자 |
| `ink-3` | `#4a463f` | 사이드바 비선택 항목 · 리본 검색 글자 |
| `ink-4` | `#555` | 표 보조 열(부서 · 이메일 · 조) |
| `ink-5` | `#666` | 구획 부제 · 요약 줄 |
| `ink-muted` | `#8a857e` | 캡션 · 메타 · 폼 절 머리 · 사이드바 하단 부서 |
| `ink-faint` | `#888` | 구획 제목 안 괄호 보조 · 표 비고 열 · 결측 `—` |
| `ink-placeholder` | `#b3ada6` | 빈 입력 안내 글자 · 값 `0`의 흐림 |
| `ink-glyph` | `#8a8680` | **비텍스트 전용** — `▼` `⌕` `▦` · No 열 숫자 |
| `ink-outmonth` | `#c3beb6` (목업 `#c3beb6` / `#c3beb8` 두 값) | 전월·익월 날짜 — §11-3 |

#### 헤어라인 — 네 종류가 표를 그린다

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `line-outer` | `#b5b1aa` | 표·패널 **바깥 테두리** · 헤더 행 밑선 · 격자 고정열 우측 경계 |
| `line-head` | `#cfccc6` | 헤더 행 **열 세로선** · FORM 라벨 셀 우측선 |
| `line-row` | `#e6e4df` | 본문 행 **밑선** · FORM 행 밑선 |
| `line-col` | `#ecebe6` | 본문 **열 세로선** |

그 밖의 구분선 셋. 표 밖에서만 쓴다.

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `line-panel` | `#e3e1dc` | 조회조건 줄 밑선 · 범례 상자 테두리 · 세로 구분자 |
| `line-chrome` | `#d9d5ce` | GNB 밑선 · 모달 내부 세로선 · 프로필 버튼 테두리 |
| `line-soft` | `#ddd8d0` | 사이드바 하단 구분선 · 기간 이동기 버튼 구분선 |

#### 면

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `surface` | `#fff` | 본문 · 표 본문 · 입력 칸 |
| `surface-sub` | `#faf9f6` | 사이드바 배경 · 폼 하단 액션 바 · 범례 상자 |
| `surface-hover` | `#f7f5f1` | 보조 버튼 hover · 요약 패널 라벨 셀 |
| `surface-menu` | `#f0ede7` | 사이드바 항목 hover · 펼친 그룹 행 |
| `head-bg` | `#eceae5` | **표 헤더 배경 · FORM 라벨 셀 배경 · 달력 요일 머리 · 패널 머리** |
| `step-bg` / `step-hover` | `#f2efe9` / `#e8e4dc` | 기간 이동기 `‹` `›` 버튼 |
| `field-ro` | `#f2f1ee` | 읽기 전용 입력 칸(사번 등) |
| `avatar-bg` | `#efece5` (GNB) · `#e5e1d8` (사이드바) | 이니셜 동그라미 |
| `booked` | `#f4f2ee` | 달력의 이미 예약된 날 |
| `outmonth-bg` | `#fafaf8` | 전월·익월 칸 |
| `weekend-bg` | `#fbfaf7` | 달력 본문의 토·일 칸 (격자 헤더는 `#f3efe9`) |
| `row-out` | `#faf7f5` | 퇴직자 행 |

#### 의미색 — 낱말에 색만 준다

| 의미 | 글자 | 칩 테두리 | 쓰임 |
|---|---|---|---|
| 주간 · 토요일 · 정보 | `#17457f` | `#9dbae2` | 근무형태 `주간` · 요일 `토` · 표 헤더 `주간` 열 |
| 야간 · 일요일 · 부정 | `#a3282a` | `#e2a7a4` | 근무형태 `야간` · 요일 `일` · `반려` · `퇴직` · 퇴사일 · `긴급` |
| 완료 · 정상 | `#1c6b41` | `#9dcdae` | `연차` · `재직` · `평가완료` · `완료` · 필수 충족 |
| 진행 · 주의 | `#b8460d` | — | `평가중` · `처리중` — 액센트와 같은 값이다 |
| 중립 · 미착수 | `#555` | — | `제출됨` · `접수` |
| 특근 | `#8a5b12` | `#e0c084` | `특주` |
| 교육 | `#0f7d7d` | `#8ecaca` | `훈련` |
| 휴직 | `#a32a72` | `#e0abc9` | `출산` |
| 휴무 | `#9a9a94` | `#e2e0da` | `OFF` |

의미색은 **글자색**이다. 배경을 칠하지 않는다. 표 셀 안의 근무형태만 예외로
`min-width:30 · height:18 · radius:2 · 1px 테두리`의 작은 칩을 쓴다(§4.3).

내 근무표의 근무형태 큰 칸만 옅은 배경을 쓴다 — `주간 #eaf1fa` · `야간 #fbeeee` ·
`연차 #ecf6ef` · `특주 #f9f1e2` · `훈련 #e8f4f4`. 좌측 `4px solid`는 그 근무형태의 글자색이다.

#### 도메인 색은 팔레트의 예외다

기준정보 `근무형태 관리`가 등록하는 `색상`(목업 실측 `#1e6fd9` `#f50535` `#9aa0a6`
`#2e9e5b` `#d64596` `#12a5a5` `#c78a2b`)은 **UI 토큰이 아니라 데이터**다. 현장이 정하고
계속 늘어난다(목업 기준 49종).

두 가지가 따라붙는다.

- **그 위의 텍스트는 대비를 계산해 보장한다.** 임의 색이 오므로 고정 글자색을 쓸 수 없다
  (`master_work_types.py`의 `_textOn`이 이 계약이다).
- **색은 보조 수단이고 구분은 약칭이 한다.** 49종을 색으로 가를 수 없고, 색각 이상 사용자에게
  색은 단독 신호가 될 수 없다. 목업 범례도 7종만 색으로 보이고 나머지는 `외 42종`으로 접는다.
  **약칭 최대 3자**를 기준정보 입력 제약으로 건다 — 그래야 셀 칩 폭(30)이 데이터에 인질로 잡히지 않는다.

### 2.2 타이포

**글꼴은 맑은고딕 하나다.**

```css
font-family: 'Malgun Gothic', '맑은 고딕', system-ui, sans-serif;
```

숫자는 `font-variant-numeric: tabular-nums` (목업 helmet이 `td, th`에 전역으로 건다).

> **왜 웹폰트를 버리는가** (2026-08-21 확정 · 되돌리려면 위 한 줄만 바꾼다)
>
> ① **AgGrid iframe이 부모의 웹폰트를 구조적으로 못 받는다.** 같은 문자열·같은 12.5px에서
> 부모 180.53px / iframe 202.57px — **12.21% 차이**다. iframe 안에서 `'Pretendard Variable'`
> 지정 폭이 존재하지 않는 폰트 지정 폭과 **완전히 같아** 로드가 아니라 폴백임이 확인됐다.
> `custom_css`는 `{선택자: 속성}` 사전이라 `@font-face`·`@import`를 실을 수 없고,
> `st_aggrid`에 임의 CSS를 iframe에 넣는 API가 없다. 회피 불가다(`demo/README.md` §결과 2).
>
> ② **공장 내부망에서 CDN이 차단되면 어차피 맑은고딕으로 폴백한다.** 웹폰트를 정본으로 삼으면
> 환경에 따라 다른 화면이 나오고, 폰트 metrics에 묶인 열 폭 역산값이 전부 흔들린다.
>
> ③ **목업 자신의 폴백 스택이 맑은고딕이다**(`'Pretendard Variable', Pretendard, 'Malgun Gothic', …`).
> 맑은고딕은 목업이 이미 상정한 두 번째 얼굴이지 새 얼굴이 아니다.
>
> 대가는 명확하다 — 목업 렌더보다 글자가 조금 넓어진다. 그 대신 **부모와 표가 같은 폭**이 되고
> 어느 망에서 열어도 같은 화면이 나온다. 일관성이 미려함보다 앞선다.

크기는 목업이 실제로 쓰는 단계만 쓴다. **이 밖의 크기를 만들지 않는다.**

| 크기 / 굵기 | 역할 | 용례 (실측) |
|---|---|---|
| **21 / 800** | 로고 워드마크 | `WorkOps` (letter-spacing −0.03em) |
| **16 / 800** | 로고 뱃지 글자 | 30×30 사각 안의 `W` |
| **15 / 700** | **리본 화면 제목**(흰색) | 내 근무표의 근무형태 큰 칸 |
| **13.5 / 700** | 기간 이동기 값 | `2026-08` |
| **13 / 600** | GNB 열린 화면 탭 | 달력 날짜(700) · `‹` `›` · `OFF` 표기(700) |
| **12.5 / 400** | **표 본문 셀** | 사이드바 그룹 행(600·700) · 선택 행 키 셀 600 · GNB 사용자명 700 |
| **12 / 400** | 입력 값 · 사이드바 하위 항목 | **조회조건 라벨·구획 제목은 12 / 700** |
| **11.5 / 700** | **표 헤더** · FORM 라벨 셀 · 패널 머리 | 버튼 11.5 / 400 (주버튼 600) · 보조 텍스트 11.5 / 400 |
| **11 / 400** | 캡션 · 범례 · No 열 · 안내문 | 알림 카운트 700 |
| **10.5 / 600** | 달력 예약 막대 · 사이드바 하단 부서 | |

**장식 글리프는 이 스케일 밖이다** — `▼` `▸` `▾`(9) · `▦` `⧉`(10) · `✓`(8) · 요일 보조(9.5).
글자가 아니라 아이콘 자리이므로 크기·대비 규칙의 대상이 아니다.

**굵기는 400 · 600 · 700 · 800이다.** 700이 기본 강조, 600은 표 안·버튼의 약한 강조,
800은 로고 전용이다.

**같은 계층에는 같은 역할을 쓴다.** 조회조건 라벨과 구획 제목은 전부 `12/700 #333`,
표 헤더는 전부 `11.5/700 #222`, 표 셀 값은 전부 `12.5/400`이다. 화면이 자기 크기를
새로 만들지 않는다 — 스케일이 흔들리는 원인은 언제나 "이 화면만 조금 다르게"였다.

긴 문장에 `text-wrap: pretty`.

### 2.3 기하 (px)

| 요소 | 값 | 비고 |
|---|---|---|
| **GNB** | **56** | 밑선 `1px line-chrome` |
| **리본** | **40** | 배경 `accent` |
| 상단 고정 합계 | **96** | 본문·사이드바는 이만큼 아래에서 시작한다 |
| **사이드바 폭** | **206** | 목업 `state.sbw` 기본값. 드래그 범위 150–340 |
| **표 헤더 행 / 본문 행** | **29 / 29** | 헤더와 본문이 같은 높이다 |
| **조회조건 컨트롤 · 액션 버튼** | **28** | radius 2 |
| 기간 이동기가 있는 조회조건 줄의 컨트롤 | **30** | `‹ ›` · 그 줄의 select · `오늘` 버튼 (§4.1-C) |
| **사이드바 그룹 행 / 하위 행** | **31 / 28** | 하위 들여쓰기 `padding: 0 12px 0 27px` |
| 사이드바 점 마커 | 5×5 원 | 그룹·단독 행에만 |
| GNB 열린 화면 탭 | 34 | radius 6 |
| GNB 프로필 버튼 / 아바타 | 32 / 24 | radius 4 / 50% |
| GNB 상태 뱃지(`DB 연결됨`) | 24 | radius 4 |
| 리본 아이콘 | 28×28 | radius 3 · **7종 고정** |
| 리본 `▦` 칩 | 19×19 | radius 3 · `rgba(255,255,255,.22)` |
| 리본 검색 박스 | 28 | radius 2 · `inset 0 1px 2px rgba(90,40,10,.14)` |
| FORM 라벨 셀 폭 | **132** | 모달·프로필은 96 · 요약 패널은 92 |
| 모달 폭 / 머리 | 640 / 34 | `max-width: 100%` |
| 표 안 근무형태 칩 | min-width 30(명단 34) × 18 | radius 2 |
| 달력 요일 머리 | **27** (목업 `일` 칸만 30) | §11-1 |
| 달력 날짜 칸 | 예약 캘린더 86 · 내 근무표 92 · 신청 달력 50 | 앞 둘은 `min-height` |
| 달력 예약 막대 | 19 (줄 간격 21 · 상단 오프셋 23) | |
| 격자 일자 열 | 34 | 고정열 기본 No 30 · 사번 82 · 성명 70 · 부서 118 · 조 44 |
| 표 스크롤 컨테이너 | `max-height: 520` | 목록형 표 |
| 라운딩 | 표·입력·버튼 **2** · GNB 탭/브랜드 6 · 리본 아이콘·칩 3 · 아바타 50% |
| 본문 여백 | 좌우 **14** · 조회조건 `11px 14px` · 구획 제목 `8px 14px 0` · 표 `margin: 6px 14px 14px` |
| 간격 | 버튼 사이 **5** · 조회조건 칸 사이 `7px 22px` · 범례 8~12 · GNB 12 |

**밀도 예외는 격자뿐이다.** 일자 열 34와 셀 칩 18은 31일을 한 화면에 넣기 위한 값이고,
이 예외를 다른 화면으로 옮기지 않는다.

---

## 3. 셸 문법 — 전 화면 공통

### 3.1 고정 프레임

```
┌─────────────────────┬───────────────────────────────────────┐
│ GNB 56  ▣ WorkOps   │ ⧉ 월간 근무표 ✕ …  DB · ? · 🔔 · 이광호 │
├─────────────────────┼───────────────────────────────────────┤
│ 리본 40   메뉴 검색  │ ▦ 월간 근무표         ⓘ 文 ＋ ⌕ 🗑 💾 ★ │
├─────────────────────┼───────────────────────────────────────┤
│ 사이드바 206         │ 본문 — 여기만 스크롤한다               │
│  · 근무 현황         │  [조회조건 줄]                         │
│  ● 근무표        ▾   │  ■ 구획 제목        [주버튼][보조][보조]│
│      근무표 편성     │  [표]                                  │
│      월간 근무표     │  ■ 다음 구획 …                         │
│  ● 아차사고 관리  ▸  │                                        │
│  …                  │                                        │
│ ──────────────────  │                                        │
│  (이) 이광호 · 전산팀 │                                        │
└─────────────────────┴───────────────────────────────────────┘
```

바깥은 `height:100vh` 세로 flex다. GNB·리본이 `flex:none`, 그 아래 줄이 `flex:1; min-height:0`,
그 안에서 사이드바가 `flex:none`, 본문이 `flex:1; min-width:0; overflow:auto`다.
**페이지는 스크롤하지 않고 본문만 스크롤한다.** 이것이 이 셸의 첫 성질이다.

GNB의 브랜드 영역과 리본의 검색 영역은 **사이드바와 같은 폭(206)** 을 쓴다. 셋이 한 세로선을 이룬다.

### 3.2 GNB (56)

- 배경 `#fff` · 밑선 `1px line-chrome` · `gap:12` · `padding: 0 16px 0 0`.
- **브랜드** — 206폭 · `padding-left:16` · `gap:9`. 30×30 radius 6 `accent` 사각에 `W`(16/800 흰색) +
  워드마크 `Work`(`ink-strong`) `Ops`(`accent`) 21/800.
- **열린 화면 탭** — 34 · `1px accent` · radius 6 · 흰 배경 · 13/600 `accent`.
  좌측 `⧉`(10 · `#d18a5e`), 우측 `✕`(15×15 · 11 · `#c07a51`, hover 배경 `#f6dcca` 글자 `#8f350a`).
  MDI 관례다 — **라우터 링크가 아니라 지금 열려 있는 화면**이고, 진짜 내비는 사이드바다.
- **우측**(`margin-left:auto` · `gap:14`) — 상태 뱃지 24(`#f2f8f3` / `1px #cfe0d3` / 11.5 `#2f6b4f`),
  도움말·알림 아이콘 22×22(stroke `#4a4640`), 알림 카운트 14 radius 7 `#d43a2f` 9/700 흰색,
  프로필 32(테두리 `line-chrome`, 열리면 `accent`) = 아바타 24 + 이름 12.5/700 + `▾`.
- 프로필 드롭다운 — 300폭 · `top:38 right:0` · `1px #b5b1aa` ·
  `box-shadow: 0 6px 18px rgba(40,30,20,.18)` · 머리 30 `head-bg`. 안은 §4.4의 라벨 셀(86폭) 구조다.

### 3.3 리본 (40)

- 배경 `accent`. 좌측 206폭 검색 영역(`padding: 0 12px 0 16px` · 우측 경계 `1px accent-hover`)에
  28 흰 검색 박스(`1px accent-line` · radius 2 · `inset 0 1px 2px rgba(90,40,10,.14)` ·
  11.5/600 `ink-3` `메뉴 검색` + `☰⌕`).
- 우측 영역 `padding: 0 14px` · `justify-content: space-between`.
  왼쪽 = `▦` 칩 19×19 + 화면 제목 15/700 흰색. 오른쪽 = **아이콘 7종 고정**(`gap:3` · 28×28 ·
  hover `rgba(255,255,255,.2)`): 정보 · 언어 · 추가 · 조회 · 삭제 · 저장 · 즐겨찾기.

> **리본 아이콘은 화면마다 바뀌지 않는다.** 구판은 "화면별로 실제 쓰는 것만 노출"로 확정했으나
> **목업이 그 결정을 뒤집었다**(부록 A-5). 이 7종은 **전역 도구**이지 그 화면의 액션이 아니다.
> 화면의 액션은 §4.9대로 구획 제목 줄에 **글자 버튼**으로 나온다. 그래서 "라벨 없는 아이콘의
> 비활성 사유"(§7-3) 문제가 성립하지 않는다 — 비활성 상태 자체를 두지 않기 때문이다.
> 아이콘이 그 화면에서 할 일이 없어도 회색으로 죽이지 않는다. **화면 액션이 애초에 거기 없다.**

### 3.4 사이드바 (206)

- 배경 `surface-sub` · 위 `padding: 2px 0 10px` · 아래에 사용자 블록.
- **단독 행**(근무 현황)과 **그룹 행** = 31 · 12.5 · `padding: 0 12px` · `gap:8` · 좌측 5×5 점.
  펼친 그룹 `700 / #2e2a26 / 배경 surface-menu / 점 accent`,
  접힌 그룹 `600 / ink-3 / 배경 없음 / 점 #b3ada6`.
  우측 끝 `▾`(펼침) / `▸`(접힘) 9 `ink-muted`.
- **하위 행** = 28 · 12 · `padding: 0 12px 0 27px` · 마커 없음.
- **선택 항목** = `accent` 글자 · 700 · 배경 `accent-tint` · `box-shadow: inset 3px 0 0 accent`.
  비선택 = `ink-3` · 400. hover = `surface-menu`.
- **그룹은 5개 + 단독 1개다** — `근무 현황` / `근무표`(3) · `아차사고 관리`(4) · `업무요청`(4) ·
  `숙소 예약`(4) · `기준정보`(3). 평평한 나열도, 업무영역(안전·품질·총무) 묶음도 아니다.
  **모듈 단위로 묶고, 현재 화면이 속한 그룹은 자동으로 펼친다.**
- **하단 사용자 블록** — 위 `1px line-soft` · `padding: 9px 12px` · `gap:7`.
  아바타 22 `#e5e1d8` 10.5/600, 이름 11.5/600 `ink-name`, 부서 10.5 `ink-muted`.
- 우측 경계는 5px 드래그 핸들(`border-left: 1px line-outer`, hover `resize-hot`).
  폭 조절은 목업 기능이며 Streamlit 구현 가능성은 §12 미측정이다.

---

## 4. 화면 부품

목업의 모든 화면은 아래 부품의 조합이다. **새 부품을 만들기 전에 이 목록을 먼저 본다.**

### 4.1 조회조건 줄

공통 — `padding: 11px 14px` · 밑선 `1px line-panel` · 라벨 **12/700 `#333`** ·
입력 칸 `1px #c9c5be` · radius 2 · 안쪽 `padding: 0 9px` · 값 12 ·
우측 글리프 `▼`(9) / `⌕`(11) / `▦`(10) `ink-glyph`.

세 형태가 있고 **화면 성격이 형태를 정한다.**

**A. 격자형** — 조건이 4개 이상이고 라벨 길이가 제각각인 화면(근무표 · 사용자 관리).

```css
display: grid; grid-template-columns: repeat(auto-fit, minmax(230px, 1fr)); gap: 7px 22px;
```

라벨 `flex: 0 0 62px; text-align: right`, 컨트롤 `flex:1`, 높이 **28**.
**폭이 모자라면 칸이 다음 줄로 접힌다** — 라벨은 끝까지 컨트롤 옆에 남는다.

**B. 나열형** — 조건이 3개 이하로 짧은 화면(조회 목록 전반).
`display:flex; gap:18px; flex-wrap:wrap`, 라벨은 폭 고정 없이 붙고 컨트롤 `min-width:130px` · 높이 28.

**C. 기간 이동형** — 기간을 앞뒤로 넘기는 화면(근무 현황 · 예약 캘린더 · 내 근무표 · 숙소 신청).
`display:flex; gap:8px; flex-wrap:wrap`. **이 줄의 조회 컨트롤만 높이 30**이다.
기간 이동기 = `‹`(27×30 `step-bg`, 우측 `1px line-soft`) + 값(13.5/700 `ink-strong` · `min-width:96`) +
`›`. 옆에 `오늘`/`이번 달` 버튼(30), 세로 구분자(`1px × 20 line-panel`), 부서 칩(30).
**액션 버튼은 이 줄에서도 28이다** — 조회 조건과 액션의 높이는 구분한다.

**필수 조건 칸은 살구색(`accent-tint-2`)** 이다. 목업은 첫 칸(기간 · 재직 여부 · 숙소)을 그렇게 칠한다.

### 4.2 구획 제목 줄

```
■ 사용자   총 127명 · 재직 126 · 퇴직 1        [비밀번호 초기화] [퇴직 처리]
```

- `padding: 8px 14px 0` · `align-items: baseline` · `gap:10`.
- `■`은 글자다 — 제목 전체가 **12/700 `#333`**. 부제는 **11.5/400 `#666`**
  (제목 `<span>` 안에 넣을 때는 `400 #888`).
- 우측 버튼 묶음 `margin-left:auto` · `gap:5` · **`flex-wrap:wrap`**.
  제목 줄 자체도 `justify-content:space-between; flex-wrap:wrap`이라 좁아지면 버튼 묶음이
  통째로 다음 줄로 내려간다. **이것이 목업이 800px까지 안 깨지는 이유다**(`demo/README.md` §결과 4).
- **지표는 부제로 말한다.** 읽기 전용 지표 타일을 따로 깔지 않는다(§7-1).

### 4.3 표

- 바깥 `1px line-outer` · `margin: 6px 14px 14px` · `overflow-x:auto`
  (스크롤형은 `overflow:auto` + `max-height:520`).
- **헤더** 29 · `head-bg` · 11.5/700 `#222` · 가운데 정렬 · `padding: 0 8px`(체크·No 열 `0 5px`) ·
  밑선 `1px line-outer` · 열선 `1px line-head`. 스크롤형은 `position:sticky; top:0`.
- **본문** 29 · **12.5/400** · 행선 `1px line-row` · 열선 `1px line-col` · `white-space:nowrap`.
- **정렬** — 수치·표시순서 우측, 날짜·상태·코드·조 가운데, 이름·번호·제목 좌측.
  No 열은 가운데 11 `ink-glyph`.
- **선택 행** — 배경 `accent-tint` · `box-shadow: inset 3px 0 0 accent` · 키 셀 600 ·
  글자 `ink-strong`. hover는 `accent-tint-2`(선택 행은 hover해도 `accent-tint`를 유지한다).
- **합계 행** — 배경 `accent-tint-2` · 전 칸 700 · 밑선만 `line-head`로 한 단 진하게.
  **합계 행이 곧 지표다.** 별도 타일을 만들지 않는다.
- **퇴직·비활성 행** — 배경 `row-out`. 값 `0`은 `ink-placeholder`로 흐린다.
- **결측은 `—`** 다(가운데 · `ink-faint`). 빈 칸으로 두지 않는다 — 목업이 `조` · `직급` ·
  `퇴사일` · `담당자`에서 일관되게 `—`를 쓴다.
- **상태·근무형태는 배지가 아니라 낱말이다**(§4.8). 근무형태만 작은 칩(30×18 · radius 2 ·
  `1px 의미색 테두리` · 글자 의미색 600)을 쓴다.
- **체크박스** — 15×15 · radius 2 · `1px #9a958d`(선택 시 `accent` 채움). 열 폭 34.

**격자(31일)는 표의 변형이다.**
`No / 사번 / 성명` 3열이 `position:sticky; left: 0 / 30 / 30+id`(z-index 헤더 3 · 본문 1),
헤더는 `sticky top`. **조 열 우측 경계만 `line-outer`로 굵혀 고정열과 일자열을 가른다.**
일자 헤더는 2줄(`31` 위 11/700 + `금` 아래 9.5/500), 주말 배경 `#f3efe9`.
열 폭은 드래그로 바뀐다(사번 54–220 · 성명 50–200 · 부서 60–280 · 조 36–140).
**조회판과 편성판은 같은 격자이고 셀 칩만 다르다** — 조회는 테두리 있는 칩(11.5),
편성은 테두리 `transparent` + 12px(편집 중인 값이 더 크게 읽혀야 한다).

### 4.4 FORM (라벨 셀 + 값 셀)

두 가지 배치가 있다.

**세로 1열** (아차사고 등록 · 업무요청 등록 · 업무요청 처리 등록) —
패널 `1px line-outer` · `flex: 1.7; min-width: 520px; max-width: 860px`.
행마다 `라벨 셀 132`(`head-bg` · 우측 `1px line-head` · 11.5/700 `#333` · `padding: 8px 10px`) +
`값 셀`(`flex:1` · `padding: 5px 8px`). 필수는 라벨 앞 `*`(accent · `margin-right:3px`).
입력 칸 `min-height`는 **한 줄 26 · 여러 줄 76 · 긴 서술 150**, radius 2.
맨 아래 액션 바 — 배경 `surface-sub` · `padding: 8px 10px` · 버튼 우측 정렬.

**2열 격자** (모달 · 프로필) — `grid-template-columns: 96px minmax(0,1fr) 96px minmax(0,1fr)`,
행 높이 30. 폭이 필요한 필드는 `grid-column: 2 / 5`로 통째로 쓴다.
읽기 전용 칸은 `field-ro` 배경 + `ink-5` 글자. 필수 칸은 `accent-tint-2` 배경.

> **산문은 2열 격자에 넣지 않는다.** 가변 길이 값이 좁은 칸에 들어가면 옆 칸과 높이가 어긋난다.
> 짧은 고정 값만 2열에 넣고, 서술·다중 선택은 통짜 행으로 뺀다(목업 `담당 지정`이 그렇다).

**폼 안의 묶음 제목**은 11.5/700 `ink-muted` · `letter-spacing: 0.04em` · `padding: 13px 0 5px`
(첫 절은 `0 0 5px`). `■`을 붙이지 않는다 — `■`은 본문 구획 전용이다.

**필수 충족 상태는 하단 요약 줄에 개수로** 적는다 — `필수 3/3`(충족 시 `#1c6b41` 700).
별도 체크리스트 패널을 두지 않는다(§7-2). 제출 버튼을 눌러야 뭐가 빠졌는지 알게 하지 않는다.

**첨부 필드**는 값 셀 안에서 `파일 업로드` 보조 버튼 + `사진 촬영` 액센트 버튼
(`accent-tint` 배경 · `1px accent` · 600) + 파일 칩(28 · `1px line-panel` · `surface-sub`) +
제약 안내(11 `ink-muted`)를 `flex-wrap:wrap`으로 늘어놓는다.

### 4.5 달력

요일 머리 — `grid-template-columns: repeat(7,1fr)` · 배경 `head-bg` · 밑선 `1px line-outer` ·
칸 **27** · 11.5/700 · 열선 `1px line-head`. `일` = `#a3282a` · `토` = `#17457f` · 나머지 `#222`.

주 행 — `grid` 7열 · 밑선 `1px line-row` · 칸 우측선 `1px line-col`.
전월·익월 칸은 배경 `outmonth-bg` 글자 `ink-outmonth`, 주말 칸은 `weekend-bg`.
**오늘은 `box-shadow: inset 0 0 0 2px accent`** 다 — 배경으로 칠하지 않는다.

세 가지 쓰임이 있다.

- **기간 막대형**(예약 캘린더 · 숙소 예약 승인) — 칸 `min-height:86` · 날짜 11/600.
  **예약 1건 = 주를 가로지르는 막대 하나.** 주 행 위에 절대배치 격자
  (`top:23 · grid-auto-rows:21 · pointer-events:none`)를 얹고, 막대는 19 · 10.5/600 ·
  `grid-column: <시작> / span <일수>`. **숙소마다 줄(lane)과 색이 고정**이고
  (대관령 `#e6e9ec`/테두리 `#a9b3bc`, 태안 `#eeeadf`/`#c3b795`, 내 예약 `accent`/흰 글자),
  승인대기는 **점선 테두리 + 옅은 배경**이다. 달을 넘는 예약은 `◀`/`▶`를 붙이고 그쪽 테두리를
  지운다 — 잘린 것이 아니라 이어진다는 뜻이다.
- **내용 칸형**(내 근무표) — 칸 `min-height:92` · `padding: 8px 9px` · 날짜 13/700.
  근무형태 칸 32 · 15/700 · 옅은 배경 + 좌측 `4px` 의미색. `OFF`는 칠하지 않고
  13/700 `#c3beb6` `letter-spacing:0.1em`로만 적는다.
- **선택형**(숙소 예약 신청) — 칸 50 · 날짜 12.5 · 아래 9.5/600 보조 낱말.
  선택 기간은 `accent` 채움 + 흰 글자, 이미 예약된 날은 `booked` 배경 +
  `inset 0 -3px 0 #c9c5be`(밑줄) + 신청자 이름, 지난 날은 `ink-placeholder`.

**범례는 달력 위 구획 제목 줄 오른쪽에 둔다** — 16×10 색 견본 + 11 낱말 · `gap:12`.

### 4.6 우측 보조 패널

폼·달력 화면 오른쪽에 붙는 세로 패널(`flex:1; min-width: 280~300; max-width: 420`).
`1px line-outer` · 머리 27~29 `head-bg` 11.5/700 · 행마다 `1px line-row`.

두 가지 내용만 담는다.

- **요약**(신청 요약) — 라벨 셀 92 `surface-hover` + 값 12.5. 맨 아래 요약 줄(`surface-sub`)에
  `필수 3/3`과 다음 단계 안내 한 줄.
- **최근 이력**(내 최근 아차사고 · 이 요청의 처리 이력) — 행마다 3줄:
  번호/시각(11 `ink-muted`) ‖ 상태(11/700 의미색) → 제목(12.5) → 날짜·작성자(11 `ink-muted`).
  hover `accent-tint-2`. 맨 아래 `전체 보기 ›`(11.5 `accent`).

**본문과 폭을 나눌 때는 바깥 줄에 `flex-wrap:wrap`을 건다.** 좁아지면 패널이 아래로 내려간다.

### 4.7 모달

640폭 · `max-width:100%` · `1px #7d766d` · `box-shadow: 0 10px 32px rgba(0,0,0,.28)` ·
바깥 `rgba(34,30,26,.42)` · `padding: 56px 20px`(위에서 시작한다).
머리 34 짙은 배경(`#3f3934`) 흰 글자 12.5/700 + `✕`.
그 아래 액션 줄(`padding: 8px 12px` · 밑선 `1px line-panel` · 주버튼 우측),
본문은 `overflow:auto` + `padding:12` + §4.4의 2열 격자.

**모달은 편집 폼에만 쓴다.** 조회·확인에는 쓰지 않는다 — 표에서 이미 보이는 것을 덮지 않는다.

### 4.8 상태 표기 — 낱말에 색만 준다

표 셀 안에서 **배지를 쓰지 않는다.** 낱말에 §2.1 의미색을 주고 굵기는 600이다.

| 낱말 | 색 |
|---|---|
| `평가완료` · `완료` · `재직` · `✓` | `#1c6b41` |
| `반려` · `퇴직` · `긴급` | `#a3282a` |
| `평가중` · `처리중` | `#b8460d` |
| `제출됨` · `접수` · `보류` | `#555` |

- **상태의 결과는 상태 옆에 따로 적는다.** 상태 이름만으로는 그 상태가 무엇을 막고 무엇을 여는지
  알 수 없다 — 사용자가 버튼을 왜 눌러야 하는지 모르는 화면이 실제로 있었다.
- **경과일·지연에 색을 칠하지 않는다.** SLA가 없으면 색이 말할 기준이 없고, 목록이 이미
  정렬로 우선순위를 말한다. SLA가 정해지면 그때 넣는다.
- **불가능한 전이는 그리지 않는다.** 조건만 안 갖춰진 것은 비활성으로 두되 **사유를 화면에 쓴다.**
  툴팁에만 두지 않는다 — 마우스를 올려야 알 수 있으면 모르는 것과 같다.

### 4.9 버튼이 놓이는 자리는 둘뿐

1. **구획 제목 줄 오른쪽** — 그 구획에 대한 동작. 목업 실측:
   `근무표 편성` = [편성 확정][임시저장][되돌리기] · `숙소 예약 승인` = [승인][반려][보완요청] ·
   `아차사고 조회` = [평가 완료][보완요청][반려] · `내 업무요청` = [수정][취소] ·
   `사용자 관리` = [비밀번호 초기화][퇴직 처리].
2. **폼 하단 액션 바** — 화면 전체를 마무리하는 동작. [제출][임시저장][초기화].

- **주버튼은 줄마다 하나**다 — `accent` 채움 · 흰 글자 · 600 · hover `accent-hover`.
- 나머지는 흰 배경 · `1px #c9c5be` · `#333` · 400 · hover `surface-hover`.
- **부정적 액션(반려 · 취소 · 퇴직 처리)도 보조 버튼 모양이다.** 목업은 붉은 버튼을 만들지 않는다 —
  파괴적 결과는 색이 아니라 **확인 단계**가 막는다.
- 세 번째 형태가 하나 있다 — `1px accent` + `accent-tint` 배경 + `accent` 글자 700.
  `비밀번호 초기화` · `사진 촬영`처럼 **주버튼은 아니지만 눈에 띄어야 하는 것**에만 쓴다.
- 같은 크기·같은 색 버튼을 **5개 이상 나열하지 않는다**(목업 최대 4 — `업무요청 처리 등록`).

---

## 5. 화면 유형

유형은 `SCREEN_ARCHETYPE` 상수로 선언하고 `scripts/test_screen_scaffold.py`가 검증한다.
**이름은 구판 그대로 6종이고 골격만 신판이다.** 한 유형의 규칙을 다른 유형에 옮기지 않는다.

### DASHBOARD — 어디에 개입할지 정한다

- **결정** — 오늘 어디에 사람이 모자란가
- **골격** — 조회조건 C(기간 이동 + 부서 칩) → `■ 요약 표`(합계 행) → `■ 명단 표`
- **해당** — 근무 현황, 아차사고 분석

**지표 타일을 두지 않는다.** 목업은 지표를 **합계 행**(`accent-tint-2` 배경 · 700)으로 말한다.
`전체 합계 / 정원 45 / 당일 근무 28 / … / 62%`가 한 줄에 있고, 그 아래 부서별 행이 같은 축으로
이어진다. 타일 6장이 하던 일을 표 한 줄이 더 정확하게 한다 — **부서별로 곧장 비교되기 때문이다.**

**역할에 따라 다른 화면을 만들지 않는다.** 같은 화면 id는 누가 보든 같은 골격·같은 블록을 쓰고,
**권한은 데이터 범위로만 갈린다**(전사 / 자기 부서).

### WORKLIST — 처리할 건을 골라 결정한다

- **결정** — 이 건을 어떻게 처리할 것인가 (승인 · 반려 · 보완요청 · 평가확정)
- **골격** — 조회조건 B(상태 포함) → `■ 목록 표` + **구획 제목 줄에 판정 버튼** → 필요하면 아래 구획
- **해당** — 아차사고 조회(평가), 숙소 예약 승인, 업무요청 현황, 개선조치 관리

> **2열 33/67 마스터-디테일은 폐기한다** (구판 §2 · §7.1). 목업은 승인 큐를
> **전폭 목록 표 + 구획 제목 줄 액션**으로 그린다(`아차사고 조회` · `숙소 예약 승인`).
> 상세가 필요하면 목업 `숙소 예약 승인`처럼 **같은 화면 아래 구획**(달력)을 잇거나
> 모달(§4.7)로 연다. 좌우로 화면을 반 잘라 목록을 33%로 좁히면 12열 목록이 들어가지 않는다 —
> 이 도메인의 목록은 열이 많다. `erp.select_list` 신규 작성도 함께 폐기한다.

- **상태 필터는 조회조건 줄의 select**다. 상태 탭(`st.segmented_control`)을 쓰지 않는다.
- 목록 행에는 **무엇을 먼저 처리할지 고를 근거**를 담는다(발생일 · 소속 · 원인 · 우선순위).
  식별자만 있는 행은 정렬 순서 외에 아무 판단도 돕지 못한다.
- 선택 행은 `accent-tint` + `inset 3px 0 0 accent`. 판정 버튼은 그 선택에 대해 동작한다.

### READ_VIEW — 현재 상태를 확인한다

- **결정** — 다음에 무엇을 할 것인가 (또는 결정 없음)
- **골격** — 조회조건 A/B/C → `■ 표` 또는 `■ 달력`
- **해당** — 아차사고 조회(일반), 내 아차사고, **내 업무요청**, 내 숙소 예약, 월간 근무표,
  내 근무표, 예약 캘린더

`내 업무요청`은 READ_VIEW다(부록 A-9). 목업이 조회 표 + [수정][취소]로 그린다 —
종결 확인·보완 요청 2지 판정은 요청자가 아니라 **처리자 화면**(업무요청 처리 등록)이 담당한다.

### FORM_ENTRY — 새 사실을 기록한다

- **결정** — 제출할 준비가 되었는가
- **골격** — `■ 제목` → 폼 패널(§4.4 세로 1열) ‖ 우측 보조 패널(§4.6) → 하단 액션 바
- **해당** — 아차사고 등록, 업무요청 등록, 업무요청 처리 등록, 숙소 예약 신청(달력형 변형)

- **읽기 화면과 라벨 열 폭·필드 순서를 일치**시킨다. 같은 레코드를 등록에서 쓰고 평가에서 읽는다 —
  두 화면의 배치가 다르면 작성자는 자기 글이 어떻게 보일지 예측할 수 없다.
- 우측 보조 패널에는 **내가 최근에 낸 같은 종류의 건**을 둔다. 목업이 세 폼 전부 그렇게 한다 —
  중복 신고를 막고 형식을 참고하게 한다.
- 숙소 예약 신청만 폼 대신 **선택형 달력 + 요약 패널**이다(§4.5). 기간 입력이 본질인 화면에서
  날짜 두 칸을 받는 것보다 달력을 직접 찍는 편이 짧다.

### EDIT_GRID — 기준정보를 고친다

- **결정** — 무엇을 저장할 것인가
- **골격** — 조회조건 A → `■ 제목 + 건수 부제 + 액션` → 스크롤 표(sticky 헤더 · 체크박스 열) →
  (행 더블클릭) 모달 편집
- **해당** — 사용자 관리, 조직 관리, 근무형태 관리, 부서 관리, 조 관리

**목업의 기준정보 편집은 인라인 편집이 아니라 모달 편집이다.** 표는 읽고 고르는 자리이고,
고치는 것은 §4.7 모달이 한다. 기존 AgGrid 인라인 편집 자산은 보존하되(§6.1) 신규 화면은
모달 경로를 기본으로 한다.

체크박스 열은 **여러 건 일괄 처리**(비밀번호 초기화 · 퇴직 처리)를 위한 것이고,
그 액션은 구획 제목 줄에 있다. 본문에 같은 버튼을 두 번 두지 않는다(§7-2).

### MATRIX_EDIT — 격자를 직접 채운다

- **결정** — 무엇을 저장할 것인가
- **골격** — 조회조건 A → `■ 제목 + 액션` → 격자(§4.3) → 범례 → 요약 줄
- **해당** — 근무표 편성 (조회판이 월간 근무표)

- 신원 열 `sticky left`, 헤더 `sticky top`. 격자는 자체 스크롤 컨테이너를 갖는다.
- **범례는 격자 바로 아래**에 `1px line-panel` 상자(`surface-sub` · `padding: 7px 10px`)로 둔다.
  9×9 색 사각 + 11px 낱말, 넘치는 종류는 `외 42종`으로 접는다.
- 맨 아래 요약 줄 — `총 45명 · 1,395건`(11.5 `#666`, 우측 정렬).
- **기존 편집 계약(클릭 순환 · 숫자키 · Ctrl+V · 저장 흐름)을 보존한다.** 이번 개편은 시각 토큰과
  셸만 바꾸고 격자 자체를 재작성하지 않는다 — 편집 계약 회귀 위험이 시각 이득보다 크다.

---

## 6. 구현 계약 — Streamlit에서 이 문법을 어떻게 세우는가

`demo/`가 셸 + 4화면으로 실측한 결과다(1536×770 · 2026-08-21). **기하는 목업과 일치한다** —
GNB 56 / 리본 40 / 사이드바 206 / 행 29 / 컨트롤 28 / 그룹 31 / 하위 28 / 페이지 무스크롤 /
sticky 신원 열 / 필수 살구색 `#fdf6f1`이 전부 목업 값으로 재현됐다.
참조 구현은 `demo/ui.py`이며 **이 문서와 그 파일이 어긋나면 둘 중 하나가 버그다.**

### 6.1 표는 두 문법으로 갈린다

| 쓰임 | 문법 | 이유 |
|---|---|---|
| **조회 · 현황 · 목록 · 격자 조회** | 페이지 DOM `<table>` (주입 HTML) | §4.3을 픽셀 그대로 재현할 수 있다. 부모 문서라 글꼴·선·행높이가 셸과 같다 |
| **편집 격자** | AgGrid (iframe) | 셀 편집 · 정렬 · 체크박스 선택이 필요하고, 기존 편집 자산이 여기 있다 |

**AgGrid에서 포기하는 것을 명시한다.** 아래는 못 하는 것이지 안 한 것이 아니다.

| 목업 요구 | AgGrid | 원인 |
|---|---|---|
| 2줄 날짜 헤더(`31` 위 / `금` 아래) | ❌ **31개 중 17개 헤더 잘림**(`10…`) | `headerComponent`(JsCode) 필요 — 배포 환경에서 JsCode 미실행 |
| 셀 칩(테두리 있는 작은 상자) | ❌ 색 글자만 남는다 | `cellRenderer` 필요 — 같은 이유 |
| 주말 헤더 색 | ❌ | 같은 이유 |
| 열이 표 폭을 채움 | ❌ 우측 **131px** 빈 공간 | 고정 폭 열 합 < viewport |
| 행 높이 · 헤더 높이 29 | ✅ | |
| 셀 편집 · 정렬 · 체크박스 | ✅ | HTML 판에는 없는 것 |

**글꼴 이질성은 §2.2(맑은고딕 통일)로 해소된다.** 그 전까지 두 문법은 같은 문자열에서 12.21%
다른 폭을 냈다 — 통일이 이 계약의 전제다.

> **판정** — `MATRIX_EDIT` 격자를 AgGrid로 옮기면 목업 격자를 잃는다. 그래서 **월간 근무표(조회)는
> DOM 표**, **근무표 편성은 기존 편집 격자 유지**로 가른다(§5 MATRIX_EDIT).

### 6.2 CSS 주입은 두 API를 섞어야 한다

**`st.html()`은 웹폰트를 넣을 수 없다.** `<link rel=stylesheet>`도 `<style>@import …</style>`도
**둘 다 제거된다**(주입 후 문서의 `@font-face` 규칙 0건 · 지정 폭 == 미설치 폰트 폭).

`st.markdown(..., unsafe_allow_html=True)`로는 들어가지만, 그 경로는 **빈 줄마다 CSS가 잘려**
텍스트로 새어나온다(마크다운 파서가 HTML 블록을 빈 줄에서 끊는다 — 데모 첫 실행이 그렇게 깨졌다).

```
폰트  = st.markdown(unsafe_allow_html=True)  ← 반드시 한 줄
그 외 = st.html()                            ← 여러 줄 CSS는 전부 이쪽
```

§2.2로 웹폰트를 버리면 첫 줄의 필요는 사라진다. **그래도 이 제약은 문서에 남긴다** —
되돌릴 때(사용자가 웹폰트를 요구할 때) 같은 함정을 다시 밟지 않기 위해서다.

### 6.3 접기 규칙과 `st-key` 문법

**Streamlit 컬럼은 좁아지면 줄어들지 않고 넘친다.** 컬럼은 **비율만** 받고 "모자라면 접는다"는
개념이 없다. 목업의 `62px 라벨`·`132px 라벨 셀`·`max-width:860px` 같은 px 고정도 표현할 수 없다.
처음 데모는 1366px에서 액션 버튼이 서로 겹쳤다.

**전역 `stHorizontalBlock { flex-wrap: nowrap }`은 금지다**(§7-8). 접힐 길을 막는다.
대신 목업처럼 **구간별로** 규칙을 준다.

| 구간 | 규칙 |
|---|---|
| 조회조건 줄(격자형) | 바깥 블록 `flex-wrap:wrap; row-gap:7px`, 각 칸 `flex: 1 1 230px; min-width:230px` |
| 조회조건 칸 **안쪽** | `flex-wrap: nowrap !important` — 라벨은 끝까지 컨트롤 옆에 남는다 |
| 조회조건 라벨 열 | `flex: 0 0 66px`(목업 62 + Streamlit 여백) — 비율로 두면 `사번·성명`이 잘린다 |
| 버튼 줄 | `st.container(key="btnrow_…")` + `flex-direction:row; flex-wrap:wrap; gap:5px`, 버튼 컨테이너를 `width:auto; flex:0 0 auto`로 되돌린다 |

**`st-key-*` 문법 두 가지를 지킨다.**

1. `st.container(key=...)`가 만드는 `st-key-*` 클래스는 **`stVerticalBlock` 자신에** 붙는다.
   자손 선택자(`.st-key-x div`)로 잡으면 매치되지 않는다 — 첫 시도가 그래서 실패했고 버튼이 세로로 쌓였다.
2. **한글·공백이 든 `key`는 치환된다**(`btnrow_2026-08-------`). `[class*="st-key-btnrow_"]`처럼
   **부분일치**로 잡는다.

**수정 후 재계측** — 1440 / 1366 / 1280 / 1100 / 1024 / 980 / 880 / 800px 전부
버튼 1줄 · 간격 5px(목업과 같음) · 우측 넘침 없음 · 잘린 요소 없음 · 가로 오버플로 0.
800px에서 조회조건 4칸은 목업처럼 2줄로 접히고 라벨은 끝까지 남는다.

### 6.4 반응형

목업이 답을 갖고 있다. **분기점은 768px**(`vw <= 768`)이고, 모바일 셸을 가진 화면은 6개다.

| 폭 | 셸 | 화면 |
|---|---|---|
| **> 768** | 데스크톱 셸(§3) | 전부 |
| **≤ 768** | 모바일 셸 — 상단 바 52 · 햄버거 드로어(행 46) · 터치 타깃 44 | 내 근무표 · 근무표 편성 · 아차사고 등록 · 업무요청 등록 · 숙소 예약 신청 · 숙소 예약 승인 |
| **≤ 768, 그 외 화면** | "PC에서 여세요" 안내 | 나머지 전부 |

- 데스크톱 구간에서는 §6.3의 두 접기 규칙만으로 800px까지 무손상이다(실측).
- 모바일 셸의 정본은 `docs/mockup/WorkOps 모바일.dc.html`(2026-08-21 확보)이다.
  **Streamlit 이관과 실렌더 계측은 아직 하지 않았다** — §12 미측정.
- **표는 열을 줄이지 않는다.** `overflow-x:auto` + `min-width`로 가로 스크롤을 준다.
  숨기면 어느 열이 사라졌는지 알 수 없다.

### 6.5 내부 선택자는 예산이다

데모는 4화면에 `data-testid` 등 Streamlit **내부 선택자를 37곳** 썼다(`demo/ui.py`에 `[ST-INTERNAL]` 표시).
운영 앱은 22화면에 약 250곳을 쓰고 있다.

내부 선택자는 Streamlit 마이너 버전에서 조용히 죽는다 — 1.59가 `selectbox`·`text_input`을
BaseWeb에서 react-aria로 갈아치우자 `div[data-baseweb="select"]` 계열 20건이 **문법은 멀쩡한 채
아무것도 매치하지 않는 상태**가 됐다. CSS는 매치 0을 오류로 알리지 않는다.

**그래서 §8의 선택자 생존 검사가 정식 게이트다.** 내부 선택자를 늘리는 변경은
그 검사를 함께 통과시켜야 한다.

---

## 7. 금지 — 이유와 함께

1. **읽기 전용 지표 타일을 화면 상단에 나열하지 않는다.** 지표가 목록을 바꾸지 않으면 자리값을
   못 한다. 건수는 구획 부제로, 집계는 표 합계 행으로 말한다(§4.2 · §4.3).
2. **한 화면에 같은 기능의 진입점을 두 곳 두지 않는다.** 어느 쪽이 정본인지 사용자가 알 수 없다.
3. **비활성 컨트롤의 사유를 숨기지 않는다.** 사유 없는 회색 버튼은 고장과 구분되지 않는다.
4. **표 셀 안에 배지를 만들지 않는다.** 낱말 + 의미색이 정본이다(§4.8). 배지를 허용하면
   상태마다 배경색이 생기고 표의 리듬이 무너진다.
5. **행 높이가 상태에 따라 달라지는 목록을 만들지 않는다.** 리듬이 깨지면 스캔이 불가능해진다.
6. **§2 토큰 밖의 색·크기를 만들지 않는다.** 예외는 §2.3의 격자 밀도 예외와 §2.1의 도메인 색뿐이고,
   쓸 때는 사유를 코드 주석에 남긴다.
7. **본문에 카드·그림자·큰 라운딩을 쓰지 않는다.** 이 문법에서 그림자는 **떠 있는 것**(모달 ·
   드롭다운)만 갖는다. 구획은 `■ 제목`과 여백으로 나눈다.
8. **전역 `flex-wrap: nowrap`을 걸지 않는다.** 좁은 폭에서 버튼이 겹친 직접 원인이었다.
   접기는 §6.3대로 구간별로 준다.
9. **산문을 좁은 2열 격자 칸에 넣지 않는다.** 가변 길이 값이 옆 칸과 높이를 어긋나게 만든다.
   같은 결함이 이 프로젝트에서 세 번 재발했다(§4.4).
10. **부정적 액션을 붉은 버튼으로 만들지 않는다.** 목업에 붉은 버튼이 없다. 파괴적 결과는
    색이 아니라 확인 단계가 막는다.

**이 목록에 없는 것은 금지가 아니다.** 규칙을 추가할 때는 이유를 함께 적고, 이유가 사라지면
규칙도 폐기한다. 구판의 "좌우 분할 마스터-디테일 금지"는 이유 없이 적혀 있었고, 그 뒤 이유 없이
되살아났다가, 이번에 **목업이라는 이유**로 다시 폐기됐다(§5 WORKLIST).

---

## 8. 검증

### 기계 — 정식 게이트

| 검사 | 무엇을 막는가 | 상태 |
|---|---|---|
| `scripts/test_screen_scaffold.py` | `SCREEN_ARCHETYPE`이 허용 유형인가 · 표준 헤더 크롬을 실제로 호출하는가 | 운영 중 |
| **`scripts/check_streamlit_selectors.py`** | **Streamlit 내부 선택자 생존** — 소스에서 선택자를 뽑아 실렌더에서 매치 수를 세고, 엔진 훅(`data-baseweb` · `react-aria` · `st-emotion`) 선택자가 0건이면 exit 1 | **이 문서로 정식 게이트 편입** |

**선택자 검사 운영 규칙**

- **셸·공용 CSS(`modules/ui.py` · `views/common/erp/` · 신설 셸 모듈)를 건드린 변경은 반드시 돌린다.**
- **Streamlit 버전을 올린 직후에는 무조건 돌린다.** 1.59의 BaseWeb → react-aria 전환이 정확히
  이 검사가 있었으면 즉시 잡혔을 사고다.
- 매치 0이면서 **같은 파일의 다른 선택자는 매치한** 경우만 `DEAD`로 올린다. 파일 전체가 0이면
  그 화면이 렌더되지 않았다는 뜻이므로 `UNVERIFIED`로 분리한다 — 도달 불가 화면을 결함으로
  오인하지 않기 위해서다.
- `--static-only`는 브라우저 없이 목록만 뽑는다. `--strict`는 `data-testid` 훅까지 게이트에 넣는다.

**아직 없는 검사**(§13) — ① 소스의 `font-size`가 §2.2 단계 안인가 ② `Pretendard`·`IBM Plex`·
모노 서체 지정이 남아 있지 않은가 ③ `SCREEN_DECISION` 문장 존재 여부.
③이 붙기 전까지 §0은 규범이지 집행 사항이 아니다.

### 측정 — pixel-qa (렌더 DOM 기하)

- 토큰 밖 색·크기 사용 여부
- 대비율 4.5:1 이상 (읽는 텍스트 — `ink-glyph` `#8a8680`와 `ink-placeholder` `#b3ada6`는
  비텍스트·안내 전용이라 대상 밖이다)
- **1366 / 1024 / 800px 세 폭**에서 겹침 · 잘림 · 가로 오버플로 0
- 표 행 높이 편차 0 (29px)
- 셸 기하 5종(56 / 40 / 206 / 29 / 28) 일치

### 사람

사용자 실브라우저 sign-off. **최종 게이트다.**
기계가 판정할 수 없는 것(정보 우선순위, 근거 배치의 적절성)을 기계 검증 항목으로 만들지 않는다.

---

## 9. 목업에 없는 화면을 만들 때

1. §3 셸에 화면을 얹는다 — 조회조건 줄 → `■ 구획` → 표.
2. 색·크기·행 높이는 §2 토큰에서만 고른다. **새 값 금지.**
3. 상태는 낱말 + 색(§4.8), 편집 가능성은 배경색(필수 살구 · 읽기 전용 `field-ro`)으로 말한다.
4. 설명문은 구획당 최대 한 줄. 문법(색 · 정렬 · 잠김)이 말할 수 있는 것을 글로 쓰지 않는다.
5. 확신이 없으면 **목업의 가장 비슷한 구획을 열어 그대로 조립한다.**

새 모듈이 들어와도 §5의 6유형 밖으로 나가지 않는다 — 시험성적서 요청 · 업무요청 · 숙소 예약은
전부 `FORM_ENTRY`(신청) + `WORKLIST`(처리) 쌍이다. **계측기 주기관리만 다르다**(주기 도래 임박이
핵심이라 요청·처리 흐름이 아니다) — `WORKLIST` 변형인지 새 유형인지는 그 화면을 설계할 때 정한다.

---

## 10. 기능 계약 — `requirements.md` 이관 대기

**이 절은 시각 기준이 아니다.** 구판 §7.2에 섞여 있던 기능 결정을 잃지 않으려고 보존한 것이고,
정본은 `docs/requirements.md`다. 아차사고 절이 신설되면 이 절은 통째로 옮기고 여기서 지운다.
**옮기기 전까지는 이 절이 그 결정들의 유일한 기록이다.**

담당은 `ui-feature`가 아니라 **`data-contract`** 다. migration·전이 상수·DB 문서는
`AGENTS.md` 승인 게이트 위에 lee-mode Codex 교차검증 2단이 더 붙는다.

### 10.1 아차사고 평가 등급

- **등급은 5×4 격자에서 산출한다** (2026-08-18 사용자 결정). 평가자는 등급을 직접 고르지 않고
  **칸 하나**를 누른다. 칸 = (가능성, 중대성) = 등급. 척도는 KOSHA 위험성평가지원시스템(KRAS)의
  가능성 5단계 × 중대성 4단계를 따른다.
- **조합은 곱셈법이 아니라 행렬법**이다. 칸마다 등급을 직접 지정한다.
  곱셈법을 쓰지 않는 이유 — `중대성 4(사망·영구장애) × 가능성 2(드묾) = 8점`이 "허용 가능" 구간에
  떨어진다. **아차사고 관리에서 가장 놓치면 안 되는 유형이 가장 낮게 나온다.** 20칸 분포도
  `D 10 / A 1`로 치우친다. 덧셈법은 반대로 빈도를 과대평가한다.
- **격자를 지배하는 두 규칙** — ① **중대성 4는 최소 A**(사망 가능은 드물어도 중대하다)
  ② **중대성 1은 최대 C**(안 다치는 일은 아무리 잦아도 상한이 있다).
- **아직 임시값** — 5단계·4단계의 **라벨 문구**(현재 `거의 없음/드묾/가끔/자주/상시` ×
  `무휴업/단기 휴업/장기 휴업/사망·영구장애`)와 **20칸 각각의 등급**. 사내 안전 기준이 있으면
  **라벨과 칸 값만 갈아끼운다.** 격자 구조와 화면은 그대로다.
- **오버라이드는 허용하되 변경 사유가 필수다.** 사유 없이는 평가확정이 활성화되지 않고,
  화면은 `산출 B → 확정 A` 형태로 **바꿨다는 사실 자체를 남긴다.**
  사유가 없으면 격자가 장식이 되고, "격자를 안 따른 비율"을 볼 수 없다 — 그 비율이 높으면
  개별 판정이 아니라 **격자 자체를 고쳐야 한다는 신호**다.
- **미해결 — `frequency` · `severity` 컬럼 migration.** 현재 `confirmed_grade`만 있어
  **등급의 근거가 저장되지 않는다.** 공정성이 목적인데 근거를 안 남기면 반쪽이다.
  산출 등급은 컬럼을 두지 않고 두 축에서 파생한다.
- **백필은 설계하지 않는다.** 현재 아차사고 데이터는 전부 샘플이며 폐기 예정이다(2026-08-18 확인).
  새 컬럼은 nullable로 추가하고 기존 행은 비운 채 둔다.
- **제안등급(`proposed_grade`) 폐기.** 등록 폼에 입력 경로가 없어 신규 건은 항상 비는데도
  4개 화면·PDF에서 표시된다. 표시를 전부 제거한다. DB 컬럼 제거 여부는
  `frequency`/`severity` 추가 migration과 **한 번에 묶어 판단**한다.

### 10.2 상태 전이

- **'평가 착수' 단계를 없앤다** (2026-08-19 결정). 평가 대기(`SUBMITTED`)에서 곧장
  **보완요청 · 반려 · 평가확정**을 누른다.
  착수는 그 자체로 아무 판정도 아니면서 모든 판정 앞에 클릭 한 번을 더 놓았다. 담당자 귀속이
  목적이라면 착수가 아니라 **판정 자체**가 기록해야 하고, 실제로 그렇게 되어 있다.
- `IN_REVIEW`(`평가중`) 상태값과 탭은 **유지한다**(통계·PDF·개선조치가 쓴다) —
  평가 관리 화면이 그 값을 **만들지 않을 뿐**이다.
- 넓어진 것은 보완요청 허용 상태 하나(`db._NEAR_MISS_REVISION_STATES = {SUBMITTED, IN_REVIEW}`)이고
  정본은 `docs/database.md` §8이다.
- **어휘는 한 도메인에 한 낱말이다.** 아차사고 평가의 정본 = 화면명 `평가` · 상태
  `평가 대기 → 평가중 → 평가완료` · 액션 `보완요청 · 반려 · 평가확정` · 등급 `평가 등급`.
  영문 코드값(`IN_REVIEW` · `confirmed_grade`)은 바꾸지 않는다 — 코드값이 한글 표기를 규정하지 않는다.
  좁은 표 헤더에서는 줄일 수 있지만(`평가 등급` → `등급`) 다른 낱말로 바꾸지 않는다.

### 10.3 경과일

- **접수일(`created_at`) 기준**이다. `incident_date`가 아니다 — 평가자의 책임 구간은 접수 이후다.
  `006_near_miss.sql:69` 주석의 "기간 통계는 `incident_date` 기준"은 **집계 축**에 대한 규칙이고,
  경과일은 처리 지연 측정이라 별개다. 나중에 "일관성"을 이유로 바꾸지 않도록 이 구분을 남긴다.
- **반송 건은 최초 접수부터 누적한다.** 리셋하면 반송을 반복해 경과일을 0으로 되돌릴 수 있다 —
  지연을 숨기는 경로를 만들지 않는다.
- **미해결** — 계산 코드가 아직 없다(`timestamptz` → 한국 날짜 경계 처리 필요).
- 시각 규칙(강조하지 않는다)은 §4.8이 소유한다.

### 10.4 권한 · 담당자

- **USER 조회 범위는 MANAGER와 같은 자기 부서다.** 대시보드 역할 통합에 따라 필요해졌다.
  같은 부서의 오늘 근무는 현장에서 이미 공유되는 정보고, `_scope_for`의 기존 fail-closed 패턴을
  그대로 재사용하면 새 권한 축이 생기지 않는다. 부서 미확정이면 전체 차단.
- **담당자 선점(`review_started_by` / `review_started_at`) migration 미작성.**
  `supabase/migrations/`는 001~011(005 결번). 착수 폐기와 별개 축이다.
- 목업의 사용자 모달은 **권한그룹 + 담당 지정 5종**(안전담당 · 숙소담당 · 근무표 편성 ·
  업무요청 처리 · 알림 메일 수신)을 보여준다. 현재 `ADMIN/MANAGER/USER` 3권한과의 관계는
  `requirements.md`가 정할 문제이고 이 문서 밖이다.

### 10.5 배포 상태 · P1 결함

- **평가 관리 보완요청 버튼이 활성인데 클릭하면 실패한다.**
  `views/near_miss_evaluate.py:171`의 활성 게이트는 **006 프로브**를 쓰는데, 실행부
  `modules/db.py:2566` `request_near_miss_revision`은 **007 프로브**로 fail-closed 차단한다.
  006만 적용된 배포에서 버튼이 눌리고 실패한다. §4.8("불가능한 전이는 그리지 않는다") 위반이자
  실사용 버그다.
- 006/007의 **운영 프로젝트** 적용 여부는 여전히 어느 문서도 말하지 않는다.
  테스트 프로젝트 적용은 `docs/database.md`(2026-08-07 live 확인)와 `WORKLOG.md:284`가 기록한다.

---

## 11. 목업과 의도적으로 다른 곳

**이 목록에 없는 어긋남은 버그다.** 각 항목은 되돌리는 방법까지 적는다.

| # | 항목 | 목업 | 현재 기준 | 왜 |
|---|---|---|---|---|
| 1 | 달력 요일 머리 | `일` 칸만 30px / 12.5px, 나머지 6칸 27px / 11.5px | **전 칸 27px / 11.5px** | 같은 줄에서 한 칸만 높으면 나머지 6개 라벨이 3px 떠 보인다. 목업 자체 불일치다 — 네 군데 중 `숙소 예약 승인` 한 곳만 전 칸 27로 자기 일관적이고, 그 판을 따른다 |
| 2 | 격자 요일 헤더 색 | 일 `#b3352f` · 토 `#1c5aa8` | **일 `#a3282a` · 토 `#17457f`** | 같은 일을 하는 색이 두 벌이다. 달력 요일 머리가 쓰는 `#a3282a`/`#17457f`가 §2.1 의미색과 같은 값이라 그쪽으로 합친다. 되돌리려면 격자 헤더 두 줄만 바꾼다 |
| 3 | 전월·익월 날짜 글자 | `#c3beb6`(내 근무표) / `#c3beb8`(숙소 신청) | **`#c3beb6`** | 2 차이의 중복값. 둘 중 더 많이 쓰인 값으로 합친다 |
| 4 | **글꼴** | Pretendard Variable (CDN) → 맑은고딕 폴백 | **맑은고딕** | §2.2의 세 근거. 되돌리려면 font stack 한 줄 |
| 5 | FORM 라벨 셀 폭 | **132px 고정** | 비율(실측 141px) | Streamlit 컬럼은 px 고정을 표현할 수 없다(§6.3). 접근 방법이 생기면 132로 되돌린다 |
| 6 | WORKLIST 골격 | (목업은 승인 큐를 전폭 표로 그린다) | 전폭 표 + 구획 제목 줄 액션 | 구판 DESIGN.md가 명시한 2열 33/67을 폐기한 것이다. **목업이 아니라 구판 문서와 어긋난다** — §5 WORKLIST 참조 |
| 7 | 모바일 셸 | 768px 이하 전용 셸 + 6화면 | 미구현 | Phase 3 이관 대상. 현행 USER 셸에 신 토큰만 적용한 상태로 남는다(§12) |

---

## 12. 미측정

**값을 지어내지 않는다.** 아래는 목업이 답하지 않거나 아직 재 보지 않은 것이다.
쓰기 전에 반드시 측정하고, 측정하면 이 목록에서 빼고 본문에 값을 넣는다.

| 항목 | 상태 |
|---|---|
| 모바일 셸(≤768) 실렌더 계측 | 목업 확보(`docs/mockup/WorkOps 모바일.dc.html` · 63,110B). Streamlit 이관·계측은 Phase 3 |
| 사이드바 폭 드래그(150–340) | 목업 기능. Streamlit 구현 가능성 미측정 — GNB 브랜드·리본 검색 영역이 같이 따라가야 한다 |
| 모달(§4.7)의 Streamlit 수단 | `st.dialog` 폭·머리 커스터마이즈 가능 범위 미측정 |
| 표 스크롤 컨테이너 높이 | 목업은 `max-height:520` 하나뿐이다. 그보다 긴 목록·짧은 목록의 규칙 없음 |
| 선택 행 위 hover | 목업 상태값이 같은 값(`accent-tint`)이라 시각 변화가 없다. 실렌더 미검증 |
| AgGrid 2줄 헤더·칩의 JsCode 없는 대안 | 미탐색. 없으면 §6.1 판정대로 격자 조회는 DOM 표다 |
| 포커스 링 · 키보드 순회 | **목업은 정지 화면이라 포커스 상태가 없다.** 없어도 된다는 뜻이 아니다 — 규칙을 새로 정해야 한다 |
| 인쇄(print) 스타일 | 목업에 없음 |
| 알림 카운트 · `DB 연결됨` 뱃지의 데이터원 | 목업은 고정값(`1` · 연결됨). 실제 무엇을 세는지 미정 |
| 사용자 모달의 `담당 지정` 5종 | 현행 3권한 모델과의 관계 미정 — §10.4 |
| 조회조건 컨트롤 28 vs 30의 경계 | 목업은 기간 이동기가 있는 줄만 30이다. 규칙으로 굳힐지 목업 슬립인지 미판정 — 현재는 **실측대로** 적어 뒀다 |

---

## 13. 미결

| # | 항목 | 상태 |
|---|---|---|
| 1 | **타이포·색 정적 검사 미구현** | §8이 요구하는 `font-size` 단계 검사와 폰트 지정 잔존 검사가 없다. §2.2 스케일이 바뀌었으므로 종전 4단계(24·20·14·12) 검사안은 폐기하고 새로 짠다 |
| 2 | **필터 축 라벨** | 근무표 편성의 "부서" 필터가 대분류를 고른다(`schedule_edit.py:300`). 라벨과 값이 다르다. 목업도 `부서 / 전체 부서`로만 적어 답하지 않는다 |
| 3 | **`SCREEN_DECISION` 미선언** | 22화면 전부 미선언. §8 기계 검증도 미구현 |
| 4 | **구판 § 번호 인용 dangling** | 코드 주석·docstring 13파일 40곳이 `§0-1`·`§1-A`·`부속서` 등을 인용하고, `docs/BACKLOG.md` 4건도 폐기된 절을 가리킨다. **이번 개정으로 절 번호가 전면 교체되므로 대상이 늘어난다** — 스킬·에이전트 정의(`screen-design` · `ux-review` · `visual-review`), `CLAUDE.md:64`, `README.md`, `HANDOFF_PROMPT.md`의 § 인용을 전수 재확인해야 한다 |
| 5 | **스킬 자산 결손** | `screen-design/references/`가 비어 있다. `duty-app`은 `SKILL.md` 없는 빈 디렉터리다 |
| 6 | **`requirements.md`에 아차사고 계약 부재** | §10의 어느 항목도 `docs/requirements.md`에 없다. 아차사고 절 신설이 필요하고, 신설되면 §10은 통째로 옮긴다 |
| 7 | **`requirements.md:27` 개인 대시보드** | USER 권한에만 "개인 대시보드"가 별도 항목으로 적혀 있어 §5 DASHBOARD의 역할 통합과 어긋난다. **목업이 답은 정했다**(근무 현황 1화면 · 역할 분기 없음) — 문서 정정만 남았다 |
| 8 | **서체 고지 정리** | `THIRD_PARTY_NOTICES.md`의 Pretendard 항목이 §2.2로 대상이 사라졌다. 항목 제거 또는 "사용하지 않음"으로 정정 |
| 9 | **`erp.metric_strip` 처분** | 9화면이 제목 직후에 깔고 있다. §7-1과 §5 DASHBOARD가 전부 대체 수단을 정했으므로 **제거 대상**이고, 남은 것은 화면별 이관 순서다 |

---

## 부록 A — 구판 §7 처분표

구판 `DESIGN.md` §7.2 표는 34행이었다(번호가 중복된 쌍 3개 포함).
전부 아래 다섯 가지 중 하나로 처분했다. **버린 것은 없다.**

| 처분 | 개수 | 뜻 |
|---|---|---|
| **목업이 답함** | 9 | 목업이 답을 내려 이 문서 본문에 흡수됐다 |
| **§10 기능 계약으로 보존** | 12 | 시각 기준이 아니라서 §10에 옮겼다. `requirements.md` 이관 대기 |
| **신판 규칙으로 승계** | 1 | 결론은 그대로이고 조항 위치만 바뀌었다 |
| **폐기** | 2 | 전제가 사라졌다 |
| **해소 — 변경 없음** | 4 | 구판에서 이미 닫힌 항목. 유지 |
| **여전히 미결** | 6 | §13으로 이월 |

### 상세

| 구판 # | 항목 | 처분 | 근거 |
|---|---|---|---|
| 1 | 등급 산출 방식 (5×4 행렬) | §10 보존 | §10.1 |
| 1-b | 제안등급 폐기 | §10 보존 | §10.1 |
| 1-c | 격자 값 (라벨·20칸) | §10 보존 | §10.1 |
| 1-d | 오버라이드 + 사유 필수 | §10 보존 | §10.1 |
| 1-e | `frequency`·`severity` migration | §10 보존 | §10.1 · `data-contract` 소관 |
| 1-f | 기존 평가 데이터 (전부 샘플) | §10 보존 | §10.1 |
| 2 | 근무형태 색 | **목업이 답함** | §2.1 도메인 색 — 목업이 `근무형태 관리`에 `색상` 열(`■ #1e6fd9`)을 데이터로 싣고, 셀은 의미색 글자 + 테두리 칩으로 그린다 |
| 3 | 근무형태 종류 수 | **목업이 답함** | 고정하지 않는다(목업 49종 · 범례 `외 42종`). 약칭 3자를 입력 제약으로 걸고 셀 칩 폭 30으로 설계한다 — §2.1 |
| 4 | 경과일 기준 (접수일) | §10 보존 | §10.3 |
| 4-b | 경과일 강조 임계 | **신판 규칙으로 승계** | §4.8 "SLA가 없으면 색을 칠하지 않는다" |
| 4-c | 반송 건 경과일 누적 | §10 보존 | §10.3 |
| 5 | 헤더 아이콘 — 화면별 노출 | **목업이 답함 (결정 뒤집힘)** | 목업 리본은 **7종 상시 고정**이고 비활성 표현이 없다. 정본 순서상 목업이 이긴다. 대신 화면 액션이 아이콘에서 §4.9 글자 버튼으로 내려와 §7-3 충돌이 소멸했다 — §3.3 |
| 5' | 헤더 아이콘 (미해결 중복 행) | **폐기** | 위 5와 같은 항목이 표에 두 번 있었다 |
| 6 | 착수 없이 반려 (착수 폐기) | §10 보존 | §10.2 |
| 6' | 필터 축 라벨 (부서 = 대분류) | **여전히 미결** | 목업도 답하지 않는다 — §13-2 |
| 7 | USER 조회 범위 | §10 보존 | §10.4 |
| 7' | 담당자 선점 migration | §10 보존 | §10.4 |
| 8 | §4-1 vs 지표 스트립 9화면 | **목업이 답함** | 목업 대시보드에 지표 타일이 없다. **합계 행**(`accent-tint-2` · 700)이 그 자리를 대신한다 — §5 DASHBOARD. `erp.metric_strip`은 제거 대상(§13-9) |
| 9 | `내 업무요청` 유형 | **목업이 답함** | READ_VIEW. 목업이 조회 표 + [수정][취소]로 그린다. 2지 판정은 처리자 화면 소관 — §5 READ_VIEW |
| 10 | 타이포 마이그레이션 (4단계) | **폐기** | 스케일 자체가 바뀌었다(24·20·14·12 → §2.2). 종전 마이그레이션 대상·잔여 목록은 무효이고 새 스케일로 다시 발행한다 |
| 11 | 구판 § 번호 dangling | **여전히 미결** | 이번 개정으로 대상이 늘어난다 — §13-4 |
| 11-b | 스킬 자산 결손 | **여전히 미결** | §13-5 |
| 12 | `SCREEN_DECISION` 미선언 | **여전히 미결** | §13-3 |
| 13 | 006/007 적용 상태 | §10 보존 | §10.5 |
| 14 | `requirements.md` 아차사고 계약 부재 | **여전히 미결** | §13-6. §10이 이관 목록을 제공한다 |
| 15 | `requirements.md:27` 개인 대시보드 | **여전히 미결** | 목업이 답은 정했고 문서 정정만 남았다 — §13-7 |
| 16 | `supabase-setup.md` stale | 해소 — 변경 없음 | 2026-08-18 해소 |
| 17 | `HANDOFF_PROMPT.md` | 해소 — 변경 없음 | 2026-08-18 재작성. 단 § 인용은 §13-4 대상 |
| 18 | `§0` 오인용 | 해소 — 변경 없음 | 2026-08-18 정정. 단 § 인용은 §13-4 대상 |
| 19 | `README.md` 인벤토리 결손 | 해소 — 변경 없음 | 2026-08-18 해소 |
| 20 | 서체 고지 | **목업이 답함** | §2.2로 웹폰트를 쓰지 않게 되어 고지 대상이 소멸했다. 항목 정리만 남았다 — §13-8 |
| 21 | 신규 모듈 4종 | **목업이 답함** | 목업 사이드바가 업무요청 4화면 · 숙소 예약 4화면을 이미 갖고 있고 전부 §5의 기존 유형에 든다. 시험성적서·계측기 주기관리는 목업에 없다 — §9 |
| 22 | 사이드바 그룹 8개 | **목업이 답함** | 단독 1 + 모듈 그룹 5(근무표 · 아차사고 관리 · 업무요청 · 숙소 예약 · 기준정보). 평평한 8개도, 업무영역 묶음도 아니다 — §3.4 |
| 23 | 서체가 CDN 의존 | **목업이 답함** | §2.2가 원인을 제거했다. 내부망 차단 시 폴백 문제 자체가 없어진다 |

### §7.1 (확정된 이관) · §7.3 (lee-mode 정합) · §7.4 (P1 결함)

- **§7.1 WORKLIST 이관 — 부분 폐기.**
  `ARCHETYPES`에 `WORKLIST` 추가 · `MASTER_DETAIL` 제거 · `SCREEN_ARCHETYPE` 7개 재배치 ·
  `test_screen_scaffold.py:492` 튜플 갱신 · `test_near_miss_ui_gates.py:161,168` 갱신은
  **그대로 유효하다.** 반면 **골격(상태 탭 + 2열 33/67)과 그 구현 수단은 폐기**한다 —
  `erp.master_detail_frame(1, 2)` · `st.segmented_control` 상태 탭 · `erp.select_list` 신규 작성.
  §5 WORKLIST가 새 골격이다. `NEAR_MISS_TRANSITIONS`·`docs/database.md` 갱신은 §10.2로 옮겼다.
  **회귀 기준선** — `scripts/test_*.py` 35개 전부 통과(원격 쓰기 `test_supabase_crud.py` 제외).
  이관 중 이 숫자가 떨어지면 원인을 먼저 찾는다.
- **§7.3-2 (UI_CENTRIC 시안 3개) — 해소.** 사용자가 목업을 승인안으로 지정했다.
  글로벌 PLAYBOOK §7의 "사용자가 정확한 결과를 이미 지정한 경우" 예외에 해당한다.
- **§7.3-3 (목업 기본형이 정적 HTML) — 해소.** `demo/`가 실제 Streamlit 프로토타입으로 기하를
  검증했다(§6). 남은 것은 §8 측정을 운영 화면에서 실행하는 것이다.
- **§7.3-1 · 4 · 5 — 유지.** migration Codex 2단, `ui-feature`/`data-contract` 담당 분리,
  "프로젝트 ERP UI 스킬" 지칭 정합.
- **§7.4 P1 결함 — §10.5로 이관.** 기능 결함이고 `data-contract` 소관이다.

---

## 부록 B — 폐기한 것

| 폐기 | 이유 |
|---|---|
| 웹폰트(Pretendard / IBM Plex) | AgGrid iframe이 구조적으로 못 받고(12.21% 폭 차), 내부망 차단 시 폴백한다 — §2.2 |
| 타이포 4단계(24 · 20 · 14 · 12) | 목업 실측이 11~15 중심이다. 24px 제목은 이 문법에 존재하지 않는다 — 화면 제목은 리본 안 15/700이다 |
| 어두운 사이드바(`#1a1917` 계열) | 목업 사이드바는 `#faf9f6` 밝은 면이다 |
| 지표 스트립 / 읽기 전용 지표 타일 | 표 합계 행과 구획 부제가 같은 일을 더 정확하게 한다 — §5 DASHBOARD |
| WORKLIST 2열 33/67 마스터-디테일 | 목업이 승인 큐를 전폭 표로 그린다. 33%에 12열 목록이 들어가지 않는다 — §5 |
| `erp.select_list` 신규 작성 | 위 골격 폐기로 필요가 사라졌다 |
| 상태 탭(`st.segmented_control`) | 상태는 조회조건 줄의 select다 — §5 WORKLIST |
| 표 셀 안의 상태 배지 | 낱말 + 의미색이 정본 — §4.8 |
| 밀도 3단(cozy / compact / condensed) | 목업은 밀도가 하나다(행 29 · 컨트롤 28). 격자만 예외를 갖는다 — §2.3 |
| 화면별 픽셀값을 코드에 복제하는 방식 | 값이 드리프트했고 정렬 실패의 직접 원인이었다. 값은 §2 토큰에서만 고른다 |
| 전역 `flex-wrap: nowrap` | 좁은 폭에서 버튼이 겹친 직접 원인 — §6.3 |
| 부속서 정정 방식 | 본문과 충돌하는 정정이 8개까지 쌓였다. 정정은 본문을 고쳐서 반영한다 |
| Streamlit 위젯 일반 대응표 | "이럴 땐 이 위젯"은 도구 사용법이지 설계 기준이 아니다. §6은 대응표가 아니라 **이 목업을 세울 때 무엇이 막혔는지의 기록**이다 |
